# Molis Work：来源清单、OAuth 与有内容的项目

状态：核心动线功能可用，真实 Gmail → Cognia → Prologue → Projects/Pages 已于 2026-09-25 验证。用户于 2026-09-24 授权先用 imagegen 设计图、完成高保真交互，然后开始生产开发。沿用已确认来源清单和自动整理动线，替换旧的 Goal/Runtime 首次向导；不改变全产品视觉体系。

完成目标：关键动线功能可用并有真实集成证据。第三方正式 OAuth 应用注册/发布审核、用户账号同意属于外部条件；不能把模拟授权或测试响应宣称真实账号可用。先完成可独立开发验证的代码，有外部缺口时明确报告，不把可开发部分留成计划。

## 行为

- 首次使用与新项目共用来源清单，可勾选/全选、调整范围、跳过或空白创建；不要求先写 Goal 或配置 Runtime。
- 账号连接从清单发起，复用全局连接。使用已配置的产品 OAuth 客户端，打开服务商官方授权页；返回后保留清单和范围，恢复本轮整理。普通用户流程无 Client ID/Secret/Token 输入。提供商或产品 OAuth 未就绪时明确显示原因，不假装连接成功。
- 已授权来源按选定范围读取，正文保留来源。成功内容可独立继续；失败可重试/跳过；刷新可恢复批次。来源能力以真实实现为准，不用图标代表完整读取能力。
- 由配置的模型生成带来源的工作总结与项目建议。未配置/失败时保留材料并提供恢复；不以硬编码样例代替真实总结。
- 用户采用后创建项目与可编辑摘要，材料引用有持久归属，重试不重复创建；不自动生成正式 Goal。空白项目仅需名称。
- 清单和总结适配浅深主题、桌面/窄屏，连接等待/回跳/取消等状态可体验。

## 实施边界与已有工作

仓库 main 当前有 429 个已有变动，包含连接器、Cognia、设计系统和项目设置的其他工作。保留其现状，仅在必要入口增量接线，不重置、覆盖或整体格式化。旧隔离原型仍保留在 `docs/design/molis-work-onboarding/prototype/`；新生成图及其精确 prompt 放在 `docs/design/molis-work-onboarding/oauth-redesign/`。

预计模块：Workbench onboarding renderer/client；Local Host onboarding orchestration 与连接回跳；Cognia/Pages/项目的公开能力；专属测试。凭据沿用 Secret/Connector owner。业务数据不写入设计原型 localStorage，不复制插件私有表。具体复用边界以代码核对后补充。

## 验证

先验证图稿与高保真主屏，再验证真实入口：全选/范围/零选择、连接发起与回跳关联、失败取消、读取范围、真实模型输入输出契约、采用与幂等、刷新恢复、空白创建、窄屏与键盘。自动测试使用隔离 home 和注入端口，第三方真人账号测试单列。运行相关包类型检查、定向业务测试及真实启动操作；不为截图运行无关全仓测试。

验收配置：用户确认 Relay 是其自有 Google OAuth 测试应用，并亲自完成授权；另行明确允许最近 7 天最多 20 封正文经 Prologue 发送 MiniMax-M3。Molis 正式 OAuth 客户端和提供商发布审核仍是发布前条件，不以本次测试应用替代。

## 核对实现后的首批接线

- Host 新增 `context-onboarding-{store,service,sources}.ts` 与 `web-context-onboarding.ts`，管理 Home 内可恢复批次，Cognia 持有原文和固定版本，Pages 持有项目文档。Workbench 新增对应 renderer/script/style；旧 update 通知独立保留，首次/新项目入口切换。
- 首批真实来源：用户明确选定目录或上传的文本文件、粘贴的浏览器正文及原网址、Gmail 指定账号最近 7/30 天最多 20 封的正文。IM 通过用户上传导出的文本接入。浏览器标签页自动捕获、飞书等聊天正文 OAuth 不在现有底座中，界面明确未接入，不伪装可读取；作为后续 provider 接入项保留。
- 一轮材料先形成一个可编辑项目建议；自动分成多个项目留在设计原型，不把模型猜测直接写入正式项目。每轮最多 50 份文本，上传总大小最多 6 MB，网页粘贴最多 100,000 字符。超过单次模型上下文的材料分批提取带来源的事实再汇总，不静默截断或假装读完。

真实邮件验收补充：用户已明确授权读取最近 7 天最多 20 封邮件并发送 MiniMax-M3。本次真实读取 20 封约 47.6 万字符，证明旧的总字符硬上限阻断正常主路径。改为完整原文保存在 Cognia、单次模型输入有界、分批事实提取再汇总；各批次沿用原始来源标记，最终引用仍打开原文。持久保存成功的中间结果供超时/重启后续跑，改变资料版本或模型后失效；界面展示批次进度。不得减少已授权的 20 封以绕过此问题，不在仓库记录私人邮件内容。验证覆盖长文末尾事实、分批引用校验、中途失败恢复与真实已读邮件继续处理。
- OAuth 复用 Gmail 的 state/PKCE 和按账号 Secret refs；只允许固定 onboarding 回跳，state 绑定 journey；取消/失败可回到原清单。产品未配置客户端时显示接入未就绪，其他来源继续。
- 批次创建/导入/采用以稳定请求标识恢复；项目创建 owner 接受经过校验的稳定 project_id，以处理创建后响应丢失。Pages 使用已有 importDocuments 幂等事务。创建摘要与每份来源快照，加入 Pages 插件并直接打开摘要文档；不自动创建 Goal。

补充实现：模型未配置时允许“带入资料，先开始”，保留真实来源快照而不生成虚构摘要。摘要编辑写回批次。原生桌面 OAuth 使用已有 open-external 桥打开系统浏览器，并观察批次授权状态自动接续；浏览器回调页提示回到 Molis。取消会清理对应 PKCE pending 状态，迟到回调不能重新绑定为旧单账号流程。

真实采用边界修正：邮件原标题和项目名称可能超过 Pages 的 80 字标题契约。采用时仅缩短 Pages 显示标题（保留来源编号、使用省略号、不截断代理对），完整原标题保留在文档正文和来源记录；项目名称仍沿用 120 字契约。失败后的采用继续使用已确认内容和同一项目，不重读邮件或重新生成摘要。补验长邮件标题、长项目名称和重试幂等。

视觉实操修正：清单同时呈现已选与可读取数量；零可读时主动作先添加内容，未配置 Google 时在折叠行直接显示暂不可连接。没有模型时明确可先导入资料，避免暗示摘要已经可生成。深色沿用根 DESIGN token。

## 完成审计 · 2026-09-25

目标等级为功能可用，核心闭环已完成。imagegen 图稿、高保真交互、正式实现和真实材料验收均已交付；不等于可发布或一骏本人已验收。完整证据以 `docs/design/molis-work-onboarding/oauth-redesign/verification.md` 为准。

| 要求 | 验证结果 |
| --- | --- |
| 首次/新项目共用清单，无强制 Goal/Runtime | 浏览器、当前源码原生壳和 3 项入口回归；新路径空白项目 goals=0 |
| 勾选/全选/范围/未接入说明 | 浏览器全选/半选/零选、目录范围测试、原生单文件和文件夹选择器通过；Chrome 自动上传因扩展权限未实操 |
| 官方 OAuth / 回跳 / 复用连接 | 用户亲自授权 Relay；真实 token 交换、账号连接、保留清单和范围通过；多账号选择复用已实操；原生打开系统浏览器和拒绝返回已实操，未重复正向授予权限 |
| Cognia 原文版本 → Prologue 总结 | 真正读取最近 7 天最多 20 封正文、不含附件；20 份原文约 47.6 万字符、7 批提取和最终汇总；全部原文和版本核对一致 |
| 编辑、引用、采用 → Projects/Pages | 显式保存编辑并刷新恢复；真实项目含 1 份已编辑摘要和 20 份可编辑来源；全部来源链接和完整正文核对一致 |
| 部分成功、跳过、重试、中断恢复 | 14 项 context 业务测试；普通入口继续上次、修改来源保留成功快照、采用失败后恢复同一项目的浏览器实操 |
| 无模型保存资料 / 空白开始 | 隔离 Home 浏览器和原生壳实操，持久化与无 Goal 测试通过 |

真实 Gmail 批次：`803a77fa-a21a-481d-a22d-565ff8331293`；最终项目：`project-onboarding-803a77fa-a21a-481d-a22d-565ff8331293`。状态 complete、错误已清除，未重建第二个项目。私密正文、凭据和截图不进入仓库。

本次真实验收暴露并修复：长材料超上限导致整理中断；多账号时入口错误引导重复连接；完成编辑依赖失焦才保存；长邮件标题不符合 Pages 标题契约。长材料使用成功批次检查点恢复；最终摘要确实来自 Prologue，自动初稿仍需人工精炼，本次保存的是经审阅编辑的版本，不宣称模型自动达到最终质量。

工程证据：相关包构建通过；14 项 context + 8 项 Gmail/连接选择 + 3 项入口回归，共 25 项通过。未跑有大量既有改动的全仓测试。原生 QA 壳与 4173 后端已退出，4339 保留实际项目预览。

边界保持：浏览器通过粘贴正文、IM 通过导出文本；自动标签页捕获、实时 IM Connector、多项目自动归类和持续同步尚未实现。正式 OAuth 发布配置、发布安装包与更广泛的摘要质量评估不包含在已完成的功能等级中。

## 2026-09-26 扩展：macOS 独立目录授权与工作准备体验

状态：用户已确认上述方向并授权开工；本轮已实现并达到功能可用，原生授权/恢复/读取与浏览器最终采用通过，最终构建的整轮原生采用仍受宿主启动健康探针延迟和随后 CUA 故障影响，未宣称通过。保留既有 Prologue、Pages、OAuth 和空白开始；在当前中性色产品视觉内升级完整交互，不迁移全局设计系统。与 Cognia 2 owner 对齐：通用资料由 Host Artifacts 持有，Cognia 2 消费项目内固定版本引用，不延续旧 Cognia 导入链路。

### 场景与行为

首次与新项目采用四步连续流程：选来源 → 补齐访问权限 → 预览本次内容 → 整理与采用。下载、文稿、桌面、其他文件夹分别勾选和记录授权；邮件/网页/聊天保留已有能力。统一主动作依次处理所选未授权来源，可跳过或失败后继续。勾选不触发系统授权或正文读取，目录预览仅读文件元数据；用户点击开始后才读取明确选中的正文、交给模型。原生授权由系统选择器完成，拒绝、取消、目录失效要保留其余状态，不冒充系统 TCC 总状态。

每个目录可选最近 7/30/90 天或全部；目录元数据按最近修改排序、显示数量/大小/文件类型，可逐文件或子目录排除。预览有界并明确上限，不静默把未列出内容送给模型。取消选择后不参与读取；取消连接清除 Molis 的目录访问凭据，不删除已导入快照，也不声称能撤销 macOS 系统权限。授权凭据跨应用重启保存；每次预览和读取重新验证，过期提示重新选择。浏览器版继续显式上传，不能伪装持有原生授权。

PDF 文本层与 DOCX 正文接入本次导入（本地解析、不获取外部资源）；扫描 PDF、加密/损坏/无正文文件明确跳过并显示原因。采用前原始文件及抽取文本暂存于本轮 journey；采用创建目标项目后，经 artifacts.import.file@1 幂等注册 io.molis.work.document@1，保存原始文件、抽取文本及准确 ArtifactReference。不会把 Home 引用直接搬进项目。历史未完成 Cognia 批次保留原记录并明确提示重新选择；不把旧 material_id 强转为新 Artifact 身份，不恢复旧 Cognia 导入。扩展该公开导入契约以容纳有界原文件快照，保留旧文本调用。正常材料不因单个失败全部丢失。现有上限保持 50 份、总原始文件 6 MB，超出时引导缩小范围。摘要亦注册固定版本，Pages 保存可编辑阅读文档；后续研究复用接收 project_id、固定引用及本次意图，不复制旧 Cognia 数据。

整理页显示真实来源读取/分批进度；结果以工作主题、正文要点、待确认事项和来源阅读开始，保留编辑、重新整理和采用，不伪造模型尚未产出的发现，不自动多建项目。

### 模块与原生契约

原生桥拥有目录授权、security-scoped bookmark 持久化与 scope 内访问。主窗口 onboarding 调用（拒绝非本机 onboarding 页面/非主窗口）；不把绝对路径交给 Node 后端当授权凭证。所有正文由原生在授权 scope 内读取，再交给现有本机服务。符号链接、隐藏/依赖目录、越界路径不读取。

- `context_directory_status()` → `[{id: downloads|documents|desktop|custom, name, path: string|null, state: unconnected|ready|unavailable, message?:string}]`。已存授权才验证，未授权位置不试读。
- `context_directory_authorize({id})` → 同一条状态；系统目录选择器，取消返回旧状态或 unconnected。内置 id 默认定位对应目录，选择其他路径时将返回 path 作为实际范围在 UI 明示。
- `context_directory_forget({id})` → unconnected；仅清除本应用记录。
- `context_directory_preview({id, days:0|7|30|90})` → `{files:[{path, size, modified_ms, identity}], skipped:number, truncated:boolean}`。只列支持的 md/markdown/txt/csv/json/html/htm/pdf/docx；最多 200 项、遍历最多 10,000 项、有界深度；缺少授权/被撤销返回可恢复错误。
- `context_directory_read({id, files:[{path,size,modified_ms,identity}]})` → `{files:[{path,data?:base64,reason?:string}]}`。最多 50 份、总原始文件 6 MB；identity 绑定 device/inode 与纳秒 mtime/ctime；重新验证同一文件、范围、修改时间与大小，拒绝越界及链接；单个文件错误保留对应 reason。

Local Host 扩展 sources kinds/范围持久化与文档解析；Workbench 接桥、顺序授权、预览/排除/恢复及状态 UI。保留既有 actions owner，不直接写插件私有数据。

### 验收

用隔离测试目录验证取消、授权、重启恢复、范围筛选、文件/子目录排除、符号链接/路径越界拒绝、文件变更与部分失败；测试 PDF/DOCX 正文、空/损坏文件和既有 Gmail/摘要恢复回归。运行相关 TypeScript 构建与定向 tests、Cargo check/test；真实当前源码原生壳操作选择器/预览/采用。未经具体授权不读取个人下载/文稿内容，不自动发送新私密材料给模型。实现完成后在验证文档区分工程、原生实操和用户本人验收。

完成证据见 `docs/design/molis-work-onboarding/mac-access/verification.md`。无模型主路径改为“资料已经准备好 → 保存资料，开始工作”，真实暂存正文可先采用；摘要仍经 Prologue，采用前容量上限对有无模型路径一致。17 项服务完整回归、随后 2 项定向边界回归（含一项复测）、2 项文档、1 项 Artifact、9 项原生测试通过。隔离目录实际保存为一个项目，3 份原始字节与 4 份 Pages/Artifact 核对通过，排除内容未带入。界面独立评审为 ship；原生完整最终采用及发布等级仍保留上述验证缺口。
