# 插件 Builder · 已确认的视觉基准

用户已确认 approved-comp.png：“可以，这个路子对了，按这个出高保真”。本次把该图译成可交互页面，前一版外观不再作为依据。

- 左栏约 26%，右侧作品约 74%；顶部 56px。暖灰工作区、近白画布、墨色文字。
- 左侧从用户需求、设计依据、三张小样到当前协作，信息连续；输入栏固定在底部。
- 右侧单一插件界面，摄影素材进入真实卡片；不使用整张概念图充当页面背景。
- 两只具名鼠标各自定位真实 DOM 控件。UI 先放元素，功能接通后原位标记，选中轮廓与当前零件一致。
- 默认展示已确认图对应的构建中状态，允许从空画布重播、暂停、继续、试用和保存独立演示版本。
- 保留明确的 Mock 边界；图片为生成的示例封面，不能冒充来源网站实证。

复用现有预览控制器、保存/校验/筛选/导出与版本隔离；本次不增模型服务或正式运行时。新 V3 草稿和素材使用独立存储键，历史演示数据保留。
