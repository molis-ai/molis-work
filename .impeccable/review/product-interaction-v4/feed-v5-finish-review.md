disposition: fix

Inputs not supplied: a separate QUALITY BAR card and a five-block/seed-bearing concept contract; this is an authorized Operate extension of the recorded Coss world, with no new approved comp. No browser or product changes; all eight named captures opened.

## persistence

Pass for this Feed scope. PRODUCT.md, DESIGN.md and the immersive-workbench surface record exist and describe the retained neutral workbench, source-task directory, readable Feed content and separate Inbox responsibility. The latest user rejection and replacement behavior are recorded in specs/product-interaction-redesign/spec.md. No comp-led reproduction or new concept selection is asserted. The eight captures show their named states without blank or corrupt regions: desktop Light/Dark, 390×844 Light/Dark and actual 1312×936 Dark. The Light desktop reader is 1435×960 rather than exactly 1440×960; the complete desktop composition remains visible and this does not invalidate that viewport evidence.

## fidelity

There is no approved comp; the following checks use the retained world and the latest Feed requirement, not an invented composition target.

| Element / promise | Result | Evidence |
| --- | --- | --- |
| TYPE | match | Compact platform text belongs to this functional Coss workbench. The 15px title, 12px summary and 14px/1.8 body distinguish scanning from reading; the actual Chinese title wraps without truncation at 390px. No decorative display lettering was promised. |
| MATERIAL | match | Neutral flat surfaces, fine boundaries and library SVG icons; no simulated physical material or missing image-native region. |
| GROUND | match | Light and Dark correspond to the documented neutral page/paper system and its shared token definitions; the reader separates from the surrounding page without a foreign-colored panel. |
| List hierarchy | match | Source/time/read state precede a stronger title and a quieter summary. Provider symbols and the right disclosure make rows identifiable and expandable. The actual 1312px viewport has the same order as the staged desktop capture. |
| Inline reading / one title | match | The open row keeps its title; the repeated detail title and summary are hidden. Body is followed by tags, a divider, destination and grouped actions. CSS limits body measure to 76ch and reduces side insets at 390px. |
| Mobile actions | adaptation | Two equal columns retain the four actions with 44px minimum button heights, as expressly required by the review packet; the primary Inbox action stays distinct. |
| Read / destination / recovery | match | Explicit unread labels appear in the captures and destination copy remains separate from read state. Supplied final test log reports the production read-state, collapse/reopen/filter and failed-detail retry regression passing. Source inspection keeps safe original links and optional attachments on the persisted path. |
| Demonstration truth | match | The project is named as an example and the reader visibly states that demo actions only change page state. The screenshots do not claim a live connection or persisted demo mutation. |
| Feed stage heading | contradicted | Every capture retains the small “Feed” eyebrow directly above “全部”. It duplicates the navigation context and conflicts with the craft-floor's explicit kicker ban. Source: plugins/native/feed/src/ui.ts, renderFeedStageToolbar. |
| THESIS / OWN-WORLD | match | Within the supplied Operate authority, the rendered Feed supports scanning source facts and acting from an inline reader using the established workbench vocabulary. |
| STORY / FIRST VIEWPORT | match | First view presents actual messages; opening one turns that row into a reading surface while keeping nearby items in sequence. The source identity, title and next action are findable without a second stacked detail header. |
| FORM | match to retained authority | This is the latest authorized Feed correction inside an existing Coss composition. No new concept roll is claimed or evidenced; the supplied contract is the Feed addendum to the existing spec. |

## ceiling

The native devices relevant to this scope are used: aligned text rows, restrained separators, a bounded reading measure, source marks, state labels and one weighted action. Reader reveal and disclosure rotation have reduced-motion overrides. An additional ornamental or image layer would not serve the retained Operate direction. A separate QUALITY BAR card was not supplied, so no card-specific ceiling claim is made.

## material_fixes

1. V5-1 — Floor / heading hierarchy: remove the redundant “Feed” eyebrow from `.feed-stage-heading` in `renderFeedStageToolbar`; retain the selected-source heading, search, filter and count. It is visibly present above “全部” in all eight captures, including the user's 1312px viewport. Recapture those same states after the change.

## keep

Keep the single inline title, clear source/time/read metadata, body-first reading order, separate destination state, grouped mobile actions, explicit demo boundary and the tested read/filter/collapse/retry behavior.
