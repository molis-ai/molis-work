# Goal 推进态势高保真切片 QA

日期：2026-08-30

Goal：goal-momentum-hifi-slice

## 完成等级与诚实边界

本轮达到 **2 · 可交互原型**。推进节奏、完整依赖拓扑、行动队列、选择详情、时间切换、范围筛选、队列空态和桌面 / 窄屏路径均可操作。

页面使用 2026-08-30 项目快照中的 30 个真实 Goal、38 条 `depends_on`、24 条 `part_of` 与当前工作状态构造演示。拓扑完整显示 30 个 Goal 与 38 条执行依赖；`part_of` 通过四个父 Goal 分组带表达，`corrects` 留在详情。7 / 30 天事件数字仍是验证界面的固定样本，不是生产实时统计。原型不会读取或写入 Molis Work 数据库，也没有替换现有关系图。

## 自动校验

- `node --import tsx --test tests/goal-momentum-prototype.test.ts`：6 / 6 通过。
- 原型页面脚本语法检查：通过。
- `impeccable detect --scope layout`：未发现规则命中；HTML 解析依赖缺失，检测器以降级正则模式运行，因此浏览器检查仍是主要视觉证据。

## 浏览器校验

### 桌面级视口

- 请求 1440 × 900 检查尺寸，浏览器实际 CSS 视口为 1152 × 720。
- 文档横向溢出为 0；拓扑视口 1105 × 650，完整画布 1120 × 1250。
- L0–L4 五层都在同一横向结构内，最右 L4 节点右缘为 1085px，桌面下无需靠大幅横向平移才能理解主链。
- 页面渲染 26 个叶子 / 独立 Goal 节点、4 个可点击父 Goal 分组标题和 38 条依赖边，共覆盖 30 个 Goal。
- 选择节点后只强调它的一阶前置与消费者，其余边降低权重；完整节点和真实关系不被删除。
- 桌面行动区为“行动队列 + 选择详情”双列，图仍是页面主舞台。

### 窄屏级视口

- 请求 720 × 820 检查尺寸，浏览器实际 CSS 视口为 576 × 656。
- 文档横向溢出为 0；拓扑视口宽 552px，画布宽 1120px，可在画布内横向与纵向平移，不把整页撑开。
- 切换“未完成 14”后显示 11 个未完成叶子节点与 3 个未完成父 Goal，隐藏已完成节点；只保留与可见节点两端都相关的 12 条边。
- 行动区在窄屏下改为单列，DOM 与视觉阅读顺序保持“节奏 → 完整拓扑 → 行动与详情”。

### 交互与语义

- 从 Session 基础节点按 ArrowRight，选择移动到最近的下游“交付全局与项目内 Sessions 管理目录”；详情同步更新，并强调它的 2 条前置与 1 条消费者边。
- 近 7 天切到 30 天后，指标由 3 / 9 / 2 / 4 更新为 15 / 14 / 3 / 6。
- 行动队列“需要决定”筛选无匹配项时显示诚实空态；可以恢复全部行动。
- 从“未完成 14”切回“全部 30”后恢复 26 个叶子节点与全部 38 条依赖边。
- 真实 Goal 详情链接只读打开新标签；页面没有 fetch、XHR、WebSocket、拖拽或可编辑状态。

## 视觉证据

- project://.impeccable/review/goal-momentum-topology-desktop.png
- project://.impeccable/review/goal-momentum-topology-narrow.png

## 验收标准映射

### goal-momentum-slice-semantics

**PASS（Runtime inspection）**。Spec 与页面逐项定义启动、完成、新阻塞、停滞、历史不足、拓扑层级、`depends_on` 箭头、`part_of` 分组和行动排序。节点层级来自依赖深度，瓶颈只作为状态，不再控制图的完整性。

### goal-momentum-slice-states

**PASS（Runtime inspection）**。正常推进、新阻塞、历史不足、完整密集关系、全部 / 未完成切换、队列空态、被阻塞节点和窄屏平移均有独立表达。图、队列和详情共享选择；桌面与窄屏检查通过。

### goal-momentum-slice-core

**等待一骏重新判断（human decision）**。需要一骏实际操作后回答：

1. 这张从左到右的完整图是否符合你说的“树状拓扑”，并且能看懂四棵 Goal 主链？
2. 当前首要瓶颈是什么，它阻挡哪些结果？
3. 行动队列前三项为什么按这个顺序？

Runtime 的测试、截图和自检不能代替这一产品判断。

## Finish review

Verdict：**READY FOR HUMAN REVIEW（符合 Level 2）**。

- 保留时间节奏与行动队列，替换的是图的语义和空间结构。
- 默认完整呈现 30 个 Goal 与 38 条执行依赖，不再用“关键链”裁剪图。
- `part_of` 以父 Goal 分组带表达，`depends_on` 才决定 L0–L4 位置；一项工作可有多个前置，因此它是树状展开的 DAG，不伪装成严格单父树。
- 瓶颈作为节点状态和选择高亮存在，不再把“解锁关系”当成整张图的结构。
- 尚未接入真实事件派生、生产排序、页面路由和旧图替换，因此不能宣称功能可用、内部完整或可发布。
