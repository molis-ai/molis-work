# 真实研究信息闭环验证记录

日期：2026-09-22。结论分开记录：工程检查通过；下表指定的 Workbench 路径已用真实数据、真实模型实操；用户验收尚未完成。未提交、推送、安装或发布 macOS App。

## 继续使用

- 起始分支：`codex/feed-inbox-pages-loop`，由已更新的 `main`（`f25ea171271ce3cfb417e19621de90194141fafd`）创建；第二轮交付时共享工作区已被并行任务切回 `main`，详见文末。
- 隔离 Home：`~/.codex/previews/molis-feed-loop`，不使用日常工作台数据。
- [研究信息闭环验证](http://127.0.0.1:4176/projects/project-43a4e132-96b5-4938-b09f-883de66389c0/)：从侧栏进入 Feed、Inbox、Functions、Pages。
- 主工作稿：**Jev 开放试用：能力边界与可验证的下一步（工作稿）**，文档 `0d63458c-e6d9-49e8-bfc5-a23cfec941e0`。
- 恢复验证稿：**Jev 的三个待验证问题**，文档 `d5e2c33f-3e07-46a2-a53c-02274377fcce`。

服务停止后，在仓库根目录启动现有构建：

```bash
NODE_USE_ENV_PROXY=1 appkey exec minimax -- node dist/web/server.js --home "$HOME/.codex/previews/molis-feed-loop" --port 4176
```

写作与助手默认使用 MiniMax-M3，密钥由 appkey 注入进程；不写入仓库。其他模型可配置 `MOLIS_WORK_TEXT_API_KEY`、`MOLIS_WORK_TEXT_BASE_URL`、`MOLIS_WORK_TEXT_MODEL`、`MOLIS_WORK_TEXT_API_FORMAT`（`anthropic-messages` 或 `openai-chat-completions`）。TypeSafe 凭据已按用户指定来源复用至这个 Home 的加密 SecretStore，Functions 设置只显示已配置状态。

## 真实输入与结果

来源选用 `molis-ai/research-library` 的 `twitter-ai-observation`，读取已发布包 `twitter-ai-daily-2026-09-21-v1`。实际同步提交为 `a26f22b5e87948e014344bf3cf2ad2c485e31700`。每条 finding 成为一个 Feed Item，共 5 条；不是一次新的 X 抓取。

- manifest SHA-256：`8df4c8c45138d6308a4d88e24820ea917d737587e5a18b1a24a5fe91c0a38315`
- research SHA-256：`d0bae41d4fdbc5cca8ece757713807a48beb9facf5fa8babdea7609c6fd75ae3`
- report SHA-256：`b4558b061dbab478be2db6af5e714d01fbee57279f7d55c0e1797294205f42cc`

语义规则 **Jev/TypeSafe 直接变化与能力边界** 仅接受直接介绍 Jev 变化、用法和能力边界的发现；其余主题以及全包未知项留在 Feed。真实试跑返回 `outcome=ok`、`choice=inbox.admit`，模型 `jev-1.13.0`。发布版本为 v1，规则显式设置自动入 Inbox。批量处理 5 条后，Inbox 中只有 1 条，原因保留规则名、判断 ID 和版本。

## 产品实操证据

| 路径 | 实际观察 |
| --- | --- |
| Feed 添加研究库、手动拉取 | 收到 5 条真实发现；同步记录带固定提交与校验结果 |
| 再次拉取 | 新增 0、去重 5；暂停后拉取按钮禁用，恢复后再次同步仍为 0 / 5 |
| 助手配置筛选 | 从自然语言生成方案，按钮创建 Functions 草稿并调用真实 Jev 试跑；显式启用后处理现有材料 |
| Inbox | 显示唯一入选 Jev 条目及具体规则原因；其余 4 条未变成待办 |
| 选择材料与写作要求 | 关闭对话框、刷新再打开，选择、标题和要求仍在 |
| 生成、打开、编辑 | 真实 MiniMax 结果写入 Pages；编辑器实际载入正文；标题及正文备注修改后自动保存，刷新仍在 |
| 重复生成请求 | 返回同一文稿；手工修改的标题和正文保留，没有再次生成或覆盖 |
| 助手准备写作 | 选中当前 Inbox 材料并带入同一个写作对话框；没有另外保存业务状态 |
| 模型缺失与恢复 | 临时在隔离服务移除模型配置，明确报错并保存失败输入；恢复配置、重启、点击恢复处理后，用同一请求生成第二篇文稿 |
| 切换项目 | 对照项目 `project-67b4ff91-1e23-4c93-98e5-9433f2ab2ad8` 看不到主项目文稿、材料、草稿要求或收据；切回后主项目状态仍在 |
| 来源追踪 | 主文稿有原帖链接与采用材料快照，保留 `SUMMARY_EXPORT`、`BOUNDED` 与未独立复现的边界 |
| 业务完成状态 | 生成没有自动将 Inbox 标为已处理；仍留给用户审读后决定 |

过程中修复了真实暴露的接线问题：创建来源后配置面板不更新、Pages 标签打开却不载入文稿、Inbox 标签显示内部 ID、生成对话框沿用上一个任务的成功提示。缺模型不会返回占位成功。

## 工程检查

- 根目录 `pnpm build` 通过；最后的客户端改动补跑 Workbench / Local Host 构建。
- `pnpm boundary:check` 通过：59 个 package，依赖方向检查无错误。
- 139 项定向回归初跑有 2 项失败：公开来源提交缺少 `await` 导致恢复逻辑未捕获异常，以及 Pages 测试硬编码旧插件名单。最小修复后，包含这两项的 51 项全部通过；其余初跑 137 项通过。
- 后续闭环、列表恢复、工作台脚本检查 11 项通过；最后的标签与壳层适用测试另行通过。
- 新用例覆盖发布包哈希拒绝、证据边界、模型失败、请求幂等和并发、项目隔离、自动入箱正负例与待复核、旧规则兼容、助手拒绝其他项目条目。
- 浏览器实操使用 CUA 操作当前构建并观察真实正文和布局；没有用 Mock 结果冒充 Jev 或写作结果。未运行 Playwright e2e 套件，浏览器证据以上表为准。

## 尚未验收与限制

文稿是工作稿，模型生成质量没有通过用户验收。两次生成均保留了边界，但存在篇幅偏长、正文重复标题、把全包限制带入单条主题的情况；恢复验证稿仍带有早期助手要求中的内部 ID。后续助手提示已要求用材料标题代替 ID，但未用这些生成结果声称文稿质量达标，也未重写已保存工作稿。用户应在 Pages 审读后决定采用内容。

已安装 macOS App、中文 IME 候选窗、VoiceOver、主观手感、长期定时采集、新研究包持续发布和外部 MCP 全链路均为 `UNVERIFIED`。本记录不表示这些范围已完成。

## 第二轮：下一步建议与多材料专题

2026-09-22，用户同意按既有材料继续验证。本轮沿用同一研究包的 5 条发现，没有重新抓取上游，也没有修改研究库。指定业务路径在本地最终构建中已实际操作；文稿质量经过 Agent 编辑，尚未经用户验收，不能称为模型自动产出质量达标。

### 建议、人工决定与状态

Inbox 的「下一步建议」已绑定已发布函数 **AI 工具观察：下一步（修订规则）**，key 为 `ai_observation_inbox_next_20260922_r2`，发布版本 v1。发布不可变，因此修订采用新函数，原函数和判断记录保留。真实预览返回模型 `jev-1.13.0`，不是 Mock。

在调用前按规格固定预期，修订规则的一次完整批量结果如下：

| 真实材料 | 实际建议 | 后续处理 |
| --- | --- | --- |
| Jev 开放试用与能力边界 | 整理成文稿 | 与比较方法共同形成观察稿 |
| 比较模型前固定请求、实际运行与产品环境 | 整理成文稿 | 手工从 Feed 加入 Inbox，再用于观察稿 |
| 工作区上传与遥测争议 | 先核查 | 与模型路由共同形成核查清单 |
| 请求模型与实际模型可能不同 | 先核查 | 清单保持待核查，不宣称已复现 |
| 全包未抓取、未观看、未复现的未知汇总 | 忽略 | 仅忽略独立的重复事项，其他材料的限制保留 |

初版规则首次批量符合预期，但重新打开未知汇总再判断时，结果从「忽略」变为「先核查」。此次不一致作为失败证据保留。修订规则明确先检查是否只是全包限制，再区分具体争议与产品动态；修订后整批结果符合预期，未知汇总另重试两次仍为忽略（含批量共三次）。这只是同批已知样本的回归，不是独立评测或通用准确率。

所有判断只产生建议和记录，不改 Inbox 状态。实操覆盖忽略、从历史重新打开、重新判断、人工选择与建议不同的动作；Feed 原文仍保留。最终状态是 **4 条待处理、1 条已忽略**，没有因为生成成功自动标记完成。

### 两份新产物

- **AI 工具观察：能力边界与比较方法（工作稿）**：`074b87dd-7844-453e-8212-3a9a176d314d`。采用 Jev 与比较方法两条材料，正文约 631 字，保留逐条引用、同包非独立佐证的说明及待核查问题。
- **AI 工具核查清单：上传与模型路由**：`833ad016-a03e-4884-8609-65b19d23fcab`。采用上传与路由两条材料，修订正文约 641 字，区分已有说法、缺失证据及尚未执行的核查步骤。

两份文稿均从 Inbox 界面的实际选择与生成按钮调用 MiniMax-M3，再写入 Pages。原始输出存在过长、Noul 拼写错误、混入全包限制、猜测日志或请求头，以及把关闭上传开关混同于否定默认行为等问题。Agent 已通过 Pages 公开接口修订正文，材料快照、来源链接、生成收据和请求标识保持不变。原始输出保存在隔离 Home 的 `validation/round2/compose-original.json` 与 `verify-original.json`，不能将编辑后的质量归功于原始模型输出。旧有两份文稿未覆盖。

### 实操与工程证据

| 路径 | 观察与边界 |
| --- | --- |
| Functions 发布与绑定 | 初版通过界面发布并绑定；修订版通过相同公开 API 发布、绑定；重启后读取仍为修订函数 |
| 批量与单条重判 | 界面批量入口、单条重判均实际调用；建议、时间和版本可见；修订规则整批及重试收据已保存 |
| Feed 人工入箱 | 即使筛选未推荐入箱，也能明确点击「手动加入 Inbox」；新事项立即刷新到列表 |
| 多材料生成 | 两次在界面选择各 2 条真实材料，填写不同要求并生成；Pages 实际加载两份正文 |
| 产物返回来源 | 最终构建中点击观察稿第 2 条「返回 Inbox 材料」，回到正确项目与比较方法事项；该事项的「处理结果」又能打开同一文稿 |
| 编辑、刷新与请求重放 | 在 Pages 界面给观察稿标题加「（工作稿）」并自动保存；刷新仍在；重放原生成请求返回相同 ID，编辑后的标题与正文完整保留，文稿数量仍为 4 |
| 重启与隔离 | 最终构建重启 4176 服务后，规则绑定、4 待处理 / 1 忽略、4 篇文稿保留；对照项目的绑定为空、Inbox 与生成收据均为空（本轮隔离复查通过 API，前轮已做界面切换） |

新增实现沿用既有 Functions 判断与 Pages 生成能力，补充成稿/核查建议、显式重判、来源返回链接、事项关联产物，以及人工入箱后的刷新。修复了 Functions 发布成功后误调用不存在的 `markSelected`，和编辑器内的 Inbox 链接在内嵌浏览器无法返回的问题。

最终根构建通过；同轮边界检查无错误；64 项适用回归通过，最终返回链接改动后补跑的 6 项闭环与客户端脚本检查通过，`git diff --check` 通过。测试曾在工作区构建清理输出期间因缺少 `dist` 失败；等待构建完成后重跑通过。构建期间另一工作流的 Pages 导入代码曾发生类型错误，其后该代码更新，最终完整构建通过。本轮未修改该导入实现。供应商本地压缩包与 HEAD 的 SHA-256 一致后，仅本次构建使用 `pnpm_config_verify_deps_before_run=false` 避开私有包元数据 404，没有写入持久配置。预览最近浏览器错误日志为空。

证据文件在隔离 Home 的 `validation/round2/`，包括修订规则整批结果、两次重试和请求重放检查。本轮不是全产品体验验收：实际截图可见 Pages 长标题输入框仍会横向截断；文稿质量、中文 IME、主观手感以及前述未覆盖范围仍待验收。核查清单中的外部产品实验尚未执行。

交付时发现共享工作区被并行任务从 `codex/feed-inbox-pages-loop` 切回 `main`，两分支仍指向同一基线 `f25ea17`；本轮没有再次切换、提交或推送。改动仍在当前共享工作区，不能把现存的 Pages 导入、插件开发及其他并行修改算成本轮成果。
