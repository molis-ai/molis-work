# Task

**定位：** 真正干活的工作对象与构图的唯一 owner。Goal 只是可选关联。

**拥有：** Task 身份、标题、可选 `goal_id`、Frame 构图（相机与引用块）。

**公开面：** 列出 / 创建 / 按 Goal 打开或复用 / 更新标题与关联 / 保存构图。

**不负责：** 不拥有 Goal、Session、Feed、Inbox、Artifact 事实；拖入只存引用。不完成 Goal 事件协议。
