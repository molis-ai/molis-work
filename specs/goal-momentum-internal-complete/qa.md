# Goal 推进态势内部完整验收

## Checks

验收日期：2026-08-30

### 真实数据与口径

- 使用本机 Molis Work 权威项目 `Molis Work 信息流工作台重设计` 启动当前源码服务验证，不使用页面内 mock 数据。
- 验收快照包含 34 个 Goal、38 条 active `depends_on`；视觉方向为前置提供者 → 消费者，`part_of` 只生成分组带。
- 近 7 天真实快照显示 22 个启动、20 个完成、2 个新阻塞、0 个可证明停滞；页面同时说明历史不足不会被计为停滞。
- 复合父 Goal（`waiting_children`）仍在拓扑中作为上下文存在，但不再进入行动队列，也不再显示“可直接开始”。
- 高影响但可开始的 Goal 不使用红色瓶颈标记；只有阻塞或停滞且仍影响下游的节点标记为瓶颈。

### 主路径与视觉

- 浏览器真实点击通过：打开“推进态势”、切换近 7/30 天、切换全部/未完成、从节点与行动队列同步选择、缩小/放大/适应窗口。
- 选择状态、时间窗口与过滤状态刷新后保持；“打开 Goal”链接保持当前项目路由并指向同一 Goal。
- 窄屏下完整拓扑使用横纵滚动，不删除节点；行动队列与选择详情纵向堆叠，主要筛选、周期与缩放按钮达到 44px 触控高度。
- 浅色与深色主题均使用同一语义状态和可读标签；节奏面板、拓扑、瓶颈、行动队列均实际可见。
- 现有高保真视觉基线保留在 `.impeccable/review/goal-momentum-topology-desktop.png` 与 `.impeccable/review/goal-momentum-topology-narrow.png`；本轮在当前会话内对真实 34 Goal 数据进行了浅色和深色浏览器截图检查。

### 自动化与性能

- `pnpm typecheck`：通过。
- `pnpm build`：通过。
- `node --import tsx --test tests/goal-momentum.test.ts`：7/7 通过。
- Momentum 定向 Web/视觉回归：2/2 通过。
- 完整 `pnpm test`：354 项中 350 项通过；4 项失败均来自当前工作区中与本 Goal 无关的既有并行改动：2 条缺失的项目目录英文翻译，以及 uninstall 测试并发清理 `dist` 导致的 3 条产物缺失。新 Momentum 模型、Web、V1、Feed、安装与大部分集成回归均通过。
- 300 Goal / 900 关系代表性派生：最终测量 7.8ms，低于本任务 100ms 内部基线。

### 异常与降级

- 自动化覆盖 active/inactive 关系、非 `depends_on` 关系、多前置 DAG、依赖循环、悬空端点、多父级 `part_of`、历史不足、停滞与密集数据。
- 循环和悬空关系保留节点并产生完整性诊断，不让整图失效。
- 懒加载保留 `aria-busy`、失败文本和原位重试；空项目显示解释性空状态。
- 旧 `/api/board/graph` endpoint、放射轨道、环形坐标与 `part_of` 图边已移除。

## Residuals

- 完整测试套件中的 4 条失败不属于本 Goal 的修改范围，不能据此宣称全仓回归全绿；本 Goal 对应的定向模型、Web、视觉、类型和构建证据均通过。
- 当前源码在 4174 端口完成内部试用验证；已安装的 4173 服务仍是既有发布版，本任务没有擅自覆盖安装或发布。
