# 05 迁入阅读能力独立视觉审查

日期：2026-09-10。范围：迁入的旧草稿、目标说明/原输入输出、风险关系、完成要求/Artifact精确版本；8张真实桌面及390截图。此结果不代表全仓功能或发布验收。原标准完整明细的最后补充另行验证。

disposition: ship

## persistence
pass. Root `PRODUCT.md` exists and was not opened. Code-led production of approved v3 Operate/Read; no `hero-repro.png` owed. Direction is `specs/archive/goal-timeline-redesign/DESIGN.md`. No comp-round mocks requiring an approval record. WI05 accepted spec is the migration authority. Detector already `[]`. Eight named captures exist and match their labels: draft pair are contracted 1440/390 footer viewports of the scrolling reader with a visible save; risk/requirements/definition pairs show the migrated reading surfaces, not substituted prototypes.

## fidelity
No generated raster spec. TYPE / MATERIAL / GROUND judged against OWN-WORLD (neutral cool gray canvas, system Chinese, paper reader, hairlines, restrained blue). Migrated capabilities judged against the WI05 table and the eight captures.

| element | class |
|---|---|
| TYPE | match — system Chinese stack; goal title / 18px reader title / 13px body / 11px index as DESIGN.md |
| MATERIAL | match — paper, 1px lines, blue-soft selection, SVG icons; no fake physicality; no rasters owed |
| GROUND | match — cool gray canvas + white paper; named `#f5f6f8` / `#fff`, not warm cream |
| Untransferred legacy draft editor in 目标说明 | match — `renderDraftEditor` only when `definition_state=draft` and not event owner; 修改草稿 only on the draft Goal; 1440/390 footers show criterion fields, reason, `保存草稿修改`; current summary stays |
| Event-owned Goals keep event agree/configure | match — requirements/definition Goals have no 修改草稿; draft path is the existing form, not a silent convert |
| Relations / risk / impact / policy work area in 目标说明 | match — 关联与约束 deck plus Goal 关系 stage on definition 1440/390; live risk record, 修改风险信息, 记录风险 on risk 1440; same risk facts in the 390 reader scroll |
| Original constraints / required inputs / promised outputs | match — 必须遵守 / 需要的输入 / 承诺的输出 with original values on definition 1440 and 390 |
| Current requirements + exact Artifact v1 | match — 当前完成要求; 首次使用检查记录 `v1 · 输入结果` and 查看这个版本 on requirements 1440/390 |
| Reader occupies the pane; continue form not crowding | match — no 使用事件记录继续 in any reader capture; summary band remains; 返回所选事件 stays in reader chrome |
| Narrow 390 usability of these four areas | match — draft save stacked full-width; requirements wrap with 尚未满足 and the v1 link; definition original lines readable above stacked factor cards; risk facts in one column with a visible reader scrollbar |

## ceiling
reached. Operate/Read devices in use on the migrated surfaces: current-result band that does not follow history, dual-pane index/reader (timeline↔reader at ≤680px), paper reading face, return path, independent `reader-content` scroll, hairline factor deck, system type. No unused native device of this world is sitting idle on these four areas.

## material_fixes

## keep
Keep the Operate/Read shell, the current-result band that ignores history selection, reader/form mutual exclusion, legacy draft editing only on untransferred drafts, exact Artifact version open, and original constraints/input/output inside 目标说明.

P2 (nonblocking; not P1; do not open a visual redesign): on 390, existing `.risk-facts > div` 92px labels wrap (“处理方式 / 对 Goal 的影响”); draft-auxiliary 登记风险 / 影响范围 / 规则 links sit above the same factors deck in one 目标说明 scroll; `.reader-content` scrollbar is unthemed while timeline/`event-sheet` are.