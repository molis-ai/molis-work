Scope: workbench Frame container slice. Operate mode. Updated 2026-09-14.

Authority: `specs/workbench-frame-container/spec.md`. Visual world is incumbent Calm Desktop / immersive graphite: 264px directory, 32px titlebars, paper `#fff`, nav `#f8f9fb`, ink `#272932`, violet focus `#6262d6`. Do not introduce a second palette, display face, or dashboard-card grammar.

Thesis: the Goal canvas is a dotted work field; Goal cards are compact paper nodes; a Frame is that Goal’s work surface for other plugins’ assets. Tabs mark location with type weight and a 2px bottom marker, never a raised paper chip.

First viewport: left directory, top Goal-canvas tab, three Goal nodes. Selected node uses a cobalt/violet border, not a halo. Chat mode is the same node unfolding into a typed thread plus composer. Expand opens the Goal’s Frame tab.

Signature: one shadow family for menus only. Resting Goal nodes and Frame blocks have a hairline, no exterior drop shadow. Nested cards (bubble-in-card, card-in-tab) are refused.

Finish: browser-checked Light/Dark at desktop and 390px; detector scan on the slice CSS. Not a production visual rewrite of `apps/workbench`.
