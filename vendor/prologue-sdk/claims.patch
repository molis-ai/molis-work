diff --git a/crates/prologue-host-tauri/src/lib.rs b/crates/prologue-host-tauri/src/lib.rs
index c5b7ac1d..5b618731 100644
--- a/crates/prologue-host-tauri/src/lib.rs
+++ b/crates/prologue-host-tauri/src/lib.rs
@@ -11,7 +11,7 @@ pub mod testing {
     pub use crate::dispatch::{close, inspect, open, settle, DispatchPermit, DispatchProof};
     pub use crate::storage::Storage;
     /// 读取侧也要有行为证据：**摘要是这里算的**，命令层只是转发。
-    pub use crate::workspace::{authorize, read_text, WorkspaceFile, WorkspaceGrant};
+    pub use crate::workspace::{authorize, read_text, read_text_up_to, search, WorkspaceFile, WorkspaceGrant};
     pub use crate::lease::{Lease, LeaseRecord, LeaseStore};
 }
 
@@ -1516,9 +1516,10 @@ fn workspace_search(
 fn workspace_read(
     root_ref: ExactRef,
     path: String,
+    max_bytes: Option<u64>,
     state: State<'_, HostState>,
 ) -> Result<workspace::WorkspaceFile, HostError> {
-    workspace::read_text(root_ref, path, &state.roots)
+    workspace::read_text_up_to(root_ref, path, max_bytes, &state.roots)
 }
 
 #[tauri::command]
diff --git a/crates/prologue-host-tauri/src/network_guard.rs b/crates/prologue-host-tauri/src/network_guard.rs
index b8a68f03..87f7aa3c 100644
--- a/crates/prologue-host-tauri/src/network_guard.rs
+++ b/crates/prologue-host-tauri/src/network_guard.rs
@@ -48,8 +48,11 @@ fn denied(message: &'static str) -> HostError {
 fn is_loopback_host(host: &str) -> bool {
     host == "localhost"
         || host == "::1"
+        || host == "[::1]"
         || host.ends_with(".localhost")
-        || host.starts_with("127.")
+        || host
+            .parse::<std::net::Ipv4Addr>()
+            .is_ok_and(|ip| ip.is_loopback())
 }
 
 /// 不该让请求打到的地址段。挡的是「用一个能过闸的名字把流量引到内网」，
@@ -81,7 +84,9 @@ fn is_private_host(host: &str) -> bool {
     if lower.starts_with("fc") || lower.starts_with("fd") {
         return true;
     }
-    if lower.starts_with("fe8") || lower.starts_with("fe9") || lower.starts_with("fea")
+    if lower.starts_with("fe8")
+        || lower.starts_with("fe9")
+        || lower.starts_with("fea")
         || lower.starts_with("feb")
     {
         return true;
@@ -147,3 +152,46 @@ pub fn check_redirect(
     }
     Ok(next)
 }
+
+#[cfg(test)]
+mod tests {
+    use super::*;
+
+    #[test]
+    fn explicit_loopback_does_not_allow_hostname_lookalikes() {
+        let allowance = NetworkAllowance {
+            model: true,
+            loopback: true,
+            ..Default::default()
+        };
+        for url in [
+            "http://localhost:11434/v1",
+            "http://model.localhost/v1",
+            "http://127.0.0.1/v1",
+            "http://127.2.3.4/v1",
+            "http://[::1]/v1",
+        ] {
+            assert!(check_target(url, &allowance).is_ok(), "{url}");
+            assert!(
+                check_target(
+                    url,
+                    &NetworkAllowance {
+                        model: true,
+                        ..Default::default()
+                    }
+                )
+                .is_err(),
+                "{url}"
+            );
+        }
+        for url in [
+            "http://localhost.example.com/v1",
+            "http://127.example.com/v1",
+            "http://127.0.0.1.example.com/v1",
+            "http://localhost@api.example.com/v1",
+            "http://192.168.0.1/v1",
+        ] {
+            assert!(check_target(url, &allowance).is_err(), "{url}");
+        }
+    }
+}
diff --git a/crates/prologue-host-tauri/src/workspace.rs b/crates/prologue-host-tauri/src/workspace.rs
index 1ff0626e..9f58c41a 100644
--- a/crates/prologue-host-tauri/src/workspace.rs
+++ b/crates/prologue-host-tauri/src/workspace.rs
@@ -13,8 +13,11 @@ const PROTECTED_SEGMENTS: [&str; 3] = [".git", "node_modules", ".prologue"];
 
 const MAX_LIST: usize = 256;
 const MAX_SEARCH: usize = 50;
-const MAX_WALK: usize = 1024;
+// A search walks at most this many files. 1024 stopped halfway through a mid-sized repository and still said "no matches".
+const MAX_WALK: usize = 20_000;
 const MAX_READ: usize = 64 * 1024;
+// Files a search reads, separately from a whole read: over 64 KiB used to be skipped silently.
+const MAX_SEARCH_FILE: usize = 2 * 1024 * 1024;
 const EXCERPT: usize = 200;
 
 /// 只信句柄，不信路径。`O_NOFOLLOW` 让最后一段是符号链接时直接失败，
@@ -103,6 +106,8 @@ pub struct WorkspaceMatch {
 #[derive(Serialize)]
 pub struct WorkspaceSearch {
     pub matches: Vec<WorkspaceMatch>,
+    /// The search stopped before covering everything (file or match limit): "no matches" then does not mean absent.
+    pub truncated: bool,
 }
 
 #[derive(Serialize)]
@@ -205,16 +210,27 @@ pub fn search(
     }
     let root = root_path(&root_ref, roots)?;
     let start = resolve_path(&root, &path)?;
+    // A path may name one file as well as a directory: listing a file as a directory fails, and that used to read as
+    // "no matches". A path that does not exist is reported as such, never as an empty search.
+    let is_file = match fs::metadata(&start) {
+        Ok(meta) => meta.is_file(),
+        Err(_) => return Err(HostError::new("ROOT_NOT_FOUND", "Path was not found.")),
+    };
     let mut matches = Vec::new();
-    walk(&root, &start, 0, &mut |file| {
+    let mut skipped_large = 0usize;
+    let mut visit = |file: &Path| {
         if matches.len() >= MAX_SEARCH {
             return false;
         }
-        let bytes = match read_whole_file(file, MAX_READ) {
+        if fs::metadata(file).map(|meta| meta.len() as usize > MAX_SEARCH_FILE).unwrap_or(false) {
+            skipped_large += 1;
+            return true;
+        }
+        let bytes = match read_whole_file(file, MAX_SEARCH_FILE) {
             Ok(value) => value,
             Err(_) => return true,
         };
-        if bytes.contains(&0) || bytes.len() > MAX_READ {
+        if bytes.contains(&0) {
             return true;
         }
         let text = String::from_utf8_lossy(&bytes);
@@ -238,8 +254,15 @@ pub fn search(
             }
         }
         true
-    });
-    Ok(WorkspaceSearch { matches })
+    };
+    let mut walked = 0;
+    if is_file {
+        visit(&start);
+    } else {
+        walked = walk(&root, &start, 0, &mut visit);
+    }
+    let truncated = matches.len() >= MAX_SEARCH || walked >= MAX_WALK || skipped_large > 0;
+    Ok(WorkspaceSearch { matches, truncated })
 }
 
 /// 一份内容的 SHA-256 十六进制。**读取和写入回执共用这一个**——
@@ -312,11 +335,24 @@ pub fn read_text(
     root_ref: ExactRef,
     path: String,
     roots: &Mutex<HashMap<String, PathBuf>>,
+) -> Result<WorkspaceFile, HostError> {
+    read_text_up_to(root_ref, path, None, roots)
+}
+
+/// A line window or an edit may read past the 64 KiB whole-read cap, up to 2 MiB; nothing lowers it below 64 KiB.
+pub fn read_text_up_to(
+    root_ref: ExactRef,
+    path: String,
+    max_bytes: Option<u64>,
+    roots: &Mutex<HashMap<String, PathBuf>>,
 ) -> Result<WorkspaceFile, HostError> {
     let root = root_path(&root_ref, roots)?;
     let target = resolve_path(&root, &path)?;
+    let max = max_bytes.map_or(MAX_READ, |asked| {
+        (asked.min(MAX_SEARCH_FILE as u64) as usize).max(MAX_READ)
+    });
     // 类型、大小和正文都从同一个句柄取，不再按路径重新解析。
-    let bytes = read_whole_file(&target, MAX_READ)?;
+    let bytes = read_whole_file(&target, max)?;
     // **先按字节算摘要**，再解码。顺序反了就变成对解码结果的断言。
     let digest = hex_digest(&bytes);
     // **「这不是文本」和「读不了」分开报**：前者调用方该换一条路走
diff --git a/crates/prologue-host-tauri/tests/workspace_large_read.rs b/crates/prologue-host-tauri/tests/workspace_large_read.rs
new file mode 100644
index 00000000..e85318e6
--- /dev/null
+++ b/crates/prologue-host-tauri/tests/workspace_large_read.rs
@@ -0,0 +1,25 @@
+//! 整读仍然有 64 KiB 的上限；行窗和编辑可以明说要读到 2 MiB。
+
+use std::collections::HashMap;
+use std::fs;
+use std::sync::Mutex;
+
+use prologue_host_tauri::testing::{authorize, read_text, read_text_up_to};
+
+#[test]
+fn a_large_file_reads_only_when_a_larger_limit_is_asked_for() {
+    let dir = std::env::temp_dir().join(format!("prologue-rs-large-read-{}", uuid::Uuid::new_v4()));
+    fs::create_dir_all(&dir).expect("temp dir");
+    fs::write(dir.join("big.css"), format!("{}\n.tail {{ color: red; }}\n", "x".repeat(80 * 1024))).expect("big file");
+    let roots = Mutex::new(HashMap::new());
+    let grant = authorize(dir.to_string_lossy().into_owned(), &roots).expect("authorize");
+
+    assert!(read_text(grant.root_ref.clone(), "big.css".into(), &roots).is_err(), "a plain read keeps the cap");
+    let held = read_text_up_to(grant.root_ref.clone(), "big.css".into(), Some(2 * 1024 * 1024), &roots).expect("larger read");
+    assert!(held.text.ends_with(".tail { color: red; }\n"));
+    // Asking for less than the whole-read cap never lowers it; asking for more than 2 MiB is clamped.
+    assert!(read_text_up_to(grant.root_ref.clone(), "big.css".into(), Some(1), &roots).is_err());
+    fs::write(dir.join("huge.txt"), "y".repeat(3 * 1024 * 1024)).expect("huge file");
+    assert!(read_text_up_to(grant.root_ref, "huge.txt".into(), Some(u64::MAX), &roots).is_err());
+    fs::remove_dir_all(&dir).ok();
+}
diff --git a/crates/prologue-host-tauri/tests/workspace_search.rs b/crates/prologue-host-tauri/tests/workspace_search.rs
new file mode 100644
index 00000000..9d03c7c2
--- /dev/null
+++ b/crates/prologue-host-tauri/tests/workspace_search.rs
@@ -0,0 +1,34 @@
+//! 搜索路径可以是单个文件；不存在的路径如实报错，不答成「没找到」。
+
+use std::collections::HashMap;
+use std::fs;
+use std::sync::Mutex;
+
+use prologue_host_tauri::testing::{authorize, search};
+
+#[test]
+fn a_file_path_searches_that_file_and_a_missing_path_is_an_error() {
+    let dir = std::env::temp_dir().join(format!("prologue-rs-search-{}", uuid::Uuid::new_v4()));
+    fs::create_dir_all(dir.join("src")).expect("temp dir");
+    fs::write(dir.join("src/streaks.ts"), "export function currentStreak() {\n  return 0;\n}\n").expect("file");
+    fs::write(dir.join("src/other.ts"), "return 0;\n").expect("file");
+    let roots = Mutex::new(HashMap::new());
+    let grant = authorize(dir.to_string_lossy().into_owned(), &roots).expect("authorize");
+
+    let one = search(grant.root_ref.clone(), "return 0".into(), "src/streaks.ts".into(), &roots).expect("search a file");
+    assert_eq!(one.matches.len(), 1);
+    assert_eq!(one.matches[0].path, "src/streaks.ts");
+    assert_eq!(one.matches[0].line, 2);
+
+    let all = search(grant.root_ref.clone(), "return 0".into(), "src".into(), &roots).expect("search a directory");
+    assert_eq!(all.matches.len(), 2);
+    assert!(!all.truncated, "a small tree is searched completely");
+
+    // A file over the whole-read limit is still searched.
+    fs::write(dir.join("src/big.css"), format!("{}\n.coding-tool-state {{ color: red; }}\n", "x".repeat(80 * 1024))).expect("big file");
+    let big = search(grant.root_ref.clone(), "coding-tool-state".into(), "src".into(), &roots).expect("search a big file");
+    assert_eq!(big.matches.len(), 1);
+    assert_eq!(big.matches[0].path, "src/big.css");
+    assert!(search(grant.root_ref, "return 0".into(), "src/gone.ts".into(), &roots).is_err());
+    fs::remove_dir_all(&dir).ok();
+}
diff --git a/docs/product/requirements/engines/agent/loop.md b/docs/product/requirements/engines/agent/loop.md
index aebbf7de..ff853719 100644
--- a/docs/product/requirements/engines/agent/loop.md
+++ b/docs/product/requirements/engines/agent/loop.md
@@ -63,7 +63,9 @@
 
 ## 空回合重采样
 
-**没往前走就再来一次，而且不扣轮次。** 一个字没说、或者被上限截在半句，都算没往前走。扣它等于让一次网络抖动吃掉用户的预算。截断那一种续写时明说「从断的地方接着，别重复已经写过的」。
+**没往前走就再来一次，而且不扣轮次。** 一个字没说，算没往前走——包括还没写出正文就撞上输出上限（思考把上限用完了），这时告诉它少想一点、把回答写出来。扣它等于让一次网络抖动吃掉用户的预算。
+
+**被上限截在半句，但写出了新正文：接着写，直到写完。** 截下来的那段先进历史，再明说「从断的地方接着，别重复已经写过的」——不放进历史，模型不知道断在哪，只能从头再写，多半又断在同一处。几段正文在对话里本来就连成一条回答；护栏和记忆提炼看的是整段。续写不扣轮次，同样两边都退，并留下一条 `model-response-repair`（`output-truncated`）。续写有自己的上界；到了还在被截断，或者重采样用完仍一个字没写出来，就以 `MODEL_OUTPUT_TRUNCATED` **失败**——半截或空的回答不能当成完成交出去。
 
 「不扣轮次」得**两边都不扣**：循环那个计数退了、账那边不退的话，重采样照样吃掉轮次预算，只是吃得更隐蔽。
 
diff --git a/docs/slices/bounded-inference.md b/docs/slices/bounded-inference.md
new file mode 100644
index 00000000..5af455d9
--- /dev/null
+++ b/docs/slices/bounded-inference.md
@@ -0,0 +1,23 @@
+# Bounded inference on the owning Runtime
+
+The consumer needs its existing text, images, structured TypeSafe judgments and local inference to share one Runtime and Host. No App model HTTP fallback or second runtime is allowed.
+
+Text remains Session.startRun with ephemeral sessions and no workspace root. Model parsers emit bounded provider-reported model identity separately from requested identity. Images retain the existing ImageSurface, model adapters, credential references, Host network gate, usage ledger and resource refs. Add Gemini, URL results fetched by Host without forwarding model credentials, signal cancellation and configured response/image bounds. Explicit auth-none is restricted to permitted loopback model endpoints. Native TypeSafe uses a structured model adapter and bounded Runtime composition surface; state/questions/results remain native JSON, never a natural-language prompt. Local engines must execute through a Prologue-owned Host process boundary with cancellation and receipts; exact process contract will be recorded before implementation.
+
+Hard boundaries: Host owns networking, credentials, processes and file access; App selects endpoint/model, request input, bounds and user consent. Adapter code only translates protocols. Configuration lives under existing module config. Runtime shutdown cancels active inference. Invalid/empty/over-limit outputs and changed credentials never publish a successful consumer result.
+
+Validation: real Node Host loopback text/OpenAI images/Gemini/TypeSafe, safe errors, abort during I/O, auth-none loopback and remote denial, image URL isolation and byte limits, resource receipt reads. Then packed SDK production consumer integration and configured model execution. Existing Action identity/budget/approval behavior remains unchanged.
+
+## Trusted Agent scopes: no workspace and owned Builder sessions
+
+The coordinating App owner authorizes a pure-inference role to run on the existing durable Session without a filesystem root. Extend existing Runtime.startAgentRun with explicit workspace: "none" (default remains required). This branch forbids rootRef, native/system/custom/MCP/Action/subagent tools, active pack tool contributions and workspace context; toolNames must be explicit empty. It preserves Character/persona, mounted text resources, history, model/usage, budgets, before-dispatch guard, same AgentLoop/Session ledger and cancellation/control. Never synthesize an authorized root or read cwd/home project files. Existing workspace Agents retain original behavior.
+
+Builder needs positive allow only within its own trusted Session, on an already authorized root. Extend the existing Session/Run permission contract or another existing frozen authority input with trusted session-scoped grants; no global allow rule. Freeze exact Session/Run/root into the existing effect-chain policy evaluation. Organizational/project deny, sandbox/authorized-root restrictions and later deny hooks still win. Grant can allow the bounded tool names and authorized-root paths; App's session-filtered hook further narrows writes to allowed builder files. Tool registration/execution remains existing Session pack mechanism. Other simultaneous Coding sessions must retain their original approvals. Validate with real Node Host two concurrent Sessions and negative policy/host cases.
+
+### Public Agent scope API and boundaries
+
+`Runtime.startAgentRun({ workspace: "none", session, start, agent: { toolNames: [], ... }, history: "session" })` uses the same AgentLoop, resource binding, Character, control, usage and durable Session ledger. It does not construct a workspace reader or mutator. Default/`workspace: "required"` still requires a real root. A no-workspace call rejects roots in either start shape, per-Run tool executors, contributed Session pack tools, Character tools/skills/MCP, subagents, workspace context, and a missing/nonempty toolNames list. Window/compaction options remain available.
+
+Rooted calls may provide trusted App `grants: { toolNames, paths }`. This slice supports paths `[]` or `["."]` only: authorize the actual build directory as its own Host root; a child directory string is rejected because lexical path matching cannot provide canonical symlink isolation. Grants are copied before the first await and never persisted or inherited. A private capability binds the grant to exact Session/Run/root, namespaces effect identities and checks authority on cached preparation and dispatch, including after Host dispatch preparation. All explicit denies, unknown effects, escalation, sandbox and final Host checks remain stronger; tool-before hooks may still narrow. Commands and network receive no implicit grant. Read tool policy denies are checked before execution.
+
+Validation: `pnpm exec vitest run packages/sdk/test/agent-scopes.live.test.ts` uses a real Node Host, local HTTP model transport and actual files for no-root Character/material/history/usage/dispatch-guard/budget/abort behavior, zero workspace calls, contradictory input rejection, concurrent Builder/Coding approval isolation, two granted Session pack executors, global/hook/sandbox/root denials, scoped replay and dispatch spoof rejection, and rejected child path grants. `pnpm build` checks the public package.
diff --git a/docs/slices/model-loopback.md b/docs/slices/model-loopback.md
new file mode 100644
index 00000000..b0690bb8
--- /dev/null
+++ b/docs/slices/model-loopback.md
@@ -0,0 +1,13 @@
+# 模型回环端点一致性
+
+消费结果：Molis Cognia 已配置的本机 HTTP 模型可经原 Prologue Session/Run/Node Host 完成整理和取消；模型目录与 Run 不再给出相互矛盾的端点判断。基于消费包对应的 a7e785b8 修复，保留其后的当前工作区不受影响。
+
+范围：Model 的目录及启动输入共用端点形状校验，HTTPS 或 HTTP 回环；Host 提供同一回环主机判断，精确识别 localhost、其子域、127/8 IPv4 和 ::1，拒绝看似回环的公网域名。登记不是授权。Session 继续只拥有 Run 生命周期；真正发出仍经过原 Host 网络许可、DNS、重定向和凭据来源检查，不新增执行链、槽或状态。
+
+合同依据：[Model](../../packages/sdk/src/model/README.md)、[Session](../../packages/sdk/src/session/README.md)、[Host](../../packages/sdk/src/host/README.md)，需求为模型目录、Run 与 Host 网络放行。允许修改上述模块中的端点校验、对应测试/合同和消费包。默认禁止网络、回环需显式授权、凭据来源约束是硬边界；协议和 Host 仍通过既有槽替换；App 传 endpoint 和 network.model/loopback；用户模型设置及文案归消费 App。
+
+不合法 URL、非回环 HTTP 在登记/启动前失败；合法回环缺少网络授权由 Host 拒绝，零请求。正常完成和取消保留原终态与回放，不自动重试；不改恢复语义。
+
+同一回环识别修正同步到 crates/prologue-host-tauri/src/network_guard.rs，保持两个 Host 的判据一致；使用 Rust 标准 IPv4 解析而非 127 前缀匹配，识别 URL 的 [::1] 主机形式。补充 cargo test --manifest-path crates/Cargo.toml -p prologue-host-tauri --lib network_guard 验证真实原生目标检查函数。npm 包只包含 Node/Tauri JS SDK，Rust 源修正随可重建补丁保存，不声称已替换原生安装版。
+
+验证：pnpm exec vitest run packages/sdk/test/model-loopback.live.test.ts packages/sdk/test/model-catalog-audit.test.ts packages/sdk/test/host-network.test.ts packages/sdk/test/host-network.live.test.ts；pnpm build；pnpm exec tsc -p tsconfig.typecheck.json。真实受控 HTTP Provider 经公开 Runtime/Session 入口验证请求、结果、权限拒绝和取消；再用重打包 SDK 验证 Molis 实际动作和浏览器路径。测试响应不代表商业模型质量。
diff --git a/packages/sdk/src/agent/README.md b/packages/sdk/src/agent/README.md
index 3041404e..5a668b61 100644
--- a/packages/sdk/src/agent/README.md
+++ b/packages/sdk/src/agent/README.md
@@ -72,3 +72,7 @@ Runtime 返回的 `control.steer(input)` 是异步接收回执：持久 Session
 Agent 通过 Model 窄口保留当前 Run 完整原生响应，并仅向原协议、地址和模型续接。工具执行仍只走原 Tool/Effect 链；签名不发公开事件、不入便携事实历史。原生块按实际内容计入预算，与工具批次一并压缩或保留。跨 Run 沿 Session 事实投影。见 [消费修复合同](../../../../docs/slices/model-native-continuation.md)。
 
 上下文整理器可通过既有输入的 reportUsage 转交同 Runtime 已结算调用；父 Run 只记录归属与本轮预算，不重复记录 Runtime 总预算。硬边界、可替换摘要器、App 模型选择及失败/取消/恢复见 [整理用量合同](../../../../docs/slices/compaction-usage.md)。无新增阈值或用户设置。
+
+### Explicit workspace-free runs
+
+`Runtime.startAgentRun` accepts `workspace: "none"` with explicit `agent.toolNames: []`. It preserves the same Character, text resources, history, loop, control, budget and Session ledger while rejecting workspace access and tools. Rooted runs keep their required-root behavior. See [bounded Agent scopes](../../../../docs/slices/bounded-inference.md).
diff --git a/packages/sdk/src/agent/core/loop.ts b/packages/sdk/src/agent/core/loop.ts
index 3d32de17..8241fac0 100644
--- a/packages/sdk/src/agent/core/loop.ts
+++ b/packages/sdk/src/agent/core/loop.ts
@@ -371,6 +371,15 @@ async function driveAgentLoop(input: AgentLoopInput): Promise<void> {
   let toolNameRepaired = false;
   let resamples = 0;
   const MAX_RESAMPLES = 2;
+  /**
+   * 截断续写。回答被输出上限截在半句时**接着写，直到写完**：写出来的那段留在历史里，模型看得见自己
+   * 写到了哪；每次续写都必须写出新正文，否则按重采样计。续写也有上界，到了还没写完就如实失败——
+   * 半截回答不能当成完成交出去。
+   */
+  let continuations = 0;
+  const MAX_CONTINUATIONS = 8;
+  /** 这一段回答里之前几次续写已经说出的正文。收工时和最后一段拼成完整回答再过护栏。 */
+  let continuedText = "";
   /**
    * 因为溢出压过几次。
    *
@@ -856,6 +865,7 @@ async function driveAgentLoop(input: AgentLoopInput): Promise<void> {
        *     模型的。未知帧同理：没读懂的东西不该变成模型说过的话。
        */
       if (
+        event.type === "model-reported" ||
         event.type === "reasoning-delta" ||
         event.type === "unknown" ||
         // 用量也照原样转发：**不转发的话「这次花了多少」在 Agent
@@ -1214,16 +1224,53 @@ async function driveAgentLoop(input: AgentLoopInput): Promise<void> {
         pending = [...lifted];
       }
     }
+    if (pending.length > 0) {
+      // 续写走到了工具调用：前面几段已在历史里，下一段回答重新计。
+      continuedText = "";
+      continuations = 0;
+    }
     if (pending.length < 1) {
+      const truncated = lastStop === "max-tokens";
+      if (truncated && turnText.trim().length > 0) {
+        /*
+         * 被上限截在半句，但写出了新正文：**接着写**。
+         *
+         * 截下来的那段先进历史，再说「从断的地方接着」——不放进去的话模型根本不知道断在哪，
+         * 只能从头再写一遍，而那一遍多半又被截在同一个地方。续写不扣轮次（两边都退），
+         * 界面上几段正文本来就连成一条回答。
+         */
+        if (continuations >= MAX_CONTINUATIONS) {
+          done({
+            type: "failed",
+            error: createPrologueError(
+              "MODEL_OUTPUT_TRUNCATED",
+              `The answer was still cut off at the output limit after ${MAX_CONTINUATIONS} continuations.`,
+            ),
+          });
+          return;
+        }
+        continuations += 1;
+        turn -= 1;
+        budget.turnResampled();
+        continuedText += turnText;
+        history.push({ kind: "text", role: "assistant", text: turnText, source: "assistant" });
+        history.push(
+          runtimePromptRecord(
+            "Your last message was cut off at the output limit. Continue from exactly where it stopped; do not repeat what you already wrote.",
+            "runtime:truncated",
+          ),
+        );
+        input.run.emit({ type: "model-response-repair", reason: "output-truncated" });
+        continue;
+      }
       /*
-       * 这一轮没往前走：一个字没说，或者被上限截在半句。
+       * 这一轮没往前走：一个字没说——没开口，或者还没写正文就撞上了上限（思考把上限用完了）。
        *
        * **重采样，而且不扣轮次**——这一轮什么也没产出，扣它等于让
        * 一次网络抖动吃掉用户的预算。重试本身有上界，否则「不扣轮次」
        * 就成了一个不会停的循环。
        */
-      const stalled =
-        turnText.trim().length < 1 ? "empty" : lastStop === "max-tokens" ? "truncated" : undefined;
+      const stalled = turnText.trim().length < 1 ? (truncated ? "truncated" : "empty") : undefined;
       if (stalled !== undefined && resamples < MAX_RESAMPLES) {
         resamples += 1;
         // **两边都退。** 只退循环那个计数的话，重采样照样吃掉轮次预算，
@@ -1233,13 +1280,26 @@ async function driveAgentLoop(input: AgentLoopInput): Promise<void> {
         history.push(
           runtimePromptRecord(
             stalled === "truncated"
-              ? "Your last message was cut off at the output limit. Continue from exactly where it stopped; do not repeat what you already wrote."
+              ? "Your last message reached the output limit before any answer text was written. Keep your reasoning shorter and write the answer."
               : "That turn produced nothing. Either call a tool to make progress, or answer the question.",
             stalled === "truncated" ? "runtime:truncated" : "runtime:empty",
           ),
         );
         continue;
       }
+      if (stalled === "truncated") {
+        // 重采样用完仍然一个字没写出来：**失败，不是完成**。空回答当完成，人会以为拿到了答案。
+        done({
+          type: "failed",
+          error: createPrologueError(
+            "MODEL_OUTPUT_TRUNCATED",
+            "The output limit was reached before any answer text was written.",
+          ),
+        });
+        return;
+      }
+      // 护栏和记忆看的是整段回答；历史里前几段已经放过，只补最后这段。
+      const answerText = continuedText + turnText;
       if (input.guards.guardrails.length > 0) {
         /*
          * **输出护栏在终态前跑。**
@@ -1250,7 +1310,7 @@ async function driveAgentLoop(input: AgentLoopInput): Promise<void> {
         const verdict = await runGuardrails({
           rails: input.guards.guardrails,
           stage: "output",
-          text: turnText,
+          text: answerText,
           onError: input.guards.guardrailOnError,
           isCancelled: input.run.isCancelled,
         });
@@ -1306,7 +1366,7 @@ async function driveAgentLoop(input: AgentLoopInput): Promise<void> {
        */
       try {
         // 空话不成候选，那一条在提炼那一侧判——**这里不重复一遍**。
-        input.work.extractMemory?.({ text: turnText });
+        input.work.extractMemory?.({ text: answerText });
       } catch {
         // **提炼失败不影响这一次的收场。** 它是附加物，不是主体。
       }
diff --git a/packages/sdk/src/agent/core/start-input.ts b/packages/sdk/src/agent/core/start-input.ts
index f9cc6d37..bf56f06f 100644
--- a/packages/sdk/src/agent/core/start-input.ts
+++ b/packages/sdk/src/agent/core/start-input.ts
@@ -23,6 +23,12 @@ export interface AgentStartInput {
   readonly mode?: string;
   /** 本轮工具名单。**不给表示不限制到更窄**，给了就只有这些能调。 */
   readonly toolNames?: readonly string[];
+  /**
+   * Tools shown to the model even though the bound character does not list them. Each still has to be in this
+   * run's list and ceiling, so it never widens what the run may call. A subagent holding board steps gets the board
+   * tools this way: it has to report on its steps whatever role it plays.
+   */
+  readonly grantedTools?: readonly string[];
   /** 可选。**不绑 Character 也能跑**，走默认执行者。 */
   readonly characterName?: string;
   /**
@@ -115,6 +121,7 @@ function narrowNames(
 
 /** 冻住的这一轮配方。转向和续跑都改不了它。 */
 export interface FrozenStart {
+  readonly workspace?: "none";
   readonly idempotencyKey: string;
   readonly limits: ModeLimits;
   /** 有效工具名单。`undefined` 表示不额外收窄。 */
@@ -158,6 +165,7 @@ export interface FrozenStart {
    */
   readonly subagentWorkspaces?: {
     readonly requireWorkspace: boolean;
+    readonly parentReads?: true;
     readonly workspaces: readonly {
       readonly id: string;
       readonly rootId: string;
@@ -256,6 +264,7 @@ function intersectNetwork(wanted: NetworkAllowance | undefined, ceiling: Network
  */
 export function freezeStart(input: {
   readonly start: AgentStartInput;
+  readonly workspace?: "none";
   readonly posture: Posture;
   readonly assembled: ModeCapabilities;
   /** Runtime 的出网上限。 */
@@ -303,6 +312,7 @@ export function freezeStart(input: {
    */
   readonly subagentWorkspaces?: {
     readonly requireWorkspace: boolean;
+    readonly parentReads?: true;
     readonly workspaces: readonly {
       readonly id: string;
       readonly rootId: string;
@@ -424,6 +434,7 @@ export function freezeStart(input: {
   });
 
   const frozen = {
+    ...(input.workspace === undefined ? {} : { workspace: input.workspace }),
     sandbox,
     skillNames: narrowNames(start.skillNames, available.skillNames ?? [], "skills"),
     mcpConnections: narrowNames(
diff --git a/packages/sdk/src/composition/api/index.ts b/packages/sdk/src/composition/api/index.ts
index 922224b5..da408b14 100644
--- a/packages/sdk/src/composition/api/index.ts
+++ b/packages/sdk/src/composition/api/index.ts
@@ -36,3 +36,7 @@ export type {
   TranscriptDefaults,
   TranscriptReceipt,
 } from "../core/index.js";
+
+export type { TypeSafeInput, TypeSafeSurface } from "../core/index.js";
+
+export type { AgentRunGrants } from "../core/agent-permissions.js";
diff --git a/packages/sdk/src/composition/core/agent-context.ts b/packages/sdk/src/composition/core/agent-context.ts
index 1bba71f6..c6774fce 100644
--- a/packages/sdk/src/composition/core/agent-context.ts
+++ b/packages/sdk/src/composition/core/agent-context.ts
@@ -74,9 +74,15 @@ export { DEFAULT_CONTEXT_WINDOW_TOKENS };
  */
 export type AssembleAgentContext = typeof assembleAgentContext;
 
-export async function assembleAgentContext(input: {
+export async function assembleAgentContext(input: ({
+  readonly workspace?: "required";
   readonly reader: WorkspaceReader;
   readonly workingDirectory: string;
+} | {
+  readonly workspace: "none";
+  readonly reader?: never;
+  readonly workingDirectory?: never;
+}) & {
   readonly messages: readonly ModelMessage[];
   readonly options: AgentContextOptions;
   /**
@@ -157,7 +163,7 @@ export async function assembleAgentContext(input: {
   // ——不编一份。只读根上那份的话，monorepo 里最具体的那一份规矩永远
   // 不生效，而那恰恰是最该生效的一份。
   const repository: InstructionFile[] = [];
-  if (options.projectTrusted === true) {
+  if (input.workspace !== "none" && options.projectTrusted === true) {
     for (const layer of instructionLayers(input.workingDirectory)) {
       for (const name of input.config.instructionNames) {
         const path = layer === "." ? name : `${layer}/${name}`;
@@ -179,7 +185,7 @@ export async function assembleAgentContext(input: {
     maxChars: input.config.maxInstructionChars,
   });
 
-  const nested = resolveNestedRules({
+  const nested = input.workspace === "none" ? { items: [] } : resolveNestedRules({
     rules: options.nestedRules ?? [],
     ...(options.rulePaths === undefined ? {} : { paths: options.rulePaths }),
     ...(options.namedRules === undefined ? {} : { named: options.namedRules }),
diff --git a/packages/sdk/src/composition/core/agent-permissions.ts b/packages/sdk/src/composition/core/agent-permissions.ts
new file mode 100644
index 00000000..08d09214
--- /dev/null
+++ b/packages/sdk/src/composition/core/agent-permissions.ts
@@ -0,0 +1,72 @@
+import { createPrologueError, type ExactRef } from "../../kernel/core/index.js";
+import { pathSegments, type EffectChain, type EffectProposal, type EffectScopeAuthority, type PolicyRule } from "../../effect-policy/core/index.js";
+
+/** Trusted App authority for one Run on its already authorized root. Never inherited. */
+export interface AgentRunGrants {
+  readonly toolNames: readonly string[];
+  /** This bounded API accepts [] or ["."]. Authorize a narrower directory as its own root. */
+  readonly paths: readonly string[];
+}
+
+export function freezeAgentGrants(input: AgentRunGrants | undefined): AgentRunGrants | undefined {
+  if (input === undefined) return undefined;
+  if (!Array.isArray(input.toolNames) || !Array.isArray(input.paths) ||
+      input.toolNames.some((name) => typeof name !== "string" || name.length === 0) ||
+      input.paths.some((path) => typeof path !== "string" || path.length === 0 ||
+        path.startsWith("/") || path.includes("\\") || /^[A-Za-z]:/.test(path) || path.split("/").includes(".."))) {
+    throw createPrologueError("AGENT_START_INVALID", "Run grants require explicit tool names and paths inside the authorized root.");
+  }
+  if (input.paths.length > 1 || input.paths.some((path) => path !== ".")) {
+    throw createPrologueError("AGENT_START_INVALID", "Run grants accept only [] or ['.']; authorize a narrower directory as its own root.");
+  }
+  return Object.freeze({ toolNames: Object.freeze([...input.toolNames]), paths: Object.freeze([...input.paths]) });
+}
+
+/** A private facade over the same effect ledger. Origin strings cannot create this authority. */
+export function bindAgentEffects(input: {
+  readonly effects: EffectChain;
+  readonly grants: AgentRunGrants;
+  readonly session: ExactRef<"session">;
+  readonly root: ExactRef<"authorized-root">;
+  readonly runId: () => string | undefined;
+  readonly scopes: WeakMap<object, EffectScopeAuthority>;
+}): { readonly effects: EffectChain; readonly revoke: () => void } {
+  let active = true;
+  let scope: EffectScopeAuthority | undefined;
+  const rules: readonly PolicyRule[] = Object.freeze([
+    ...input.grants.toolNames.map((name): PolicyRule => Object.freeze({ source: "runtime", effect: "approved", match: Object.freeze({ what: "tool", name }) })),
+    ...input.grants.paths.map((path): PolicyRule => Object.freeze({ source: "runtime", effect: "approved", kinds: Object.freeze(["mutate-local"] as const), match: Object.freeze({ what: "path", under: Object.freeze([...pathSegments(path)]) }) })),
+  ]);
+  const authority = (): EffectScopeAuthority => {
+    const run = input.runId();
+    if (!active || run === undefined) throw createPrologueError("EFFECT_NOT_AUTHORIZED", "This Run's trusted grants are no longer active.");
+    scope ??= Object.freeze({ id: JSON.stringify({ session: input.session, run, root: input.root }), rules, active: () => active });
+    return scope;
+  };
+  const sealed = (proposal: EffectProposal): EffectProposal => {
+    const held = authority();
+    const copied = structuredClone(proposal);
+    const subject = copied.subject;
+    if (subject.what === "path" && subject.segments.some((part) => part === ".." || part.includes("/") || part.includes("\\"))) {
+      throw createPrologueError("EFFECT_NOT_AUTHORIZED", "A scoped path must remain inside its authorized root.");
+    }
+    const result: EffectProposal = { ...copied,
+      origin: { ...copied.origin, session: input.session.id, run: input.runId()! },
+      inputFingerprint: `@run-scope:${JSON.stringify([held.id, copied.kind, copied.subject, copied.escalation === true, copied.inputFingerprint])}`,
+    };
+    input.scopes.set(result, held);
+    return result;
+  };
+  const effects = Object.freeze<EffectChain>({ ...input.effects,
+    evaluate: (proposal: EffectProposal) => input.effects.evaluate(sealed(proposal)),
+    restriction: (proposal: EffectProposal) => input.effects.restriction(sealed(proposal)),
+    prepare: (proposal, now, restrict) => input.effects.prepare(sealed(proposal), now, restrict),
+    dispatch: (ref, request) => {
+      const held = authority();
+      const bound = { ...request, recheck: async (effect: Parameters<typeof request.recheck>[0]) => active && await request.recheck(effect) };
+      input.scopes.set(bound, held);
+      return input.effects.dispatch(ref, bound);
+    },
+  });
+  return { effects, revoke: () => { active = false; } };
+}
diff --git a/packages/sdk/src/composition/core/images.ts b/packages/sdk/src/composition/core/images.ts
index 9cb2a8da..233775d4 100644
--- a/packages/sdk/src/composition/core/images.ts
+++ b/packages/sdk/src/composition/core/images.ts
@@ -1,199 +1,93 @@
 import { createPrologueError, type ExactRef } from "../../kernel/core/index.js";
-import {
-  assertImageCount,
-  assertImageRequest,
-  unknownUsage,
-  type ImagePayload,
-  type ImageRequest,
-  type SelectImageAdapter,
-  type UsageLedger,
-} from "../../model/core/index.js";
-import type { PrologueHost } from "../../host/core/index.js";
-import { readAllBytes, type ResourceStore } from "../../resource/core/index.js";
-
-/**
- * 一张输入图最多多大。
- *
- * **比资源库那道存储上限更严，这是有意的。** 资源库管「能不能存」（单份
- * 8MB）；这一层管「一次读进内存受不受得了」——改图要把整份字节读出来再
- * base64 一遍，一张 8MB 的图峰值是原件加编码串加整个 JSON，三十来兆，
- * 而 `maxPerCall` 允许一次带好几张。
- *
- * 第一版这个数写的是 8MB，和资源库那道一样——于是它**永远轮不到生效**，
- * 存的那一关先拒了。一个够不着的上限和没有上限是同一件事。
- */
-const MAX_INPUT_IMAGE_BYTES = 4 * 1024 * 1024;
-
-/**
- * 出图接在装配根上。
- *
- * **App 不起 Agent 也要得到图**——这是把它做成 adapter 而不是只做成一个
- * 给模型点的工具的全部理由。参照物里 Codex / Grok / OpenAI Agents JS 都
- * 只做了那个工具，于是他们的 App 想要一张图，得先假装派一个 Agent。
- */
-
-/** 一次出图的回执。**里面没有字节。** */
-export interface ImageReceipt {
-  /** 每一张一份资源引用。**正文只以引用出现**，字节从不进回执。 */
-  readonly images: readonly ExactRef<"resource">[];
-  /** 对方改写过的提示词。有些服务会重写，人得看得见它照着什么画的。 */
-  readonly revisedPrompt: string | undefined;
-}
+import { assertImageCount, assertImageRequest, unknownUsage, type ImagePayload, type ImageRequest, type ImageLimits, type SelectImageAdapter, type UsageLedger } from "../../model/core/index.js";
+import { readAllBytes, type ResourceStore, type ResourceStage } from "../../resource/core/index.js";
+import type { ModelResponses } from "./model-response.js";
 
+export interface ImageReceipt { readonly images: readonly ExactRef<"resource">[]; readonly revisedPrompt: string | undefined }
 export interface ImageGenerateInput extends ImageRequest {
-  readonly protocol: string;
-  readonly endpoint: string;
-  readonly credentialRef: ExactRef<"credential">;
-  readonly timeoutMs?: number;
+  readonly protocol: string; readonly endpoint: string; readonly credentialRef?: ExactRef<"credential">;
+  /** Explicitly unauthenticated local model only; the Host validates origin and allowance. */
+  readonly auth?: "none"; readonly timeoutMs?: number; readonly signal?: AbortSignal;
 }
-
-/** 模型那条路用哪一份配置。**模型说不出这几样，也不该说得出。** */
 export interface ImageDefaults {
-  readonly protocol: string;
-  readonly endpoint: string;
-  readonly model: string;
-  readonly credentialRef: ExactRef<"credential">;
-  readonly timeoutMs?: number;
+  readonly protocol: string; readonly endpoint: string; readonly model: string;
+  readonly credentialRef?: ExactRef<"credential">; readonly auth?: "none"; readonly timeoutMs?: number;
 }
-
 export interface ImageSurface {
-  /**
-   * 要几张图。生成、改图、局部重绘**走的是同一条路**——差别只在给不给
-   * `from` 和 `mask`。
-   */
   generate(input: ImageGenerateInput): Promise<ImageReceipt>;
-  /**
-   * 定下模型那条路用哪一份配置。
-   *
-   * **为什么不让模型点名**：用哪个 provider、往哪发、拿哪把钥匙，是这台
-   * Runtime 的配置。让模型在工具参数里点，等于把选 provider 和选钥匙的
-   * 权力交给了提示词——而提示词可能是从网页上读来的。
-   *
-   * **为什么不做成构造参数**：钥匙引用是 `runtime.credentials.write`
-   * 之后才有的东西，而那要先有 Runtime。
-   *
-   * 不设就是模型那条路不可用——工具**仍在闭集里**，调用时照实报。
-   */
   useDefaults(defaults: ImageDefaults): void;
-  /** 现在定的是哪一份。没设过就是 `undefined`。 */
   defaults(): ImageDefaults | undefined;
 }
 
+/** Native image protocols share the Runtime model gate, usage ledger and resource store. */
 export function createImageSurface(deps: {
-  readonly host: PrologueHost;
-  readonly resources: ResourceStore;
-  readonly usage: UsageLedger;
-  readonly selectAdapter: SelectImageAdapter;
-  readonly assertReady: () => void;
-  readonly defaultTimeoutMs: number;
+  readonly responses: ModelResponses; readonly resources: ResourceStore; readonly usage: UsageLedger;
+  readonly selectAdapter: SelectImageAdapter; readonly assertReady: () => void; readonly defaultTimeoutMs: number;
+  readonly limits: ImageLimits;
 }): ImageSurface {
-  /**
-   * 把一份输入图读出来。**读和上限都归 `resource`**——这一层只说得出
-   * 「我受得了多大」，说不出「按块读怎么算读完」。
-   */
-  const bytesOf = async (ref: ExactRef<"resource">): Promise<ImagePayload> => ({
-    bytes: await readAllBytes(deps.resources, ref, MAX_INPUT_IMAGE_BYTES),
-    mediaType: undefined,
-  });
-
+  const bytesOf = async (ref: ExactRef<"resource">): Promise<ImagePayload> => ({ bytes: await readAllBytes(deps.resources, ref, 4 * 1024 * 1024), mediaType: undefined });
   let defaults: ImageDefaults | undefined;
-
   return {
-    useDefaults(next): void {
-      deps.assertReady();
-      defaults = next;
-    },
-
-    defaults(): ImageDefaults | undefined {
-      return defaults;
-    },
-
-    async generate(input): Promise<ImageReceipt> {
+    useDefaults(next) { deps.assertReady(); defaults = next; },
+    defaults: () => defaults,
+    generate(input) { return deps.responses.run(input.signal, async lifecycle => {
       deps.assertReady();
+      const timeoutMs = input.timeoutMs ?? deps.defaultTimeoutMs;
+      const signal = AbortSignal.any([lifecycle, AbortSignal.timeout(timeoutMs)]);
+      signal.throwIfAborted();
       const adapter = deps.selectAdapter(input.protocol);
-      /*
-       * **两道校验都在出网之前。**
-       *
-       * 要多了发出去再被拒，已经花了一次往返；而有些服务是**截到上限
-       * 就返回**——那时调用方拿到「要了十张回来四张」，还以为是全部。
-       */
-      assertImageRequest(input);
-      assertImageCount(input, adapter);
-
+      assertImageRequest(input); assertImageCount(input, adapter);
       const from: ImagePayload[] = [];
-      for (const ref of input.from ?? []) {
-        from.push(await bytesOf(ref));
-      }
+      for (const ref of input.from ?? []) from.push(await bytesOf(ref));
       const mask = input.mask === undefined ? undefined : await bytesOf(input.mask);
       const body = adapter.buildBody({ request: input, from, mask });
-
-      const handle = await deps.host.prepareModelNetwork({
-        // **同一条出网门、同一个通道。** 出图不另开一条路。
-        channel: "model",
-        endpoint: input.endpoint,
-        credentialRef: input.credentialRef,
-        body: body.bytes,
-        timeoutMs: input.timeoutMs ?? deps.defaultTimeoutMs,
-        authScheme: adapter.authScheme,
-        /*
-         * **类型跟着请求体走。** adapter 说了才写；没说就让 Host 用它的
-         * 默认（JSON）。改图那条路发的是 multipart，而它的 boundary 是
-         * 按这一次的内容算出来的——所以只能在这儿一起递。
-         */
-        extraHeaders: {
-          ...adapter.extraHeaders(),
-          ...(body.contentType === undefined ? {} : { "content-type": body.contentType }),
-        },
-      });
-      let raw = "";
-      await deps.host.executeModelNetwork({
-        handle,
-        onFrame: (chunk) => {
-          raw += chunk;
-        },
-      });
-      const outcome = adapter.readOutcome(raw);
-
-      /*
-       * 记一笔用量。**token 那几项留空，不补零。**
-       *
-       * 按张计费的调用报 0 token，会让预算那本账把一次真花了钱的调用
-       * 算成免费。`unknownUsage()` 本来就是这个意思——未知单独记着。
-       */
-      deps.usage.record({
-        ...unknownUsage(),
-        ...(outcome.spend.amount === undefined && outcome.spend.currency === undefined
-          ? {}
-          : {
-              cost: {
-                source: "reported" as const,
-                amount: outcome.spend.amount,
-                currency: outcome.spend.currency,
-              },
-            }),
-      });
-
-      /*
-       * 字节落进资源库，回执里只带引用。
-       *
-       * **这不是为出图新立的规矩**——「大正文只给引用」这一册本来就有。
-       * Codex 那边是回来之后再把 base64 从历史里擦掉（`result.clear()`），
-       * 我们早一步：它从一开始就没进过事件账。
-       */
-      const images: ExactRef<"resource">[] = [];
-      for (const [index, image] of outcome.images.entries()) {
-        const stage = deps.resources.stage({
-          mediaKind: "binary",
-          byteLength: image.bytes.byteLength,
-          label: `generated image ${String(index + 1)} of ${String(outcome.images.length)}`,
-        });
-        stage.write(image.bytes);
-        images.push(stage.publish().ref);
+      signal.throwIfAborted();
+      const response = await deps.responses.request({ channel: "model", endpoint: input.endpoint,
+        ...(input.credentialRef ? { credentialRef: input.credentialRef } : {}), authScheme: input.auth ?? adapter.authScheme, body: body.bytes, timeoutMs,
+        maxResponseBytes: deps.limits.maxResponseBytes,
+        extraHeaders: { ...adapter.extraHeaders(), ...(body.contentType ? { "content-type": body.contentType } : {}) },
+      }, signal);
+      const outcome = adapter.readOutcome(new TextDecoder().decode(response.bytes), deps.limits);
+      signal.throwIfAborted();
+      if (!outcome.images.length) throw createPrologueError("MODEL_RESPONSE_INVALID", "The image response carried no images.");
+      deps.usage.record({ ...unknownUsage(), ...(outcome.spend.amount === undefined && outcome.spend.currency === undefined ? {} : {
+        cost: { source: "reported" as const, amount: outcome.spend.amount, currency: outcome.spend.currency },
+      }) });
+      const ready: ImagePayload[] = [];
+      for (const source of outcome.images.slice(0, deps.limits.maxImages)) {
+        signal.throwIfAborted();
+        if ("bytes" in source) ready.push(source);
+        else {
+          const downloaded = await deps.responses.request({ channel: "model", method: "GET", imageDownloadOf: response.handle,
+            endpoint: source.url, authScheme: "none", body: new Uint8Array(), timeoutMs, maxResponseBytes: deps.limits.maxImageBytes,
+          }, signal);
+          ready.push({ bytes: downloaded.bytes, mediaType: source.mediaType ?? downloaded.contentType?.split(";")[0]?.trim() });
+        }
       }
-      return {
-        images: Object.freeze(images),
-        revisedPrompt: outcome.revisedPrompt,
-      };
-    },
+      for (const image of ready) validateImage(image, deps.limits.maxImageBytes);
+      signal.throwIfAborted();
+      const stages: ResourceStage[] = [], images: ExactRef<"resource">[] = [];
+      try {
+        for (const [index, image] of ready.entries()) {
+          const stage = deps.resources.stage({ mediaKind: "binary", byteLength: image.bytes.byteLength, label: `generated image ${index + 1}` });
+          stages.push(stage); stage.write(image.bytes);
+        }
+        for (const stage of stages) { signal.throwIfAborted(); images.push(stage.publish().ref); }
+        return { images: Object.freeze(images), revisedPrompt: outcome.revisedPrompt };
+      } catch (error) {
+        for (const stage of stages) stage.discard();
+        await Promise.all(images.map(ref => deps.resources.revoke(ref)));
+        throw error;
+      }
+    }); },
   };
 }
+function validateImage(image: ImagePayload, maxBytes: number): void {
+  const b = image.bytes;
+  if (!b.length || b.length > maxBytes) throw createPrologueError("MODEL_RESPONSE_INVALID", "Image data exceeds its byte limit.");
+  const type = b.length >= 8 && [137,80,78,71,13,10,26,10].every((n,i) => b[i] === n) ? "image/png"
+    : b.length >= 3 && b[0] === 255 && b[1] === 216 && b[2] === 255 ? "image/jpeg"
+    : b.length >= 12 && new TextDecoder().decode(b.slice(0,4)) === "RIFF" && new TextDecoder().decode(b.slice(8,12)) === "WEBP" ? "image/webp"
+    : b.length >= 6 && /GIF8[79]a/.test(new TextDecoder().decode(b.slice(0,6))) ? "image/gif" : undefined;
+  if (!type || image.mediaType !== undefined && image.mediaType !== "application/octet-stream" && image.mediaType.toLowerCase() !== type) throw createPrologueError("MODEL_RESPONSE_INVALID", "Image bytes do not match a supported image MIME type.");
+}
diff --git a/packages/sdk/src/composition/core/index.ts b/packages/sdk/src/composition/core/index.ts
index 83f41a8c..dbba51a5 100644
--- a/packages/sdk/src/composition/core/index.ts
+++ b/packages/sdk/src/composition/core/index.ts
@@ -41,3 +41,7 @@ export type {
   TranscriptDefaults,
   TranscriptReceipt,
 } from "./speech.js";
+
+export type { TypeSafeInput, TypeSafeSurface } from "./typesafe.js";
+
+export type { AgentRunGrants } from "./agent-permissions.js";
diff --git a/packages/sdk/src/composition/core/model-response.ts b/packages/sdk/src/composition/core/model-response.ts
new file mode 100644
index 00000000..38d03592
--- /dev/null
+++ b/packages/sdk/src/composition/core/model-response.ts
@@ -0,0 +1,74 @@
+import { createPrologueError } from "../../kernel/core/index.js";
+import type { ModelNetworkHandle, ModelNetworkPrepareInput, PrologueHost } from "../../host/core/index.js";
+
+/** Bounded native model I/O shared by image and structured protocols. Host alone performs network access. */
+export function createModelResponses(host: PrologueHost) {
+  const active = new Map<AbortController, Promise<unknown>>();
+  let closed = false;
+  const shutdown = new AbortController();
+  return {
+    /** Covers parsing and publication too, not only the network Promise. */
+    run<T>(signal: AbortSignal | undefined, operation: (signal: AbortSignal) => Promise<T>): Promise<T> {
+      if (closed) return Promise.reject(createPrologueError("RUNTIME_NOT_READY", "Model inference is closed."));
+      const controller = new AbortController();
+      const combined = AbortSignal.any([shutdown.signal, controller.signal, ...(signal ? [signal] : [])]);
+      const task = (async () => { combined.throwIfAborted(); const result = await operation(combined); combined.throwIfAborted(); return result; })();
+      active.set(controller, task);
+      void task.finally(() => active.delete(controller)).catch(() => {});
+      return task;
+    },
+    request(input: ModelNetworkPrepareInput, signal?: AbortSignal): Promise<{ handle: ModelNetworkHandle; bytes: Uint8Array; contentType?: string }> {
+      if (closed) return Promise.reject(createPrologueError("RUNTIME_NOT_READY", "Model inference is closed."));
+      const controller = new AbortController();
+      const combined = AbortSignal.any([shutdown.signal, controller.signal, AbortSignal.timeout(input.timeoutMs), ...(signal ? [signal] : [])]);
+      const task = (async () => {
+        let handle: ModelNetworkHandle | undefined;
+        const abort = () => { if (handle) void host.cancelModelNetwork(handle).catch(() => {}); };
+        combined.addEventListener("abort", abort, { once: true });
+        try {
+          combined.throwIfAborted();
+          const prepared = host.prepareModelNetwork(input);
+          void prepared.then(late => { if (combined.aborted) return host.cancelModelNetwork(late); }).catch(() => {});
+          handle = await waitForModelWork(prepared, combined);
+          if (combined.aborted) { await host.cancelModelNetwork(handle); combined.throwIfAborted(); }
+          const chunks: Uint8Array[] = [];
+          let length = 0, contentType: string | undefined;
+          const outcome = await waitForModelWork(host.executeModelNetwork({ handle, onFrame: () => {},
+            onResponse: response => {
+              if (response.status < 200 || response.status > 299) throw createPrologueError("MODEL_HTTP_ERROR", "The model provider rejected the request.", { status: response.status });
+              contentType = response.contentType;
+            },
+            onBytes: chunk => {
+              combined.throwIfAborted(); length += chunk.byteLength;
+              if (input.maxResponseBytes !== undefined && length > input.maxResponseBytes) throw createPrologueError("MODEL_RESPONSE_INVALID", "The model response exceeded its byte limit.");
+              chunks.push(Uint8Array.from(chunk));
+            },
+          }), combined);
+          combined.throwIfAborted();
+          if (outcome === "cancelled") throw createPrologueError("CANCELLED", "Model inference was cancelled.");
+          const bytes = new Uint8Array(length); let offset = 0;
+          for (const chunk of chunks) { bytes.set(chunk, offset); offset += chunk.byteLength; }
+          return { handle, bytes, ...(contentType ? { contentType } : {}) };
+        } catch (error) {
+          if (handle) await host.cancelModelNetwork(handle).catch(() => {});
+          combined.throwIfAborted(); throw error;
+        } finally { combined.removeEventListener("abort", abort); }
+      })();
+      active.set(controller, task);
+      void task.finally(() => active.delete(controller)).catch(() => {});
+      return task;
+    },
+    async close() { closed = true; shutdown.abort(); for (const controller of active.keys()) controller.abort(); await Promise.allSettled([...active.values()]); },
+  };
+}
+export type ModelResponses = ReturnType<typeof createModelResponses>;
+
+/** Stop waiting for an uncooperative preparation; a late handle is cancelled by its owner. */
+function waitForModelWork<T>(work: Promise<T>, signal: AbortSignal): Promise<T> {
+  if (signal.aborted) { void work.catch(() => {}); return Promise.reject(signal.reason); }
+  return new Promise<T>((resolve, reject) => {
+    const abort = () => reject(signal.reason);
+    signal.addEventListener("abort", abort, { once: true });
+    void work.then(resolve, reject).finally(() => signal.removeEventListener("abort", abort));
+  });
+}
diff --git a/packages/sdk/src/composition/core/runtime.ts b/packages/sdk/src/composition/core/runtime.ts
index 8e1690f1..210d161a 100644
--- a/packages/sdk/src/composition/core/runtime.ts
+++ b/packages/sdk/src/composition/core/runtime.ts
@@ -1,3 +1,8 @@
+import { fingerprintText } from "../../kernel/core/index.js";
+import { bindAgentEffects, freezeAgentGrants, type AgentRunGrants } from "./agent-permissions.js";
+import { createModelResponses } from "./model-response.js";
+import { createTypeSafeSurface, type TypeSafeSurface } from "./typesafe.js";
+import { TYPESAFE_SYSTEMONE } from "../../model/plugin/typesafe-systemone.js";
 import { sessionRecovery } from "./session-recovery.js";
 import type { SessionRecoveryReport } from "../../session/core/index.js";
 import type { RandomSource } from "../../kernel/core/index.js";
@@ -54,6 +59,7 @@ import {
   createHeartbeatRegistry,
   createLocalQueue,
   createTaskBoardStore,
+  type BoardAssignee,
   type BoardLimits,
   type GoalRegistry,
   type TaskBoardStore,
@@ -335,6 +341,7 @@ import {
 import {
   createChildStartBarrier,
   recoverSubagents,
+  refreshClosedSubagentRun,
 } from "./subagent-recovery.js";
 import {
   freezeSubagentWorkspaces,
@@ -732,6 +739,7 @@ export interface Runtime {
    * 回执里**只有资源引用，没有字节**。
    */
   readonly images: ImageSurface;
+  readonly typesafe: TypeSafeSurface;
   /**
    * 网页搜索。**App 不起 Agent 也要搜得了**——和出图同一个理由。
    *
@@ -890,9 +898,17 @@ export interface Runtime {
    * `runTool` 是**插件缝**：不给就用这台 Runtime 的默认系统工具执行器，
    * App 可以整个换成自己的。换不掉的是它下游那条副作用链。
    */
-  startAgentRun(input: {
-    readonly session: Session;
+  startAgentRun(input: ({
+    readonly workspace?: "required";
     readonly rootRef: ExactRef<"authorized-root">;
+  } | {
+    /** Pure inference on this Session: no workspace capability or tools. */
+    readonly workspace: "none";
+    readonly rootRef?: never;
+  }) & {
+    readonly session: Session;
+    /** Trusted App grants, frozen for this exact Session/Run/root; never global or inherited. */
+    readonly grants?: AgentRunGrants;
     readonly start: StartRunInput;
     readonly agent: AgentStartInput;
     readonly runTool?: ToolRunner;
@@ -931,7 +947,8 @@ export interface Runtime {
      */
     readonly todo: TodoList;
     /**
-     * 这一次 Run 真正生效的权限快照。
+     * 这一次 Run 的规则与沙箱基线快照。显式 grants 仅在私有的 Run
+     * 作用域生效，其实际决策记录在同一 Effect 账中，不转成全局规则。
      *
      * **整个 Run 用同一份**，事后能核「当时批的是这一份」。Run 期间
      * 改规则不影响这一次。
@@ -1563,8 +1580,10 @@ export async function createRuntime(
     },
   };
   const policyStore = createStoredPolicy(store);
+  const effectScopes = new WeakMap<object, import("../../effect-policy/core/index.js").EffectScopeAuthority>();
   const effects = createEffectChain({
     store: policyStore,
+    scopeAuthority: effectScopes,
     // 派出权威在 Host 那边。**JS 侧的计数器不算已派出。**
     authority: input.host,
     newId: () => {
@@ -1591,7 +1610,7 @@ export async function createRuntime(
     defaultPosture: posture.sandbox,
     defaultIgnored: runtimeConfig.context.ignored,
   });
-  const tools = createToolCatalog();
+  const tools = createToolCatalog({ ...DEFAULT_TOOL_LIMITS, maxTimeoutMs: runtimeConfig.tool.maxTimeoutMs });
   const systemTools = resolveSystemTools({ assembled });
   const protocols = assembleProtocols(
     slotImpl<Parameters<typeof assembleProtocols>[0]>(assembly, input.slots, "model-protocol"),
@@ -1754,6 +1773,10 @@ export async function createRuntime(
      * 名字的节点，只报得了还没定负责人的那些。
      */
     readonly by?: () => ExactRef<"character"> | undefined;
+    /** The run's own session: tasks assigned to it are this run's to report on. Not given outside a run. */
+    readonly session?: () => ExactRef<"session"> | undefined;
+    /** How other sessions read on the board from this run. */
+    readonly name?: (assignee: BoardAssignee) => string | undefined;
     /** 板的上界。**不给就用 Runtime 那份**；这条会话装的包配得动它。 */
     readonly boardLimits?: BoardLimits;
     /** Omitted keeps the Runtime scope; explicit lists freeze the Agent selection. */
@@ -1832,6 +1855,8 @@ export async function createRuntime(
       characters,
       limits: at.boardLimits ?? runtimeConfig.orchestration.board,
       by: at.by ?? (() => undefined),
+      ...(at.session === undefined ? {} : { session: at.session }),
+      ...(at.name === undefined ? {} : { name: at.name }),
       now: async () => (await runtime.readClock()).wallTimeMs,
     }),
     mutator: runtime.createMutator({
@@ -2020,9 +2045,26 @@ export async function createRuntime(
      * 没有「现在谁在跑」，所以这个参数不给。
      */
     origin?: () => EffectOrigin | undefined,
+    scopedEffects: EffectChain = effects,
   ): ToolInvoker =>
     createToolInvoker({
       ...(origin === undefined ? {} : { origin }),
+      // A subagent's character is named as id@version; one that does not exist fails before the dispatch is reviewed.
+      precheck: (name, args) => {
+        if (name !== "dispatch-subagent" || typeof args.character !== "string") return;
+        const ref = parseCharacterRef(args.character);
+        if (ref === undefined) return;
+        try {
+          characters.resolve(ref);
+        } catch {
+          const id = args.character.slice(0, args.character.lastIndexOf("@"));
+          const known = characters.list().map((one) => one.ref.id).filter((each) => each.startsWith(`${id}@`));
+          throw createPrologueError(
+            "CHARACTER_NOT_FOUND",
+            `There is no character "${args.character}"${known.length ? `; use one of: ${known.join(", ")}` : ""}. Nothing was dispatched.`,
+          );
+        }
+      },
       /*
        * **在这里冻一份。**
        *
@@ -2030,7 +2072,7 @@ export async function createRuntime(
        * 那一刻的定义；登记表后来怎么改，都改不动正在跑的这一次。
        */
       catalog: tools.freeze(),
-      effects,
+      effects: scopedEffects,
       resources,
       observation,
       allowed: systemTools,
@@ -2353,8 +2395,12 @@ export async function createRuntime(
    * 派出走的是同一条 `effects` 链，所以「关掉 Runtime 之后动作为零」
    * 不用在这一册再写一遍——封口之后链本身就不派了。
    */
+  const modelResponses = createModelResponses(input.host);
+  const typesafe = createTypeSafeSurface({ responses: modelResponses, adapter: TYPESAFE_SYSTEMONE, usage,
+    assertReady: () => assertReady(state), maxResponseBytes: runtimeConfig.model.maxStructuredResponseBytes });
   const images = createImageSurface({
-    host: input.host,
+    responses: modelResponses,
+    limits: runtimeConfig.model.images,
     resources,
     usage,
     selectAdapter: imageAdapters,
@@ -2625,6 +2671,10 @@ export async function createRuntime(
       resources.assertUsableAs(binding.ref, binding.as),
     track: (run: Run, origin: { readonly session: string; readonly startedAtMs: number }) => {
       live.add(run);
+      let detach: (() => void) | undefined;
+      let terminal = false;
+      detach = run.subscribe(event => { if (isTerminalEventType(event.type)) { terminal = true; live.delete(run); detach?.(); } });
+      if (terminal) detach();
       if (isTerminalRunState(run.state)) {
         return;
       }
@@ -2711,6 +2761,83 @@ export async function createRuntime(
     return out;
   };
 
+  const makeMutator = ({
+      rootRef,
+      mode,
+      requireObserved,
+      sessionId,
+      runId,
+      sandbox: sandboxCeiling,
+    }: Parameters<Runtime["createMutator"]>[0], scopedEffects: EffectChain = effects): WorkspaceMutator => {
+      assertReady(state);
+      assertAssembled(assembled, "workspace", "Changing files in a project");
+      const root = workspace.get(rootRef);
+      if (root === undefined) {
+        throw createPrologueError(
+          "ROOT_NOT_FOUND",
+          "That authorized root is not available.",
+        );
+      }
+      const sandbox =
+        sandboxCeiling === undefined
+          ? root.posture
+          : narrowerSandbox(sandboxCeiling, root.posture);
+      // 档位来自模式 ∩ Runtime 姿态 ∩ 装配——三者求交，只会更窄。
+      const limits = resolveModeLimits({
+        mode: mode ?? "plan",
+        posture: { ...posture, sandbox },
+        assembled,
+      });
+      return createWorkspaceMutator({
+        host: input.host,
+        root,
+        effects: scopedEffects,
+        limits,
+        canWrite: (path) => workspace.canWrite(rootRef, path),
+        newId: () => {
+          nextId += 1;
+          return `${nextId.toString(36)}-${random.nextBelow(0xffffff).toString(36)}`;
+        },
+        reviewMaxBytes: runtimeConfig.resource.publish.maxBytes,
+        publishReview: async (review: PatchReview | CommandReview | RewindReview) => {
+          const bytes = new TextEncoder().encode(JSON.stringify(review));
+          const stage = resources.stage({
+            mediaKind: "json",
+            byteLength: bytes.byteLength,
+            label:
+              review.kind === "workspace-patch"
+                ? `workspace-patch:${review.path}`
+                : review.kind === "workspace-command"
+                  ? `workspace-command:${review.executable}`
+                  : `workspace-rewind:${review.checkpointId}`,
+          });
+          try {
+            const chunk = Math.max(1, runtimeConfig.resource.publish.maxChunkBytes);
+            for (let offset = 0; offset < bytes.byteLength; offset += chunk) {
+              stage.write(bytes.subarray(offset, Math.min(offset + chunk, bytes.byteLength)));
+            }
+            return (await stage.publishDurable()).ref;
+          } catch (error) {
+            stage.discard();
+            throw error;
+          }
+        },
+        /*
+         * **只在模型那条路上要「先读后写」。**
+         *
+         * 这条规矩挡的是「凭记忆改文件」——而记忆是模型才有的问题：
+         * 它可能记的是上一次会话、另一个分支、或者干脆是它编的。App
+         * 自己调这个入口时手上就有内容（不然它拼不出 `nextText`），
+         * 逼它再读一遍是白加一道，挡不住任何东西。
+         */
+        ...(requireObserved === true
+          ? { observed: workspace.observedOn(rootRef) }
+          : {}),
+        ...(sessionId === undefined ? {} : { sessionId }),
+        ...(runId === undefined ? {} : { runId }),
+      });
+  };
+
   const runtime: Runtime = {
     get identity() {
       return identity;
@@ -3376,6 +3503,7 @@ export async function createRuntime(
       assertReady(state);
       return surfaces;
     },
+    get typesafe() { assertReady(state); return typesafe; },
     get images() {
       assertReady(state);
       return images;
@@ -3488,82 +3616,7 @@ export async function createRuntime(
         callMcpTool,
       });
     },
-    createMutator({
-      rootRef,
-      mode,
-      requireObserved,
-      sessionId,
-      runId,
-      sandbox: sandboxCeiling,
-    }): WorkspaceMutator {
-      assertReady(state);
-      assertAssembled(assembled, "workspace", "Changing files in a project");
-      const root = workspace.get(rootRef);
-      if (root === undefined) {
-        throw createPrologueError(
-          "ROOT_NOT_FOUND",
-          "That authorized root is not available.",
-        );
-      }
-      const sandbox =
-        sandboxCeiling === undefined
-          ? root.posture
-          : narrowerSandbox(sandboxCeiling, root.posture);
-      // 档位来自模式 ∩ Runtime 姿态 ∩ 装配——三者求交，只会更窄。
-      const limits = resolveModeLimits({
-        mode: mode ?? "plan",
-        posture: { ...posture, sandbox },
-        assembled,
-      });
-      return createWorkspaceMutator({
-        host: input.host,
-        root,
-        effects,
-        limits,
-        canWrite: (path) => workspace.canWrite(rootRef, path),
-        newId: () => {
-          nextId += 1;
-          return `${nextId.toString(36)}-${random.nextBelow(0xffffff).toString(36)}`;
-        },
-        reviewMaxBytes: runtimeConfig.resource.publish.maxBytes,
-        publishReview: async (review: PatchReview | CommandReview | RewindReview) => {
-          const bytes = new TextEncoder().encode(JSON.stringify(review));
-          const stage = resources.stage({
-            mediaKind: "json",
-            byteLength: bytes.byteLength,
-            label:
-              review.kind === "workspace-patch"
-                ? `workspace-patch:${review.path}`
-                : review.kind === "workspace-command"
-                  ? `workspace-command:${review.executable}`
-                  : `workspace-rewind:${review.checkpointId}`,
-          });
-          try {
-            const chunk = Math.max(1, runtimeConfig.resource.publish.maxChunkBytes);
-            for (let offset = 0; offset < bytes.byteLength; offset += chunk) {
-              stage.write(bytes.subarray(offset, Math.min(offset + chunk, bytes.byteLength)));
-            }
-            return (await stage.publishDurable()).ref;
-          } catch (error) {
-            stage.discard();
-            throw error;
-          }
-        },
-        /*
-         * **只在模型那条路上要「先读后写」。**
-         *
-         * 这条规矩挡的是「凭记忆改文件」——而记忆是模型才有的问题：
-         * 它可能记的是上一次会话、另一个分支、或者干脆是它编的。App
-         * 自己调这个入口时手上就有内容（不然它拼不出 `nextText`），
-         * 逼它再读一遍是白加一道，挡不住任何东西。
-         */
-        ...(requireObserved === true
-          ? { observed: workspace.observedOn(rootRef) }
-          : {}),
-        ...(sessionId === undefined ? {} : { sessionId }),
-        ...(runId === undefined ? {} : { runId }),
-      });
-    },
+    createMutator(request): WorkspaceMutator { return makeMutator(request); },
     async connectMcp({ ref, transport, signal }): Promise<McpSnapshot> {
       assertReady(state);
       if (mcpShutdown.signal.aborted) {
@@ -3846,9 +3899,28 @@ export async function createRuntime(
      * 只剩「谁把什么接给了它」。
      */
     sessions: sessionSurface({
-      recovery: sessionRecovery({ store, effects, assertReady: () => assertReady(state),
-        liveSession: id => startingSessions.has(id) || [...liveRuns.values()].some(run => run.session === id),
-        pending: () => runtime.restorePendings(), readClock: () => input.host.readClock(), recovering: recoveringSessions }),
+      recovery: (() => {
+        const recovery = sessionRecovery({ store, effects, assertReady: () => assertReady(state),
+          liveSession: id => startingSessions.has(id) || [...liveRuns.values()].some(run => run.session === id),
+          pending: () => runtime.restorePendings(), readClock: () => input.host.readClock(), recovering: recoveringSessions });
+        return {
+          ...recovery,
+          // A closed run may be a subtask's: its parent learns the outcome now, not at the next restart.
+          async close(session: ExactRef<"session">, run: ExactRef<"run">, expectedVersion: number) {
+            const report = await recovery.close(session, run, expectedVersion);
+            await refreshClosedSubagentRun({
+              rows: (await subagentStore.list({ kind: "global" })).map((row) => row.value),
+              restore: (entry) => subagentRegistry.restore(entry),
+              openSession: (ref) => openSession(sessionDeps(), ref),
+              maxSummaryChars: runtimeConfig.orchestration.maxReportSummaryChars,
+              sessionId: session.id,
+              runId: run.id,
+            });
+            subagentRegistry.refreshCurrentProjection();
+            return report;
+          },
+        };
+      })(),
 
       assertReady: () => assertReady(state),
       report,
@@ -3893,6 +3965,8 @@ export async function createRuntime(
     async startAgentRun({
       session,
       rootRef,
+      workspace: workspaceMode,
+      grants: requestedGrants,
       start,
       agent,
       runTool,
@@ -3903,6 +3977,24 @@ export async function createRuntime(
     }) {
       assertReady(state);
       assertAssembled(assembled, "agent", "Running an Agent");
+      const noWorkspace = workspaceMode === "none";
+      const grants = freezeAgentGrants(requestedGrants);
+      if (rootRef !== undefined) rootRef = Object.freeze({ ...rootRef });
+      if (workspaceMode !== undefined && workspaceMode !== "required" && !noWorkspace) {
+        throw createPrologueError("AGENT_START_INVALID", "Unknown Agent workspace mode.");
+      }
+      if (noWorkspace) {
+        if (grants !== undefined || rootRef !== undefined || start.rootRef !== undefined || runTool !== undefined ||
+            subagentOptions !== undefined || agent.toolNames === undefined || agent.toolNames.length !== 0 ||
+            agent.workingDirectory !== undefined || (agent.skillNames?.length ?? 0) > 0 ||
+            (agent.mcpConnections?.length ?? 0) > 0 || (agent.mount?.sources?.length ?? 0) > 0 ||
+            (agent.mount?.artifacts?.length ?? 0) > 0 ||
+            Object.keys(input_context ?? {}).some((key) => key !== "windowTokens" && key !== "compactAboveTokens")) {
+          throw createPrologueError("AGENT_START_INVALID", "A workspace-free Agent requires explicit toolNames: [] and cannot carry a root, tools, subagents, or workspace context.");
+        }
+      } else if (rootRef === undefined || workspace.get(rootRef) === undefined) {
+        throw createPrologueError("ROOT_NOT_FOUND", "An Agent needs an available authorized workspace root.");
+      }
       if (recoveringSessions.has(session.ref.id)) throw createPrologueError("EFFECT_RECONCILE_REQUIRED", "This session is being recovered; start a new run only after recovery finishes.");
       if (history !== undefined && history !== "session") {
         throw createPrologueError(
@@ -3937,7 +4029,7 @@ export async function createRuntime(
       assertSlotReady(assembly, "agent-loop", "Running an Agent");
       assertSlotReady(assembly, "context-pack", "Running an Agent");
       assertSlotReady(assembly, "model-protocol", "Running an Agent");
-      assertSlotReady(assembly, "tool-executor", "Running an Agent");
+      if (!noWorkspace) assertSlotReady(assembly, "tool-executor", "Running an Agent");
       /*
        * 显式摘要器可以填补未装配的默认槽，不能绕过明确关掉的槽。
        */
@@ -3969,6 +4061,9 @@ export async function createRuntime(
        * 改那张表碰不到已经开跑的这一个。
        */
       const installed = freezeSessionPackBinding(sessionPacks.get(session.ref.id));
+      if (noWorkspace && (installed?.pack.tools.length ?? 0) > 0) {
+        throw createPrologueError("AGENT_START_INVALID", "A workspace-free Agent cannot use a Session pack that contributes tools.");
+      }
       /*
        * 包的配置作为**一层**进来，在 App 显式之下、模块默认之上。
        *
@@ -4045,18 +4140,24 @@ export async function createRuntime(
        * 必须在第一次 catalog 之前同步取完——中间若夹了 await，目录
        * 和定义就可能来自两份不同的表。
        */
+      if (noWorkspace && ((profile?.input.tools?.length ?? 0) > 0 ||
+          (profile?.input.skills?.length ?? 0) > 0 || (profile?.input.mcp?.length ?? 0) > 0)) {
+        throw createPrologueError("AGENT_START_INVALID", "A workspace-free Character cannot bind tools, skills, or MCP.");
+      }
       const skillSnapshot = skills.snapshot();
       const frozenWorkspaces =
-        subagentOptions === undefined
+        subagentOptions === undefined || rootRef === undefined
           ? undefined
           : freezeSubagentWorkspaces({
               workspaces: subagentOptions.workspaces ?? [],
               requireWorkspace: subagentOptions.requireWorkspace === true,
+              parentReads: subagentOptions.parentReads === true,
               parentRootRef: rootRef,
               lookup: (ref) => workspace.get(ref),
             });
-      const resolved = freezeStart({
-        start: agent,
+      const startSnapshot = freezeStart({
+        start: noWorkspace ? { ...agent, skillNames: [], mcpConnections: [] } : agent,
+        ...(noWorkspace ? { workspace: "none" as const } : {}),
         posture,
         assembled,
         defaultMaxTurns: config.agent.maxTurns,
@@ -4101,6 +4202,13 @@ export async function createRuntime(
           : { subagentWorkspaces: subagentWorkspaceFact(frozenWorkspaces) }),
       });
 
+      if (grants !== undefined && grants.toolNames.some((name) => !isToolAllowed(startSnapshot, name) ||
+          ![...available, ...tools.list().filter((entry) => visibleHere(entry.name)).map((entry) => entry.name)].includes(name))) {
+        throw createPrologueError("AGENT_START_INVALID", "Run grants may name only tools available to this Run.");
+      }
+      const resolved = grants === undefined ? startSnapshot : Object.freeze({ ...startSnapshot,
+        fingerprint: fingerprintText(JSON.stringify([startSnapshot.fingerprint, session.ref, rootRef, grants])),
+      });
       // freezeStart 已在第一次 await 前复制并冻结引用；历史读取失败仍不消耗幂等键。
       const historyMessages =
         history === "session" ? historyFromTurns((await session.conversation()).turns) : undefined;
@@ -4157,14 +4265,18 @@ export async function createRuntime(
        * 被记成父深度 + 1。并发名额仍是 Runtime 那一份。
        */
       const lineageDepth = subagentLineage.get(session.ref.id) ?? 0;
-      const mutator = runtime.createMutator({
+      const scoped = grants === undefined || rootRef === undefined ? undefined : bindAgentEffects({
+        effects, grants, session: session.ref, root: Object.freeze({ ...rootRef }), runId: () => currentRunId, scopes: effectScopes,
+      });
+      const runEffects = scoped?.effects ?? effects;
+      const mutator = rootRef === undefined ? undefined : makeMutator({
         rootRef,
         mode: frozen.limits.mode,
         requireObserved: true,
         sessionId: session.ref.id,
         runId: () => currentRunId,
-        ...(lineageDepth > 0 ? { sandbox: frozen.sandbox } : {}),
-      });
+        ...(lineageDepth > 0 || grants !== undefined ? { sandbox: frozen.sandbox } : {}),
+      }, runEffects);
 
       /**
        * 二点五、Character。
@@ -4191,6 +4303,8 @@ export async function createRuntime(
           .filter((one) => isToolAllowed(frozen, one.name))
           .map((one) => one.name),
       ];
+      // Granted past the character, never past this run's ceiling.
+      const grantedTools = (agent.grantedTools ?? []).filter((name) => ceilingTools.includes(name));
       /**
        * 这一次由哪个角色干活。
        *
@@ -4257,7 +4371,7 @@ export async function createRuntime(
           systemTools,
           catalogTools: visibleCatalog(),
           frozen,
-          character,
+          character: grantedTools.length < 1 ? character : { tools: [...character.tools, ...grantedTools] },
         });
         return {
           all,
@@ -4360,7 +4474,7 @@ export async function createRuntime(
        * 还要再过一次本轮名单：开跑时点了 `skillNames`，目录里也只能
        * 有那几条，否则模型看得见却调不动。
        */
-      const visibleSkills = config.skill.autoSelect
+      const visibleSkills = !noWorkspace && config.skill.autoSelect
         ? skillSnapshot
             .catalog({ capabilities: available, forAutoSelection: true })
             .entries.filter(
@@ -4394,8 +4508,12 @@ export async function createRuntime(
        */
       const packFor = async (who: FrozenCharacter) => {
         const assembled = await packContext({
-          reader: workspace.reader(rootRef, workspace.ignoreFor(rootRef)),
-          rootIgnoreLines: workspace.get(rootRef)?.ignoreLines ?? [],
+          ...(rootRef === undefined
+            ? { workspace: "none" as const }
+            : { reader: workspace.reader(rootRef, workspace.ignoreFor(rootRef)), workingDirectory: joinInsideRoot(
+                workspace.get(rootRef)?.workingDirectory ?? ".", frozen.workingDirectory,
+              ) }),
+          rootIgnoreLines: rootRef === undefined ? [] : workspace.get(rootRef)?.ignoreLines ?? [],
           persona: await gatherPersona({
             character: who,
             readChunk: (ref, offset) => resources.readChunk(ref, offset),
@@ -4408,10 +4526,7 @@ export async function createRuntime(
           // 改的，读口按它解析）和这一次运行自己给的那一段。分层拼接要
           // 跟着**实际生效的那个**——只看其中一个的话，模型看到的规矩会
           // 和它实际能读到的文件对不上。
-          workingDirectory: joinInsideRoot(
-            workspace.get(rootRef)?.workingDirectory ?? ".",
-            frozen.workingDirectory,
-          ),
+
           // 钩子改写过的那一份。没改就是原来那一份。
           // 显式续聊只把本轮新用户任务送进 `messages`；历史走 conversation，
           // 按原角色和数据来源入模，不经过 source:user 那条扁平路径，
@@ -4553,6 +4668,7 @@ export async function createRuntime(
               // 隐形档案，凭空写个名字等于替 App 认了一个角色。
               ...(isDefaultExecutor(character) ? {} : { character: character.ref.id }),
             },
+        runEffects,
       );
       /*
        * 报过的不再报。**按这一次 Run 记**——一次新的 Run 该重新看见
@@ -4591,8 +4707,9 @@ export async function createRuntime(
         }
         return undefined;
       };
-      const dispatchChild = () =>
-        subagentDispatcher({
+      const dispatchChild = () => {
+        if (rootRef === undefined) throw createPrologueError("TOOL_UNAVAILABLE", "A workspace-free Agent cannot dispatch tools or subagents.");
+        return subagentDispatcher({
           subagents: runSubagents,
           registry: subagentRegistry,
           parentRun: () => {
@@ -4658,7 +4775,14 @@ export async function createRuntime(
           ...(heldCompactor === undefined ? {} : { compactor: heldCompactor }),
           compactAboveTokens,
           windowTokens,
+          boards: {
+            store: boards,
+            limits: config.orchestration.board,
+            now: async () => (await runtime.readClock()).wallTimeMs,
+          },
+          parentCharacter: () => (isDefaultExecutor(character) ? undefined : character.ref),
         });
+      };
       /**
        * 内置那份：系统工具闭集里那些，各归各的主人。
        *
@@ -4667,7 +4791,9 @@ export async function createRuntime(
        * 没法被引用。
        */
       const runSignal: { current: AbortSignal | undefined } = { current: undefined };
-      const builtinRunner = createSystemToolRunner({
+      const builtinRunner: ToolRunner = rootRef === undefined || mutator === undefined
+        ? async () => { throw createPrologueError("TOOL_UNAVAILABLE", "A workspace-free Agent has no tools."); }
+        : createSystemToolRunner({
           ...sharedSystemToolDeps({
             rootRef,
             ignore: assembledContext.ignore,
@@ -4677,6 +4803,16 @@ export async function createRuntime(
              * 就是没有人——凭空写个名字等于替 App 认了一个角色。
              */
             by: () => (isDefaultExecutor(character) ? undefined : character.ref),
+            session: () => session.ref,
+            // A subagent reads by the reference the model got when dispatching it; a subagent's dispatcher, as such.
+            name: (assignee) => {
+              if (assignee.kind !== "session") return undefined;
+              const all = subagentRegistry.list();
+              const child = all.find((one) => one.session.id === assignee.id);
+              if (child !== undefined) return `subagent ${child.ref.id}`;
+              const self = all.find((one) => one.session.id === session.ref.id);
+              return self !== undefined && self.parentSession.id === assignee.id ? "the session that dispatched you" : undefined;
+            },
             // 板的上界用**这一次**那份：包配得动它。
             boardLimits: config.orchestration.board,
             mcpConnections: frozen.mcpConnections,
@@ -4685,6 +4821,10 @@ export async function createRuntime(
           limits: frozen.limits,
           todo,
           evidence,
+          // Read-only views of the children's workspaces, when the parent was granted them.
+          ...(frozenWorkspaces?.parentReads
+            ? { readRoots: frozenWorkspaces.workspaces.map(({ id, rootRef }) => ({ id, rootRef })) }
+            : {}),
           async onCommandReceipt(callId, record) {
             const bytes = new TextEncoder().encode(JSON.stringify(record));
             const stage = resources.stage({ mediaKind: "json", byteLength: bytes.byteLength, label: "command-receipt" });
@@ -5011,9 +5151,10 @@ export async function createRuntime(
        * 闭集里那三样有主人的名字轮不到它们答。内置提问者同样：丢掉
        * `asksUser` 会把普通提问重新变成一笔虚构 Effect。
        */
-      const handsOff = runTool ?? runtimeToolRunner ?? builtinRunner;
+      const handsOff = noWorkspace ? builtinRunner : runTool ?? runtimeToolRunner ?? builtinRunner;
 
       const handOff: ToolRunner = async (callInput) => {
+        if (noWorkspace) return builtinRunner(callInput);
         if (runTool !== undefined) {
           return runTool(callInput);
         }
@@ -5051,7 +5192,7 @@ export async function createRuntime(
 
       const packed = {
         ...start,
-        rootRef,
+        ...(rootRef === undefined ? {} : { rootRef }),
         // 未选续聊时仍把整包装进账，保持原来的独立起跑记账。
         // 显式续聊只记本轮新用户任务——历史已经在上一本账里，再记就成两份。
         messages:
@@ -5107,7 +5248,7 @@ export async function createRuntime(
           if (!runAbort.signal.aborted) {
             runAbort.abort();
           }
-          void mutator.abortForeground();
+          void mutator?.abortForeground();
         });
         /*
          * 这一轮交给谁跑。**选择在这里冻住**，之后这一轮就认它。
@@ -5214,6 +5355,7 @@ export async function createRuntime(
                 });
               },
               skills: () => {
+                if (noWorkspace) return [];
                 const seen = new Set(remindedSkills);
                 const fresh = skillSnapshot
                   .catalog({ capabilities: [] })
@@ -5316,6 +5458,7 @@ export async function createRuntime(
        */
       run.subscribe((event) => {
         if (isTerminalEventType(event.type)) {
+          scoped?.revoke();
           liveRuns.delete(run.ref.id);
           todo.finish();
           /*
@@ -5347,7 +5490,7 @@ export async function createRuntime(
            * 最常见的一种就是 Runtime 已经关了——那时 Host 那边本来
            * 就把所有子进程一起收了。
            */
-          void mutator.reapBackground().catch(() => undefined);
+          void mutator?.reapBackground().catch(() => undefined);
         }
       });
       /*
@@ -5377,6 +5520,7 @@ export async function createRuntime(
     },
 
     async shutdown(): Promise<ShutdownReceipt> {
+      await modelResponses.close();
       await effects.pendings.endLiveWaits();
       /*
        * **心跳第一个停。**
diff --git a/packages/sdk/src/composition/core/subagent-recovery.ts b/packages/sdk/src/composition/core/subagent-recovery.ts
index 3651f600..c6f2d085 100644
--- a/packages/sdk/src/composition/core/subagent-recovery.ts
+++ b/packages/sdk/src/composition/core/subagent-recovery.ts
@@ -177,6 +177,28 @@ async function inspectRow(input: {
   return { handle: handleFrom(row, terminal), report };
 }
 
+/**
+ * A subtask whose interrupted run was just closed by recovery: read its outcome again.
+ *
+ * At startup such a child is `reconcile-required` because its run had no terminal event. Closing the run writes
+ * one, but the registry only reads rows at startup — so without this the parent session stays blocked ("an
+ * unsettled side effect") until the next restart, even though the person already reconciled it.
+ */
+export async function refreshClosedSubagentRun(input: {
+  readonly rows: readonly StoredSubagent[];
+  readonly restore: SubagentRegistry["restore"];
+  readonly openSession: (ref: ExactRef<"session">) => Promise<Session | undefined>;
+  readonly maxSummaryChars: number;
+  readonly sessionId: string;
+  readonly runId: string;
+}): Promise<void> {
+  for (const row of input.rows) {
+    if (row.sessionId !== input.sessionId || row.runId !== input.runId) continue;
+    const inspected = await inspectRow({ row, openSession: input.openSession, unfinishedRunIds: new Set(), maxSummaryChars: input.maxSummaryChars });
+    input.restore({ handle: inspected.handle, ...(inspected.report === undefined ? {} : { report: inspected.report }), live: false });
+  }
+}
+
 export async function recoverSubagents(input: {
   readonly rows: readonly StoredSubagent[];
   readonly restore: SubagentRegistry["restore"];
diff --git a/packages/sdk/src/composition/core/subagent-workspaces.ts b/packages/sdk/src/composition/core/subagent-workspaces.ts
index 5784a7d7..1a8bee4d 100644
--- a/packages/sdk/src/composition/core/subagent-workspaces.ts
+++ b/packages/sdk/src/composition/core/subagent-workspaces.ts
@@ -40,6 +40,11 @@ export interface SubagentWorkspaceGrant {
 export interface SubagentRunOptions {
   readonly workspaces?: readonly SubagentWorkspaceGrant[];
   readonly requireWorkspace?: boolean;
+  /**
+   * The parent may read, list and search these workspaces by id — never write or run commands in them — to check
+   * what its children did. Off by default: a parent sees only its own root.
+   */
+  readonly parentReads?: boolean;
   readonly onStarted?: SubagentStartedObserver;
 }
 
@@ -57,6 +62,7 @@ export interface FrozenSubagentGrant {
 
 export interface FrozenSubagentWorkspaces {
   readonly requireWorkspace: boolean;
+  readonly parentReads: boolean;
   readonly workspaces: readonly FrozenSubagentGrant[];
 }
 
@@ -80,6 +86,8 @@ export type SubagentStartedObserver = (input: {
 /** 参与已有 Agent 开始指纹的那一份不可变事实。不含回调。 */
 export interface SubagentWorkspaceFact {
   readonly requireWorkspace: boolean;
+  /** Present only when granted, so runs started without it keep their fingerprint. */
+  readonly parentReads?: true;
   readonly workspaces: readonly {
     readonly id: string;
     readonly rootId: string;
@@ -134,6 +142,7 @@ export function subagentWorkspaceFact(
 ): SubagentWorkspaceFact {
   return Object.freeze({
     requireWorkspace: frozen.requireWorkspace,
+    ...(frozen.parentReads ? { parentReads: true as const } : {}),
     workspaces: Object.freeze(
       frozen.workspaces
         .map((one) =>
@@ -153,6 +162,7 @@ export function subagentWorkspaceFact(
 export function freezeSubagentWorkspaces(input: {
   readonly workspaces: readonly SubagentWorkspaceGrant[];
   readonly requireWorkspace: boolean;
+  readonly parentReads?: boolean;
   readonly parentRootRef: ExactRef<"authorized-root">;
   readonly lookup: (ref: ExactRef<"authorized-root">) => AuthorizedRoot | undefined;
 }): FrozenSubagentWorkspaces {
@@ -207,6 +217,7 @@ export function freezeSubagentWorkspaces(input: {
 
   return Object.freeze({
     requireWorkspace: input.requireWorkspace,
+    parentReads: input.parentReads === true,
     workspaces: Object.freeze(copied),
   });
 }
diff --git a/packages/sdk/src/composition/core/surfaces.ts b/packages/sdk/src/composition/core/surfaces.ts
index 6b13b77a..43860a71 100644
--- a/packages/sdk/src/composition/core/surfaces.ts
+++ b/packages/sdk/src/composition/core/surfaces.ts
@@ -12,17 +12,20 @@
 
 import { freezeStart, isToolAllowed } from "../../agent/core/index.js";
 import { createDeliveryBox } from "../../delivery/core/index.js";
-import { createPrologueError, mintRef, type ExactRef } from "../../kernel/core/index.js";
+import { createPrologueError, isPrologueError, mintRef, type ExactRef } from "../../kernel/core/index.js";
 import {
   assertNoImplicitInheritance,
   assertReportIsBounded,
   createSubagentTracker,
+  describeAssignee,
   intersectPermissions,
+  type BoardAssignee,
   type BoardLimits,
   type SubagentHandle,
   type SubagentRegistry,
   type SubagentReport,
   type TaskBoardStore,
+  type TaskNode,
 } from "../../orchestration/core/index.js";
 import type { CharacterRegistry } from "../../character/core/index.js";
 import type { ModelEvent } from "../../model/core/index.js";
@@ -228,6 +231,13 @@ export function taskBoardSurface(deps: {
    * 记的是新角色。没绑档案就是没有人，不编一个。
    */
   readonly by: () => ExactRef<"character"> | undefined;
+  /**
+   * The session this run works in. A report made here counts for tasks assigned to that session, and the board
+   * reads such tasks back as "you". Not given (outside a run): no session, as with `by`.
+   */
+  readonly session?: () => ExactRef<"session"> | undefined;
+  /** How other sessions read to this run (a subagent it dispatched, say). Not given: their kind and id. */
+  readonly name?: (assignee: BoardAssignee) => string | undefined;
   readonly now: () => Promise<number>;
 }): TaskBoardSurface {
   const { boards, characters, limits, by } = deps;
@@ -239,8 +249,8 @@ export function taskBoardSurface(deps: {
    * 到派活那一刻才发现没人接，而那时前面几步已经跑完了。认不出来
    * `resolve` 自己会抛 `CHARACTER_NOT_FOUND`。
    */
-  const known = (ref: ExactRef<"character"> | undefined): void => {
-    if (ref !== undefined) {
+  const known = (ref: BoardAssignee | undefined): void => {
+    if (ref?.kind === "character") {
       characters.resolve(ref);
     }
   };
@@ -299,6 +309,10 @@ export function taskBoardSurface(deps: {
         nodeId: input.node,
         dependsOn: input.dependsOn,
       }),
+    name: (assignee) => {
+      if (assignee.kind === "session" && assignee.id === deps.session?.()?.id) return "you (this session)";
+      return deps.name?.(assignee) ?? (assignee.kind === "character" ? assignee.id : describeAssignee(assignee));
+    },
     reassign: async (input) => {
       known(input.assignee);
       return boards.reassign({
@@ -316,6 +330,10 @@ export function taskBoardSurface(deps: {
         // **谁报的由这一层填**，不收模型给的名字：那样任何一跑都能
         // 冒充任何角色，而「只报得了自己名下的」就是一句空话。
         by: by(),
+        ...((): { session?: ExactRef<"session"> } => {
+          const session = deps.session?.();
+          return session === undefined ? {} : { session };
+        })(),
         note: input.note,
         atMs: await deps.now(),
         ...(input.state === undefined ? {} : { to: input.state }),
@@ -324,6 +342,24 @@ export function taskBoardSurface(deps: {
   };
 }
 
+/** The board tools a subagent holding steps is always shown. */
+const BOARD_TOOLS = ["board-read", "board-report"] as const;
+
+function finishedStep(state: TaskNode["state"]): boolean {
+  return state === "succeeded" || state === "failed" || state === "cancelled";
+}
+
+/** What a subagent is told about the steps it holds, after its instruction. */
+function heldStepsNote(board: string, nodes: readonly TaskNode[]): string {
+  return [
+    `You hold these steps on task board ${board}:`,
+    ...nodes.map((node) => `- ${node.id}: ${node.title}`),
+    "They are yours while you run: only you report on them. Read them with board-read and report progress with " +
+      "board-report, ending each as succeeded or failed. Whatever you leave unfinished goes back to the one who " +
+      "dispatched you when you end.",
+  ].join("\n");
+}
+
 /**
  * 派一个子任务。
  *
@@ -394,6 +430,14 @@ export function subagentDispatcher(deps: {
   readonly compactAboveTokens: number;
   /** 父亲这一跑冻住的窗口。 */
   readonly windowTokens: number;
+  /** The task boards, when this Runtime has them: a dispatch naming `claims` hands those steps to the child. */
+  readonly boards?: {
+    readonly store: TaskBoardStore;
+    readonly limits: BoardLimits;
+    readonly now: () => Promise<number>;
+  };
+  /** The parent's character, when it has one: steps assigned to it are the parent's to hand over too. */
+  readonly parentCharacter?: () => ExactRef<"character"> | undefined;
 }): NonNullable<
   Parameters<typeof createSystemToolRunner>[0]["dispatchSubagent"]
 > {
@@ -408,6 +452,51 @@ export function subagentDispatcher(deps: {
     session,
     start,
   } = deps;
+  /** Whether a step is the parent's to hand over: its session's, its character's, or no one's yet. */
+  const parentHolds = (assignee: BoardAssignee | undefined): boolean =>
+    assignee === undefined ||
+    (assignee.kind === "session" && assignee.id === session.ref.id) ||
+    (assignee.kind === "character" && assignee.id === deps.parentCharacter?.()?.id);
+  /**
+   * Pass steps to another assignee, one at a time, each against the board as it is now: another run may be writing
+   * the same board, so a version conflict is read again and retried. A step `mayPass` refuses is left alone.
+   */
+  const passSteps = async (input: {
+    readonly boards: NonNullable<typeof deps.boards>;
+    readonly ref: ExactRef<"task-board">;
+    readonly ids: readonly string[];
+    readonly to: (from: BoardAssignee | undefined) => BoardAssignee | undefined;
+    readonly note: string;
+    readonly mayPass: (node: TaskNode) => boolean;
+    readonly passed?: (id: string, from: BoardAssignee | undefined) => void;
+  }): Promise<void> => {
+    for (const id of input.ids) {
+      for (let attempt = 0; ; attempt += 1) {
+        const node = input.boards.store.get(input.ref).nodes.find((one) => one.id === id);
+        if (node === undefined || !input.mayPass(node)) break;
+        const atMs = await input.boards.now();
+        const version = input.boards.store.get(input.ref).version;
+        try {
+          await input.boards.store.handOver({
+            ref: input.ref,
+            expectedVersion: version,
+            nodeId: id,
+            from: node.assignee,
+            to: input.to(node.assignee),
+            note: input.note,
+            atMs,
+            limits: input.boards.limits,
+          });
+          input.passed?.(id, node.assignee);
+          break;
+        } catch (error) {
+          const conflict = isPrologueError(error) &&
+            (error.code === "TASKBOARD_CONFLICT" || error.code === "TASKBOARD_NOT_ASSIGNED");
+          if (!conflict || attempt >= 4) throw error;
+        }
+      }
+    }
+  };
   return async (request, deferResult) => {
     assertNoImplicitInheritance(request);
     const childKey = request.idempotencyKey;
@@ -442,13 +531,70 @@ export function subagentDispatcher(deps: {
       deps.assertParentMayDispatch?.();
       subagents.assertCanDispatch();
     }
+    /*
+     * Steps the child takes on. Checked in full before anything is created: a step that is finished, missing, or
+     * someone else's fails the dispatch, and no child exists. A repeated dispatch took them the first time.
+     */
+    const claimed = existing || request.claims === undefined ? undefined : (() => {
+      const boards = deps.boards;
+      if (boards === undefined) {
+        throw createPrologueError("TOOL_UNAVAILABLE", "This Runtime has no task boards; a subagent cannot take on steps.");
+      }
+      const ref = mintRef("task-board", request.claims.board);
+      const held = boards.store.get(ref);
+      const nodes = request.claims.nodes.map((id) => {
+        const node = held.nodes.find((one) => one.id === id);
+        if (node === undefined) {
+          throw createPrologueError("TASKBOARD_NOT_FOUND", `This board has no node "${id}".`);
+        }
+        if (finishedStep(node.state)) {
+          throw createPrologueError("TASKBOARD_CONFLICT", `"${id}" is already ${node.state}; a subagent cannot take it on.`);
+        }
+        if (!parentHolds(node.assignee)) {
+          throw createPrologueError(
+            "TASKBOARD_NOT_ASSIGNED",
+            `"${id}" belongs to ${describeAssignee(node.assignee)}; only steps that are yours or no one's can be handed to a subagent.`,
+          );
+        }
+        return node;
+      });
+      return { boards, ref, nodes };
+    })();
+    /*
+     * A named role brings the tools it declares, as far as this parent itself holds them. A parent that leaves one
+     * out of its list (a read-only "list", say) no longer makes the whole dispatch fail on the role's ceiling, and the
+     * child still never gets a tool its parent does not have.
+     */
+    const parentTools = systemTools
+      .filter((tool) => tool.available && isToolAllowed(frozen, tool.name))
+      .map((tool) => tool.name);
+    if (claimed !== undefined) {
+      // A child holding steps reads and reports on them, whatever else it was given.
+      const missing = BOARD_TOOLS.filter((name) => !(parentTools as readonly string[]).includes(name));
+      if (missing.length > 0) {
+        throw createPrologueError(
+          "TOOL_ARGUMENTS_INVALID",
+          `Handing steps to a subagent needs ${missing.join(" and ")} on this run, so the subagent can report on them.`,
+        );
+      }
+      request = { ...request, tools: [...new Set([...request.tools, ...BOARD_TOOLS])] as typeof request.tools };
+    }
+    const roleTools = (() => {
+      if (request.characterRef === undefined) return [];
+      try {
+        return runtime.characters.resolve(request.characterRef).input.tools ?? [];
+      } catch {
+        return [];
+      }
+    })().filter((name) => (parentTools as readonly string[]).includes(name));
+    if (roleTools.some((name) => !(request.tools as readonly string[]).includes(name))) {
+      request = { ...request, tools: [...new Set([...request.tools, ...roleTools])] as typeof request.tools };
+    }
     const effective = intersectPermissions({
       parent: {
         // 父亲的有效名单 = 本 Run **真正开着**的那些：装配了、
         // 而且没被开始输入收窄掉。不是闭集全体。
-        tools: systemTools
-          .filter((tool) => tool.available && isToolAllowed(frozen, tool.name))
-          .map((tool) => tool.name),
+        tools: parentTools,
         network: {
           model: frozen.network.model === true,
           web: frozen.network.web === true,
@@ -533,6 +679,44 @@ export function subagentDispatcher(deps: {
         if (deps.durable || deps.onStarted !== undefined) {
           deps.holdChildStart?.(child.ref.id);
         }
+        /*
+         * The steps pass to the child's session before its first turn, so from its first report on they are its
+         * own. Should one have changed hands in between, the ones already passed go back and nothing starts.
+         */
+        if (claimed !== undefined) {
+          const taken: Array<{ readonly id: string; readonly from: BoardAssignee | undefined }> = [];
+          try {
+            await passSteps({
+              boards: claimed.boards,
+              ref: claimed.ref,
+              ids: claimed.nodes.map((node) => node.id),
+              to: () => child.ref,
+              note: `taken on by subagent "${request.idempotencyKey}"`,
+              mayPass: (node) => {
+                if (finishedStep(node.state) || !parentHolds(node.assignee)) {
+                  throw createPrologueError(
+                    "TASKBOARD_NOT_ASSIGNED",
+                    `"${node.id}" changed hands (${node.state}, ${describeAssignee(node.assignee)}) before the subagent started; nothing was dispatched.`,
+                  );
+                }
+                return true;
+              },
+              passed: (id, from) => taken.push({ id, from }),
+            });
+          } catch (error) {
+            for (const one of taken) {
+              await passSteps({
+                boards: claimed.boards,
+                ref: claimed.ref,
+                ids: [one.id],
+                to: () => one.from,
+                note: `subagent "${request.idempotencyKey}" was not dispatched`,
+                mayPass: (node) => !finishedStep(node.state) && node.assignee?.kind === "session" && node.assignee.id === child.ref.id,
+              }).catch(() => undefined);
+            }
+            throw error;
+          }
+        }
         /**
          * 子循环。**同一个 `startAgentRun`**——同一套 Session / Run /
          * Effect / 事件账。它是调度，不是第二套执行核。
@@ -548,7 +732,12 @@ export function subagentDispatcher(deps: {
           rootRef: childRootRef,
           start: {
             ...start,
-            messages: [{ role: "user", text: request.instruction }],
+            messages: [{
+              role: "user",
+              text: claimed === undefined
+                ? request.instruction
+                : `${request.instruction}\n\n${heldStepsNote(claimed.ref.id, claimed.nodes)}`,
+            }],
             /*
              * **子任务有自己的预算。** 不给的话它花的是父亲那一
              * 本，于是「一个跑飞的孩子」和「父亲自己跑飞」在账上
@@ -574,6 +763,7 @@ export function subagentDispatcher(deps: {
             idempotencyKey: `${frozen.idempotencyKey}.${request.idempotencyKey}`,
             mode: frozen.limits.mode,
             toolNames: effective.tools,
+            ...(claimed === undefined ? {} : { grantedTools: BOARD_TOOLS }),
             /*
              * 点名了角色就让它按那份档案干活：角色说明进**它自己**的
              * 提示，工具在下面这份求交结果之内再收一次。
@@ -633,8 +823,7 @@ export function subagentDispatcher(deps: {
             deps.notifyParentUncertain?.();
             deps.registry.notifyUncertainAncestors(session.ref.id);
           }
-          settleReport(
-            assertReportIsBounded(
+          const report = assertReportIsBounded(
               {
                 sessionRef: child.ref,
                 state,
@@ -650,8 +839,23 @@ export function subagentDispatcher(deps: {
                 ...(error === undefined ? {} : { error }),
               },
               config.orchestration.maxReportSummaryChars,
-            ),
-          );
+            );
+          if (claimed === undefined) {
+            settleReport(report);
+            return;
+          }
+          /*
+           * What the child leaves unfinished comes back before the parent hears it ended, so the parent reads its
+           * steps back as its own. Steps the child finished stay as it reported them.
+           */
+          void passSteps({
+            boards: claimed.boards,
+            ref: claimed.ref,
+            ids: claimed.nodes.map((node) => node.id),
+            to: () => session.ref,
+            note: `subagent "${request.idempotencyKey}" ended (${state}) without finishing this step; it is back with the one that dispatched it`,
+            mayPass: (node) => !finishedStep(node.state) && node.assignee?.kind === "session" && node.assignee.id === child.ref.id,
+          }).catch(() => undefined).then(() => settleReport(report));
         });
 
         if (deps.parentCancelled?.() === true) {
diff --git a/packages/sdk/src/composition/core/system-tools.ts b/packages/sdk/src/composition/core/system-tools.ts
index 11201b1e..86541448 100644
--- a/packages/sdk/src/composition/core/system-tools.ts
+++ b/packages/sdk/src/composition/core/system-tools.ts
@@ -7,7 +7,7 @@ import {
   type ToolAbort,
   type ToolRunner,
 } from "../../tool/core/index.js";
-import type { Workspace, WorkspaceMutator } from "../../workspace/core/index.js";
+import type { Workspace, WorkspaceMutator, WorkspaceReader } from "../../workspace/core/index.js";
 import type {
   EffectGate,
   PendingLedger,
@@ -16,15 +16,18 @@ import type {
 } from "../../effect-policy/core/index.js";
 import type { SkillInvocationMode, SkillReceipt } from "../../skill/core/index.js";
 import { MCP_TOOL_PREFIX } from "../../mcp/core/index.js";
-import type {
-  FailurePolicy,
-  NodeProposal,
-  NodeState,
-  SubagentHandle,
-  SubagentReport,
-  SubagentRequest,
-  TaskBoard,
-  TaskNode,
+import {
+  describeAssignee,
+  type BoardAssignee,
+  type FailurePolicy,
+  type NodeProposal,
+  type NodeReport,
+  type NodeState,
+  type SubagentHandle,
+  type SubagentReport,
+  type SubagentRequest,
+  type TaskBoard,
+  type TaskNode,
 } from "../../orchestration/core/index.js";
 import type { IgnoreRules } from "../../context/core/index.js";
 import { isCommandAllowed, type ModeLimits, type TodoList, type TodoState } from "../../agent/core/index.js";
@@ -227,8 +230,12 @@ export interface TaskBoardSurface {
     readonly board: string;
     readonly version: number;
     readonly node: string;
-    readonly assignee: ExactRef<"character"> | undefined;
+    readonly assignee: BoardAssignee | undefined;
   }): Promise<TaskBoard>;
+  /**
+   * How an assignee reads to this run: "you (this session)", "subagent …". Not given: the bare kind and id.
+   */
+  name?(assignee: BoardAssignee): string;
   /** 报一条进展，可以顺带改状态。**只报得了自己名下的。** */
   report(input: {
     readonly board: string;
@@ -283,6 +290,20 @@ function nodeState(value: string, reporting: boolean): NodeState {
   );
 }
 
+/** The steps a subagent is asked to take on, as the model wrote them. Shape only; the board checks the rest. */
+function boardClaims(value: unknown): { readonly board: string; readonly nodes: readonly string[] } {
+  const held = (value ?? {}) as Record<string, unknown>;
+  const nodes = Array.isArray(held.nodes) ? held.nodes : [];
+  if (typeof held.board !== "string" || held.board.length < 1 || nodes.length < 1 ||
+      nodes.some((one) => typeof one !== "string" || one.length < 1) || new Set(nodes).size !== nodes.length) {
+    throw createPrologueError(
+      "TOOL_ARGUMENTS_INVALID",
+      '"claims" needs a board id and a list of distinct step ids on it.',
+    );
+  }
+  return { board: held.board, nodes: nodes as string[] };
+}
+
 /**
  * 一个节点讲给模型听。
  *
@@ -290,11 +311,30 @@ function nodeState(value: string, reporting: boolean): NodeState {
  * 把两百个节点的进展全念出来会把上下文撑满，而**读的一方自己决定要
  * 多少**：想看就点名读那一个。
  */
-function describeNode(node: TaskNode, detailed: boolean): string {
+function describeNode(
+  node: TaskNode,
+  detailed: boolean,
+  named?: (assignee: BoardAssignee) => string,
+): string {
+  // A role reads as its reference, as it always has; sessions and people say which they are.
+  const name = (assignee: BoardAssignee): string =>
+    named?.(assignee) ?? (assignee.kind === "character" ? assignee.id : describeAssignee(assignee));
+  const who = (report: NodeReport): string =>
+    report.handover !== undefined
+      ? `handed from ${report.handover.from === undefined ? "no one" : name(report.handover.from)} to ${
+          report.handover.to === undefined ? "no one" : name(report.handover.to)
+        }`
+      : report.human !== undefined
+        ? name(report.human)
+        : report.by !== undefined
+          ? report.by.id
+          : report.session !== undefined
+            ? name(report.session)
+            : "unattributed";
   const head = [
     `${node.id} [${node.state}]`,
     node.title,
-    node.assignee === undefined ? "unowned" : `owner ${node.assignee.id}`,
+    node.assignee === undefined ? "unowned" : `owner ${name(node.assignee)}`,
     node.dependsOn.length < 1 ? undefined : `waits on ${node.dependsOn.join(", ")}`,
   ]
     .filter((one): one is string => one !== undefined)
@@ -307,9 +347,7 @@ function describeNode(node: TaskNode, detailed: boolean): string {
   }
   return [
     head,
-    ...node.reports.map(
-      (report) => `  · ${report.by === undefined ? "unattributed" : report.by.id}: ${report.note}`,
-    ),
+    ...node.reports.map((report) => `  · ${who(report)}: ${report.note}`),
   ].join("\n");
 }
 
@@ -346,6 +384,11 @@ export function createSystemToolRunner(input: {
    * 那时没有「这一次声称做了什么」可核。
    */
   readonly evidence?: EvidenceLedger;
+  /**
+   * Other roots this run may read, list and search by id, never write. A parent gets its children's workspaces here
+   * when it was granted them; the model names one with the `workspace` argument.
+   */
+  readonly readRoots?: readonly { readonly id: string; readonly rootRef: ExactRef<"authorized-root"> }[];
   /** Persist actual command facts before yielding the tool result. */
   readonly onCommandReceipt?: (callId: string, record: import("../../workspace/core/index.js").WorkspaceCommandRecord) => Promise<void>;
   /**
@@ -599,6 +642,21 @@ export function createSystemToolRunner(input: {
 
   // 忽略规则跟着这一次的装配走——**读也要拦**，不只是列表过滤。
   const reader = input.workspace.reader(input.rootRef, input.ignore);
+  const readRoots = new Map((input.readRoots ?? []).map((one) => [one.id, input.workspace.reader(one.rootRef, input.ignore, { observe: false })]));
+  /** The reader a read-only tool call names: this run's own root, or one of the roots it may read. */
+  const readerFor = (args: Record<string, unknown>): WorkspaceReader => {
+    if (args.workspace === undefined) return reader;
+    const named = typeof args.workspace === "string" ? readRoots.get(args.workspace) : undefined;
+    if (named === undefined) {
+      throw createPrologueError(
+        "TOOL_ARGUMENTS_INVALID",
+        readRoots.size < 1
+          ? "This run reads only its own workspace; leave out \"workspace\"."
+          : `"workspace" must be one of ${[...readRoots.keys()].map((id) => `"${id}"`).join(", ")}, or left out for this run's own workspace.`,
+      );
+    }
+    return named;
+  };
 
   const text = (value: unknown, field: string): string => {
     if (typeof value !== "string" || value.length < 1) {
@@ -669,7 +727,7 @@ export function createSystemToolRunner(input: {
   }): Promise<string> => {
     switch (registration.name) {
       case "list": {
-        const listed = await reader.list({ path: typeof args.path === "string" ? args.path : "." });
+        const listed = await readerFor(args).list({ path: typeof args.path === "string" ? args.path : "." });
         // 被忽略的默认看不见。它们在磁盘上还在，只是不进默认集合。
         const visible = listed.entries.filter((entry) => input.ignore.isVisible(entry.path));
         return visible.length < 1
@@ -677,14 +735,19 @@ export function createSystemToolRunner(input: {
           : visible.map((entry) => `${entry.kind}\t${entry.path}`).join("\n");
       }
       case "search": {
-        const found = await reader.search({
+        const found = await readerFor(args).search({
           query: text(args.query, "query"),
           path: typeof args.path === "string" ? args.path : ".",
         });
         const visible = found.matches.filter((m) => input.ignore.isVisible(m.path));
-        return visible.length < 1
-          ? "(no matches)"
-          : visible.map((m) => `${m.path}:${m.line}:${m.text}`).join("\n");
+        // 没搜全时说清楚：「没找到」不等于「不存在」，缩小 path 再搜。
+        if (visible.length < 1) {
+          return found.truncated
+            ? "(no matches in the files searched so far — the search stopped before covering everything under this path; search a narrower path, e.g. a subdirectory, before concluding it is absent)"
+            : "(no matches)";
+        }
+        const lines = visible.map((m) => `${m.path}:${m.line}:${m.text}`).join("\n");
+        return found.truncated ? `${lines}\n(more matches may exist: the search stopped early; search a narrower path to see the rest)` : lines;
       }
       case "read": {
         const path = text(args.path, "path");
@@ -693,7 +756,15 @@ export function createSystemToolRunner(input: {
         if (hidden !== undefined) {
           throw createPrologueError("ROOT_PATH_PROTECTED", `"${path}" is not in the default set: ${hidden}.`);
         }
-        return (await reader.read({ path })).text;
+        // A window of lines when asked (or needed for a large file); the reply says which lines it holds.
+        if (args.offset !== undefined || args.limit !== undefined) {
+          const offset = typeof args.offset === "number" && Number.isInteger(args.offset) && args.offset >= 1 ? args.offset : 1;
+          const limit = typeof args.limit === "number" && Number.isInteger(args.limit) && args.limit >= 1 ? Math.min(args.limit, 2000) : 400;
+          const held = await readerFor(args).read({ path, lines: { from: "head", skip: offset - 1, take: limit } });
+          const shown = held.window && held.window.lastLine >= held.window.firstLine ? `lines ${held.window.firstLine}–${held.window.lastLine}` : "no lines";
+          return `${held.text}\n[${shown} of ${held.totalLines ?? "?"}]`;
+        }
+        return (await readerFor(args).read({ path })).text;
       }
 
       case "write": {
@@ -712,7 +783,8 @@ export function createSystemToolRunner(input: {
       case "edit": {
         const path = text(args.path, "path");
         const find = text(args.find, "find");
-        const current = (await reader.read({ path })).text;
+        // The whole file is read to make the change, not to show it: a file over 64 KB can still be edited.
+        const current = (await reader.read({ path, forEdit: true })).text;
         if (!current.includes(find)) {
           throw createPrologueError("TOOL_ARGUMENTS_INVALID", "That exact text is not in the file.");
         }
@@ -1065,6 +1137,7 @@ export function createSystemToolRunner(input: {
           ...(typeof args.maxToolCalls === "number" ? { maxToolCalls: args.maxToolCalls } : {}),
           ...(typeof args.maxTurns === "number" ? { maxTurns: args.maxTurns } : {}),
           ...(typeof args.workspace === "string" ? { workspace: args.workspace } : {}),
+          ...(args.claims === undefined ? {} : { claims: boardClaims(args.claims) }),
         }, afterDispatch !== undefined);
         /*
          * 说得出这活是谁干的。**派了三个角色出去就得分得清**——只说
@@ -1374,7 +1447,9 @@ export function createSystemToolRunner(input: {
             if (held.nodes.length < 1) {
               return [head, "(no steps match)"].join("\n");
             }
-            const lines = held.nodes.map((node) => describeNode(node, asked !== undefined));
+            const lines = held.nodes.map((node) =>
+              describeNode(node, asked !== undefined, board.name === undefined ? undefined : (one) => board.name!(one)),
+            );
             return [
               head,
               ...lines,
diff --git a/packages/sdk/src/composition/core/typesafe.ts b/packages/sdk/src/composition/core/typesafe.ts
new file mode 100644
index 00000000..5d2b4237
--- /dev/null
+++ b/packages/sdk/src/composition/core/typesafe.ts
@@ -0,0 +1,21 @@
+import { unknownUsage, type TypeSafeRequest, type TypeSafeOutcome, type TypeSafeAdapter, type UsageLedger } from "../../model/core/index.js";
+import type { ExactRef } from "../../kernel/core/index.js";
+import type { ModelResponses } from "./model-response.js";
+export interface TypeSafeInput extends TypeSafeRequest {
+  readonly protocol: "typesafe"; readonly endpoint: string; readonly credentialRef: ExactRef<"credential">;
+  readonly timeoutMs?: number; readonly signal?: AbortSignal;
+}
+export interface TypeSafeSurface { evaluate(input: TypeSafeInput): Promise<TypeSafeOutcome> }
+export function createTypeSafeSurface(deps: { responses: ModelResponses; adapter: TypeSafeAdapter; usage: UsageLedger; assertReady(): void; maxResponseBytes: number }): TypeSafeSurface {
+  return { evaluate(input) { return deps.responses.run(input.signal, async signal => {
+    deps.assertReady(); signal.throwIfAborted();
+    const response = await deps.responses.request({ channel: "model", endpoint: input.endpoint, credentialRef: input.credentialRef,
+      authScheme: "bearer", body: deps.adapter.buildBody(input), timeoutMs: input.timeoutMs ?? 30_000, maxResponseBytes: deps.maxResponseBytes }, signal);
+    const result = deps.adapter.readOutcome(new TextDecoder().decode(response.bytes));
+    signal.throwIfAborted();
+    const usage = unknownUsage();
+    const count = (n: unknown) => typeof n === "number" && Number.isFinite(n) && n >= 0 ? { source: "reported" as const, tokens: n } : usage.input;
+    deps.usage.record({ ...usage, input: count(result.usage?.input_tokens), output: count(result.usage?.output_tokens) });
+    return result;
+  }); } };
+}
diff --git a/packages/sdk/src/effect-policy/README.md b/packages/sdk/src/effect-policy/README.md
index 118f4c92..312c737e 100644
--- a/packages/sdk/src/effect-policy/README.md
+++ b/packages/sdk/src/effect-policy/README.md
@@ -46,3 +46,7 @@
 - **回执。** policy-receipt 可 issue / inspect / revoke / assert，状态 active / expired / revoked；过期或撤销后不能再当授权用。sandbox lease 把「沙箱已套上」变成可检查的事实。
 
 功能定义只在需求分册里。本文件不复述功能清单，也不记进度。
+
+### Trusted per-Run grants
+
+The Runtime may bind trusted App tool/root grants privately to one Session/Run/authorized-root. They use this same effect ledger and evaluator, never caller-provided origin as authority. Cached effects and dispatch require the original live capability; expiry and restart do not recreate grants. Denies, unknown kinds, escalation, hooks and Host restrictions remain stronger. The bounded public API supports whole authorized roots only (`paths: ["."]`), not lexical child-directory isolation.
diff --git a/packages/sdk/src/effect-policy/core/chain.ts b/packages/sdk/src/effect-policy/core/chain.ts
index 67bdab00..12b50d83 100644
--- a/packages/sdk/src/effect-policy/core/chain.ts
+++ b/packages/sdk/src/effect-policy/core/chain.ts
@@ -43,6 +43,13 @@ import type { StoredEffectRecord, StoredPolicy } from "./stored-policy.js";
 
 export type { EffectOrigin };
 
+/** Private composition capability; proposal origin is never permission authority. */
+export interface EffectScopeAuthority {
+  readonly id: string;
+  readonly rules: readonly PolicyRule[];
+  readonly active: () => boolean;
+}
+
 /**
  * 副作用的唯一一条门。
  *
@@ -333,6 +340,7 @@ function fromStoredProposal(proposal: StoredEffectRecord["proposal"]): EffectPro
 
 export function createEffectChain(input: {
   readonly newId: () => string;
+  readonly scopeAuthority?: { get(request: object): EffectScopeAuthority | undefined };
   readonly rules: readonly PolicyRule[];
   readonly posture: Posture;
   readonly store?: StoredPolicy;
@@ -344,6 +352,12 @@ export function createEffectChain(input: {
   readonly reviewAvailable?: (ref: ExactRef<"resource">) => boolean | Promise<boolean>;
 }): EffectChain {
   const learned: PolicyRule[] = [];
+  const effectScopes = new Map<string, EffectScopeAuthority>();
+  const scopeOf = (request: object): EffectScopeAuthority | undefined => {
+    const scope = input.scopeAuthority?.get(request);
+    if (scope !== undefined && !scope.active()) throw createPrologueError("EFFECT_NOT_AUTHORIZED", "This Run's trusted grants expired.");
+    return scope;
+  };
   const waiters = new Map<string, ((effect: Effect) => void)[]>();
   // Share the original store for read-only history; linked mutations below still commit together.
   const book = createPendingBook(input.newId, input.store);
@@ -385,8 +399,17 @@ export function createEffectChain(input: {
     }
   };
 
-  const inForce = (): readonly PolicyRule[] =>
-    learned.length < 1 ? input.rules : [...input.rules, ...learned];
+  const inForce = (proposal: EffectProposal): readonly PolicyRule[] => {
+    const scoped = scopeOf(proposal)?.rules ?? [];
+    return learned.length < 1 && scoped.length < 1 ? input.rules : [...input.rules, ...learned, ...scoped];
+  };
+  const assertScope = (id: string, request: object): void => {
+    const expected = effectScopes.get(id);
+    const actual = scopeOf(request);
+    if (expected !== actual || (expected === undefined && held.get(id)?.proposal.inputFingerprint.startsWith("@run-scope:"))) {
+      throw createPrologueError("EFFECT_NOT_AUTHORIZED", "This effect belongs to a different or expired Run authority.");
+    }
+  };
 
   const view = (id: string, entry: Held): Effect => ({
     ref: mintRef("effect", id),
@@ -992,22 +1015,23 @@ export function createEffectChain(input: {
     },
 
     evaluate(proposal: EffectProposal): PolicyDecision {
-      return evaluate(inForce(), targetOf(proposal), input.posture);
+      return evaluate(inForce(proposal), targetOf(proposal), input.posture);
     },
 
     restriction(proposal: EffectProposal): PolicyOutcome | undefined {
-      return tightening(evaluate(inForce(), targetOf(proposal), input.posture));
+      return tightening(evaluate(inForce(proposal), targetOf(proposal), input.posture));
     },
 
     prepare(proposal: EffectProposal, now: ClockReading, restrict?: PolicyOutcome): Promise<Effect> {
       return enqueue(async () => {
         const existing = byFingerprint.get(proposal.inputFingerprint);
         if (existing !== undefined) {
+          assertScope(existing, proposal);
           return view(existing, held.get(existing) as Held);
         }
 
         const target: PolicyTarget = targetOf(proposal);
-        const ruled = evaluate(inForce(), target, input.posture);
+        const ruled = evaluate(inForce(proposal), target, input.posture);
         const said = restrict === "allow" ? undefined : restrict;
         const merged = stricter(tightening(ruled), said);
         const decision: PolicyDecision =
@@ -1016,6 +1040,7 @@ export function createEffectChain(input: {
             : { ...ruled, outcome: merged, reason: "hook-restricted" };
 
         const id = input.newId();
+        const scoped = scopeOf(proposal);
         const isolated = isolateProposal(proposal);
         const entry: Held = {
           state:
@@ -1058,6 +1083,7 @@ export function createEffectChain(input: {
           book.install(pending.id, pending.held);
         }
         held.set(id, entry);
+        if (scoped !== undefined) effectScopes.set(id, scoped);
         byFingerprint.set(proposal.inputFingerprint, id);
         return view(id, entry);
       });
@@ -1144,13 +1170,16 @@ export function createEffectChain(input: {
       });
     },
 
-    async dispatch(ref, { recheck, run }): Promise<EffectReceipt> {
+    async dispatch(ref, request): Promise<EffectReceipt> {
+      const { recheck, run } = request;
       const loaded = load(ref);
+      assertScope(loaded.id, request);
       rejectUnlessDispatchable(loaded.entry);
       const stillGood = await recheck(view(loaded.id, loaded.entry));
       await enqueue(async () => {
         const { id, entry } = load(ref);
         rejectUnlessDispatchable(entry);
+        assertScope(id, request);
         if (!stillGood) {
           const failed: Held = { ...entry, state: "failed" };
           await persist({ effects: [{ id, entry: failed }] });
@@ -1195,6 +1224,7 @@ export function createEffectChain(input: {
         throw error;
       }
       try {
+        assertScope(id, request);
         await run(view(id, load(ref).entry));
       } catch (error) {
         const code = (error as { code?: unknown } | undefined)?.code;
diff --git a/packages/sdk/src/effect-policy/core/index.ts b/packages/sdk/src/effect-policy/core/index.ts
index 08d1ba2a..90abcbac 100644
--- a/packages/sdk/src/effect-policy/core/index.ts
+++ b/packages/sdk/src/effect-policy/core/index.ts
@@ -64,6 +64,7 @@ export {
   type EffectChain,
   type EffectGate,
   type EffectOrigin,
+  type EffectScopeAuthority,
   type EffectProposal,
   type EffectReceipt,
   type EffectRecoveryReport,
diff --git a/packages/sdk/src/host/README.md b/packages/sdk/src/host/README.md
index a577eaf9..d42ead7c 100644
--- a/packages/sdk/src/host/README.md
+++ b/packages/sdk/src/host/README.md
@@ -14,6 +14,8 @@ JS 与原生 Host 之间的合同和窄桥：握手、只写凭证、网络放
 
 不定义 Session/Run 状态，不拥有产品交互。不拥有授权根上的读、补丁、命令状态机——那些见 `workspace/`。JS 侧不做权限判定——判定在原生 Host。
 
+Model 预检可使用 Host 的纯回环主机判断窄口，但不能因此获得网络许可。实际回环 HTTP 仍需显式 model/loopback 放行；127 前缀域名不是回环 IP。见 [回环端点合同](../../../../docs/slices/model-loopback.md)。
+
 ## 可配与可换
 
 - **槽（可换执行逻辑）：** `host`——原生能力：存储、网络、命令、时钟、沙箱。**现役唯一真的能换的槽。**
diff --git a/packages/sdk/src/host/core/contract.ts b/packages/sdk/src/host/core/contract.ts
index 030d2153..b4e4ba65 100644
--- a/packages/sdk/src/host/core/contract.ts
+++ b/packages/sdk/src/host/core/contract.ts
@@ -150,10 +150,10 @@ export type HostSandboxPosture = "read-only" | "workspace-write" | "danger-full-
  *
  * `none` 只在 `web` / `mcp` 通道上合法：模型调用不带钥匙是 bug 不是功能。
  */
-export type ModelAuthScheme = "bearer" | "x-api-key" | "none";
+export type ModelAuthScheme = "bearer" | "x-api-key" | "x-goog-api-key" | "none";
 
 /** 出网允许的 HTTP 方法。缺省 POST；DELETE 只给 MCP 结束会话。 */
-export type ModelNetworkMethod = "POST" | "DELETE";
+export type ModelNetworkMethod = "POST" | "DELETE" | "GET";
 
 /**
  * 最终 HTTP 响应头里 MCP 需要的那三个有界字段。
@@ -190,6 +190,10 @@ export interface AttachmentSubstitution {
 }
 
 export interface ModelNetworkPrepareInput {
+  /** Only GET of an image URL returned by this completed model call. No body, credentials, headers or redirects. */
+  readonly imageDownloadOf?: ModelNetworkHandle;
+  /** Bounds both declared and streamed response bytes at the Host. */
+  readonly maxResponseBytes?: number;
   /** 这一次算哪一类出网。放行了模型不等于放行了网页。 */
   readonly channel: NetworkChannel;
   readonly endpoint: string;
@@ -698,10 +702,14 @@ export interface WorkspaceReadPort {
     readonly rootRef: ExactRef<"authorized-root">;
     readonly query: string;
     readonly path: string;
-  }): Promise<{ readonly matches: readonly WorkspaceMatch[] }>;
+  }): Promise<{ readonly matches: readonly WorkspaceMatch[]; readonly truncated?: boolean }>;
   readFile(input: {
     readonly rootRef: ExactRef<"authorized-root">;
     readonly path: string;
+    /**
+     * 整份读的字节上限，默认 64KB，最多 2MB。只在正文不整份进模型上下文时放宽：编辑（只出差异）和取行窗。
+     */
+    readonly maxBytes?: number;
   }): Promise<{ readonly path: string; readonly text: string; readonly digest: string }>;
 }
 
@@ -815,7 +823,7 @@ export interface PrologueHost {
     readonly rootRef: ExactRef<"authorized-root">;
     readonly query: string;
     readonly path: string;
-  }): Promise<{ readonly matches: readonly WorkspaceMatch[] }>;
+  }): Promise<{ readonly matches: readonly WorkspaceMatch[]; readonly truncated?: boolean }>;
   /**
    * 读一个文件。**摘要和正文出自同一次读取**。
    *
@@ -826,6 +834,10 @@ export interface PrologueHost {
   readFile(input: {
     readonly rootRef: ExactRef<"authorized-root">;
     readonly path: string;
+    /**
+     * 整份读的字节上限，默认 64KB，最多 2MB。只在正文不整份进模型上下文时放宽：编辑（只出差异）和取行窗。
+     */
+    readonly maxBytes?: number;
   }): Promise<{ readonly path: string; readonly text: string; readonly digest: string }>;
   /**
    * 按**字节**读授权根里的一份文件。
diff --git a/packages/sdk/src/host/core/index.ts b/packages/sdk/src/host/core/index.ts
index 14995ad8..dd28b723 100644
--- a/packages/sdk/src/host/core/index.ts
+++ b/packages/sdk/src/host/core/index.ts
@@ -67,6 +67,7 @@ export {
   DENY_ALL,
   CHANNELS_NEEDING_CREDENTIAL,
   isChannelAllowed,
+  isLoopbackHost,
   MAX_REDIRECTS,
   type CheckedTarget,
   type NetworkAllowance,
diff --git a/packages/sdk/src/host/core/network-guard.ts b/packages/sdk/src/host/core/network-guard.ts
index 3e8b6696..a6d872a9 100644
--- a/packages/sdk/src/host/core/network-guard.ts
+++ b/packages/sdk/src/host/core/network-guard.ts
@@ -48,13 +48,14 @@ export function isChannelAllowed(allowance: NetworkAllowance, channel: NetworkCh
   return allowance[channel] === true;
 }
 
-function isLoopbackHost(host: string): boolean {
+/** Pure target classification; this never grants network permission. */
+export function isLoopbackHost(host: string): boolean {
   return (
     host === "localhost" ||
     host === "::1" ||
     host === "[::1]" ||
     host.endsWith(".localhost") ||
-    /^127\./.test(host)
+    /^127(?:\.(?:\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])){3}$/.test(host)
   );
 }
 
diff --git a/packages/sdk/src/host/plugin/node-mutation.ts b/packages/sdk/src/host/plugin/node-mutation.ts
index 0fcc70bc..fcfc3b5b 100644
--- a/packages/sdk/src/host/plugin/node-mutation.ts
+++ b/packages/sdk/src/host/plugin/node-mutation.ts
@@ -2,6 +2,7 @@ import { spawn, type ChildProcess } from "node:child_process";
 import {
   closeSync,
   constants as fsConstants,
+  fchmodSync,
   readdirSync,
   writeFileSync,
   existsSync,
@@ -656,14 +657,24 @@ export class NodeMutation {
     return existsSync(absolute) ? readFileSync(absolute, "utf8") : undefined;
   }
 
-  /** 临时文件 + fsync + 原子改名。写一半不会让原文变坏。 */
+  /**
+   * 临时文件 + fsync + 原子改名。写一半不会让原文变坏。
+   * 改已有文件时沿用它原来的权限（可执行的脚本改完仍可执行）；新文件按普通文件的默认权限（0666 减去 umask）。
+   */
   private write(absolute: string, text: string): number {
     mkdirSync(dirname(absolute), { recursive: true });
     const temp = `${absolute}.${randomBytes(6).toString("hex")}.tmp`;
     const bytes = Buffer.from(text, "utf8");
+    let mode: number | undefined;
+    try {
+      mode = statSync(absolute).mode & 0o7777;
+    } catch {
+      mode = undefined;
+    }
     try {
-      const handle = openSync(temp, "wx", 0o600);
+      const handle = openSync(temp, "wx", mode ?? 0o666);
       try {
+        if (mode !== undefined) fchmodSync(handle, mode);
         writeSync(handle, bytes);
         fsyncSync(handle);
       } finally {
diff --git a/packages/sdk/src/host/plugin/node-workspace.ts b/packages/sdk/src/host/plugin/node-workspace.ts
index 3db3108f..7a671e72 100644
--- a/packages/sdk/src/host/plugin/node-workspace.ts
+++ b/packages/sdk/src/host/plugin/node-workspace.ts
@@ -21,8 +21,12 @@ import type { WorkspaceEntry, WorkspaceGrant, WorkspaceMatch } from "../core/con
 
 const MAX_LIST = 256;
 const MAX_SEARCH = 50;
-const MAX_WALK = 1024;
+// 一次搜索最多走过的文件数。原来 1024：中等仓库（几千个文件）搜到一半就停，还答成「没找到」。
+const MAX_WALK = 20_000;
 const MAX_READ = 64 * 1024;
+// 搜索读的单个文件上限，和整份读取（MAX_READ）分开：原来超过 64KB 的文件在搜索里被悄悄跳过，
+// 76KB 的样式文件里明明有，却答「没找到」。更大的文件仍跳过，并计入「没搜全」。
+const MAX_SEARCH_FILE = 2 * 1024 * 1024;
 const EXCERPT = 200;
 
 export class NodeWorkspace {
@@ -90,21 +94,37 @@ export class NodeWorkspace {
     rootRef: ExactRef<"authorized-root">,
     query: string,
     path: string,
-  ): { matches: WorkspaceMatch[] } {
+  ): { matches: WorkspaceMatch[]; truncated: boolean } {
     if (query.length < 1) {
       throw createPrologueError("RUNTIME_OPTIONS_INVALID", "query is required.");
     }
     const root = this.rootPath(rootRef);
     const start = this.resolvePath(root, path);
     const matches: WorkspaceMatch[] = [];
-    this.walk(root, start, 0, (file) => {
+    // 路径可以是目录，也可以是单个文件：把文件当目录去列举会拿到 ENOTDIR，原来被吞掉后答成「没找到」，
+    // 模型据此断定文件里没有这段代码。路径不存在时如实报错，不答「没找到」。
+    let kind: "file" | "dir";
+    try {
+      const stat = statSync(start);
+      kind = stat.isFile() ? "file" : "dir";
+    } catch {
+      throw createPrologueError("ROOT_NOT_FOUND", "Path was not found.");
+    }
+    let skippedLarge = 0;
+    const visitFile = (file: string): boolean => {
       if (matches.length >= MAX_SEARCH) {
         return false;
       }
       let text: string;
       try {
-        const buf = readWholeFile(file);
-        if (buf.includes(0) || buf.byteLength > MAX_READ) {
+        let buf: Buffer;
+        try {
+          buf = readWholeFile(file, MAX_SEARCH_FILE);
+        } catch (error) {
+          if ((error as { code?: string } | undefined)?.code === "WORKSPACE_TOO_LARGE") skippedLarge += 1;
+          return true;
+        }
+        if (buf.includes(0)) {
           return true;
         }
         text = buf.toString("utf8");
@@ -128,20 +148,28 @@ export class NodeWorkspace {
         }
       }
       return true;
-    });
-    return { matches };
+    };
+    // 没走完（文件数到上界）或结果条数到上界，都如实说「没搜全」，不让「没找到」被读成「不存在」。
+    let walked = 0;
+    if (kind === "file") {
+      visitFile(start);
+    } else {
+      walked = this.walk(root, start, 0, visitFile);
+    }
+    return { matches, truncated: matches.length >= MAX_SEARCH || walked >= MAX_WALK || skippedLarge > 0 };
   }
 
   read(
     rootRef: ExactRef<"authorized-root">,
     path: string,
+    maxBytes: number = MAX_READ,
   ): { path: string; text: string; digest: string } {
     const root = this.rootPath(rootRef);
     const target = this.resolvePath(root, path);
     // 打开一次，之后只认这个句柄。原来是「校验一条路径，再按路径重新
     // 打开」——两次解析之间最后一段可以被换成指向根外的符号链接，
     // 检查过的和读到的就不是同一个东西了。
-    const buf = readWholeFile(target);
+    const buf = readWholeFile(target, Math.min(Math.max(maxBytes, MAX_READ), MAX_SEARCH_FILE));
     // **按字节算**，再解码。顺序反了就变成对解码结果的断言。
     const digest = createHash("sha256").update(buf).digest("hex");
     /*
@@ -380,7 +408,7 @@ function inside(root: string, target: string): boolean {
  * 只信句柄，不信路径。`O_NOFOLLOW` 让最后一段是符号链接时直接失败，
  * `fstat` 在同一个句柄上判类型和大小——不再有第二次按路径解析。
  */
-function readWholeFile(target: string): Buffer {
+function readWholeFile(target: string, max: number = MAX_READ): Buffer {
   let handle: number;
   try {
     handle = openSync(target, fsConstants.O_RDONLY | fsConstants.O_NOFOLLOW);
@@ -403,7 +431,7 @@ function readWholeFile(target: string): Buffer {
     if (!stat.isFile()) {
       throw createPrologueError("WORKSPACE_READ_FAILED", "Only regular files can be read.");
     }
-    if (stat.size > MAX_READ) {
+    if (stat.size > max) {
       /*
        * **换行窗或按字节读，不是这份文件坏了。**
        *
@@ -411,8 +439,8 @@ function readWholeFile(target: string): Buffer {
        */
       throw createPrologueError(
         "WORKSPACE_TOO_LARGE",
-        `That file is ${stat.size} bytes, over the ${MAX_READ}-byte limit for one whole read; ` +
-          "read a line window or a byte range instead.",
+        `That file is ${stat.size} bytes, over the ${max}-byte limit for one whole read; ` +
+          "read a line window instead (the read tool's offset and limit).",
       );
     }
     const buf = Buffer.alloc(stat.size);
diff --git a/packages/sdk/src/host/plugin/node.ts b/packages/sdk/src/host/plugin/node.ts
index 6c344644..cc94d175 100644
--- a/packages/sdk/src/host/plugin/node.ts
+++ b/packages/sdk/src/host/plugin/node.ts
@@ -35,6 +35,7 @@ import {
   HOST_PROTOCOL_MINOR,
   CHANNELS_NEEDING_CREDENTIAL,
   isChannelAllowed,
+  isLoopbackHost,
   MAX_REDIRECTS,
   type CheckedTarget,
   type HostSandboxPosture,
@@ -89,6 +90,8 @@ interface CallRecord {
   authScheme: string;
   extraHeaders: Record<string, string>;
   method: ModelNetworkMethod;
+  imageDownload: boolean;
+  maxResponseBytes?: number;
   channel: string;
   status: ModelNetworkInspectStatus;
   abort: AbortController;
@@ -100,6 +103,7 @@ interface CallRecord {
 
 
 function readNetworkMethod(value: string | undefined): ModelNetworkMethod {
+  if (value === "GET") return "GET";
   if (value === undefined || value === "POST") {
     return "POST";
   }
@@ -180,6 +184,8 @@ function parseRetryAfter(header: string | null, nowMs: number): number | undefin
 }
 
 export interface NodeHostOptions {
+  /** Trusted App revocation check after preparation, immediately before model bytes leave. Can deny, never bypass Host policy. */
+  readonly beforeModelDispatch?: () => void | Promise<void>;
   /**
    * 落盘根目录。不给就用本机默认位置。**这是 Host 的内部事实**，
    * 只在这里出现一次，不会顺着回执流到 App。
@@ -490,21 +496,29 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
           "That HTTP method is not allowed on this network channel.",
         );
       }
-      if (input.channel === "model" && input.authScheme === "none") {
+      const download = input.imageDownloadOf === undefined ? undefined : calls.get(input.imageDownloadOf.id);
+      if (input.maxResponseBytes !== undefined && (!Number.isSafeInteger(input.maxResponseBytes) || input.maxResponseBytes < 1)) throw createPrologueError("MODEL_REQUEST_INVALID", "A response bound must be a positive byte count.");
+      if (input.imageDownloadOf !== undefined && (input.channel !== "model" || method !== "GET" || !download || download.channel !== "model" || download.status !== "completed" || download.imageDownload || input.authScheme !== "none" || input.credentialRef !== undefined || input.body.byteLength !== 0 || Object.keys(input.extraHeaders ?? {}).length || input.attachments?.length || input.maxResponseBytes === undefined)) throw createPrologueError("MODEL_REQUEST_INVALID", "Image downloads require a completed model call and no credentials or request content.");
+      if (method === "GET" && !download) throw createPrologueError("MODEL_REQUEST_INVALID", "GET is reserved for model image downloads.");
+      const wanted = new URL(input.endpoint);
+      const localNoAuth = allowance.loopback === true && isLoopbackHost(wanted.hostname) && input.credentialRef === undefined;
+      if (download && (wanted.username || wanted.password || wanted.protocol !== "https:" && !(localNoAuth && wanted.origin === new URL(download.endpoint).origin) || isLoopbackHost(wanted.hostname) && wanted.origin !== new URL(download.endpoint).origin)) throw createPrologueError("NETWORK_TARGET_DENIED", "An image download needs public HTTPS or its original loopback model origin.");
+      if (input.channel === "model" && input.authScheme === "none" && !download && !localNoAuth) {
         throw createPrologueError(
           "MODEL_NETWORK_FAILED",
           `Egress on the "${input.channel}" channel needs a credential.`,
         );
       }
       // 地址闸在准备阶段就过一次：不放行的目标出网次数为零。
-      const target = checkNetworkTarget(input.endpoint, allowance);
+      const targetAllowance = download ? { ...allowance, loopback: localNoAuth && wanted.origin === new URL(download.endpoint).origin } : allowance;
+      const target = checkNetworkTarget(input.endpoint, targetAllowance);
       /*
        * 名字过了闸还不够——**把它真的解析一遍，逐个地址核**。
        *
        * `evil.example.com` 是个公网名字，A 记录却可以指向
        * `169.254.169.254`。只看名字的话这一枪就打进元数据服务了。
        */
-      await assertTargetResolves(target.url, allowance, resolveHost);
+      await assertTargetResolves(target.url, targetAllowance, resolveHost);
       /*
        * **有几条通道必须带钥匙**，名单在 `CHANNELS_NEEDING_CREDENTIAL`。
        *
@@ -513,7 +527,7 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
        * 的事，不该由一个否定式替它决定。
        */
       if (input.credentialRef === undefined) {
-        if (CHANNELS_NEEDING_CREDENTIAL.includes(input.channel)) {
+        if (CHANNELS_NEEDING_CREDENTIAL.includes(input.channel) && !(input.authScheme === "none" && (download || localNoAuth))) {
           throw createPrologueError(
             "MODEL_NETWORK_FAILED",
             `Egress on the "${input.channel}" channel needs a credential.`,
@@ -550,6 +564,8 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
         authScheme: input.authScheme,
         extraHeaders: { ...(input.extraHeaders ?? {}) },
         method,
+        imageDownload: download !== undefined,
+        ...(input.maxResponseBytes === undefined ? {} : { maxResponseBytes: input.maxResponseBytes }),
         channel: input.channel,
         status: "prepared",
         abort: new AbortController(),
@@ -650,6 +666,8 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
       }
       if (record.authScheme === "x-api-key") {
         headers.set("x-api-key", secret);
+      } else if (record.authScheme === "x-goog-api-key") {
+        headers.set("x-goog-api-key", secret);
       } else if (record.authScheme === "bearer") {
         headers.set("authorization", `Bearer ${secret}`);
       }
@@ -688,11 +706,14 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
          * 之后任何一种挂法——连接被拒、读到一半断线、进程被杀——都不该
          * 让引擎以为它没出去过。下面的 `catch` 只在能确定没上线时翻回来。
          */
+        if (record.channel === "model" && options.beforeModelDispatch) await awaitDispatchGuard(options.beforeModelDispatch(), signal);
+        if (isCancelled()) return "cancelled";
         record.dispatched = true;
         response = await fetch(target.url, {
           method: record.method,
           headers,
-          body,
+          ...(record.method === "GET" ? {} : { body }),
+          credentials: "omit",
           redirect: "manual",
           signal,
         }).catch((error: unknown) => {
@@ -715,6 +736,10 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
         if (response.status < 300 || response.status >= 400) {
           break;
         }
+        if (record.imageDownload) {
+          await response.body?.cancel();
+          throw createPrologueError("NETWORK_TARGET_DENIED", "Image downloads cannot follow redirects.");
+        }
         if (hop >= MAX_REDIRECTS) {
           setStatus("failed");
           throw createPrologueError(
@@ -735,6 +760,12 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
         // 每一跳都重新解析并逐个核——否则第一跳干净、第二跳指进内网。
         await assertTargetResolves(target.url, allowance, resolveHost);
       }
+      const declaredLength = Number(response.headers.get("content-length"));
+      if (record.maxResponseBytes !== undefined && declaredLength > record.maxResponseBytes) {
+        await response.body?.cancel();
+        setStatus("failed");
+        throw createPrologueError("MODEL_RESPONSE_INVALID", "The model response exceeded its byte limit.");
+      }
       if (!response.ok) {
         // 只认对方自己说的。400 这类是请求本身不对，重发一百次也一样。
         record.retryable = PROVIDER_SAYS_RETRY.has(response.status);
@@ -747,7 +778,7 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
           setStatus("failed");
           throw createPrologueError(
             "MODEL_HTTP_ERROR",
-            `The model provider rejected the request (${response.status}).`,
+            `The model provider rejected the request (${response.status}).`, { status: response.status },
           );
         }
       } else if (input.onResponse !== undefined) {
@@ -782,7 +813,10 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
       };
       try {
         armIdle();
+        let receivedBytes = 0;
         for await (const chunk of response.body) {
+          receivedBytes += chunk.byteLength;
+          if (record.maxResponseBytes !== undefined && receivedBytes > record.maxResponseBytes) throw createPrologueError("MODEL_RESPONSE_INVALID", "The model response exceeded its byte limit.");
           if (isCancelled()) {
             return "cancelled";
           }
@@ -954,7 +988,7 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
       return workspace.search(input.rootRef, input.query, input.path);
     },
     async readFile(input) {
-      return workspace.read(input.rootRef, input.path);
+      return workspace.read(input.rootRef, input.path, input.maxBytes);
     },
     async readBytes(input) {
       return workspace.readBytes(input.rootRef, input.path, input.offset, input.length);
@@ -1045,3 +1079,11 @@ export function createNodeHost(options: NodeHostOptions = {}): PrologueHost {
     intake,
   };
 }
+
+async function awaitDispatchGuard(work: void | Promise<void>, signal: AbortSignal): Promise<void> {
+  signal.throwIfAborted();
+  let abort: (() => void) | undefined;
+  try { await Promise.race([work, new Promise<never>((_resolve, reject) => { abort = () => reject(signal.reason); signal.addEventListener("abort", abort, { once: true }); })]); }
+  finally { if (abort) signal.removeEventListener("abort", abort); }
+  signal.throwIfAborted();
+}
diff --git a/packages/sdk/src/host/plugin/tauri.ts b/packages/sdk/src/host/plugin/tauri.ts
index c6c2dc00..99dbc3c5 100644
--- a/packages/sdk/src/host/plugin/tauri.ts
+++ b/packages/sdk/src/host/plugin/tauri.ts
@@ -132,6 +132,7 @@ export function createTauriHost(): PrologueHost {
       return readCredential(raw);
     },
     async prepareModelNetwork(input: ModelNetworkPrepareInput): Promise<ModelNetworkHandle> {
+      if (input.imageDownloadOf || input.maxResponseBytes !== undefined || input.authScheme === "x-goog-api-key" || input.channel === "model" && input.authScheme === "none") throw createPrologueError("HOST_CAPABILITY_MISSING", "This Tauri Host does not support bounded native model requests yet.");
       const raw = await call("model_network_prepare", {
         channel: input.channel,
         endpoint: input.endpoint,
@@ -435,7 +436,8 @@ export function createTauriHost(): PrologueHost {
       return readCommandReceipt(raw);
     },
     async readFile(input) {
-      const raw = await call("workspace_read", { rootRef: input.rootRef, path: input.path });
+      // An absent maxBytes is dropped when serialised, and Rust reads it as None (the 64 KiB cap).
+      const raw = await call("workspace_read", { rootRef: input.rootRef, path: input.path, maxBytes: input.maxBytes });
       return readFileResult(raw);
     },
     async readBytes(input) {
diff --git a/packages/sdk/src/index.ts b/packages/sdk/src/index.ts
index 3d395680..9e32ef00 100644
--- a/packages/sdk/src/index.ts
+++ b/packages/sdk/src/index.ts
@@ -669,6 +669,7 @@ export {
   createLocalQueue,
   createTaskBoardStore,
   DEFAULT_BOARD_LIMITS,
+  describeAssignee,
 } from "./orchestration/api/index.js";
 export type {
   BackgroundClaim,
@@ -697,9 +698,11 @@ export type {
 export type {
   AcceptanceCheck,
   AcceptanceOutcome,
+  BoardAssignee,
   BoardLimits,
   BoardProposal,
   ExecutionBinding,
+  HumanPrincipal,
   FailurePolicy,
   Goal,
   GoalInput,
@@ -710,6 +713,7 @@ export type {
   TaskBoard,
   TaskBoardStore,
   TaskNode,
+  NodeReport,
 } from "./orchestration/api/index.js";
 export { createDeliveryBox, DEFAULT_DELIVERY_LIMITS } from "./delivery/api/index.js";
 export type {
@@ -1205,3 +1209,7 @@ export {
 } from "./data-source/api/index.js";
 
 export type { InterruptedRun, RunRecoveryOperation, RunRecoveryView, SessionRecoveryReport } from "./session/api/index.js";
+
+export type { TypeSafeInput, TypeSafeSurface } from "./composition/api/index.js";
+
+export type { AgentRunGrants } from "./composition/api/index.js";
diff --git a/packages/sdk/src/kernel/core/error.ts b/packages/sdk/src/kernel/core/error.ts
index baf61a66..763c2082 100644
--- a/packages/sdk/src/kernel/core/error.ts
+++ b/packages/sdk/src/kernel/core/error.ts
@@ -26,6 +26,12 @@ export type PrologueErrorCode =
    * 手里其实是一段空的。
    */
   | "MODEL_REFUSED"
+  /**
+   * 回答一直撞输出上限：续写到上界仍没写完，或者还没写出正文就用完了上限。
+   *
+   * **是失败，不是成功**——半截回答当成完成，人会以为拿到了完整的答案。
+   */
+  | "MODEL_OUTPUT_TRUNCATED"
   | "MODEL_STRUCTURED_INVALID"
   /** 这份 schema 用了这个构建核不动的关键字。**当场失败**，不是「先跑起来，核不动的那几条就算过」。 */
   | "MODEL_STRUCTURED_UNSUPPORTED"
@@ -394,6 +400,8 @@ export interface PrologueError extends Error {
   readonly name: "PrologueError";
   readonly code: PrologueErrorCode;
   readonly safeMessage: string;
+  /** Safe HTTP status only, never provider response content. */
+  readonly status?: number;
 }
 
 const CODES: ReadonlySet<string> = new Set([
@@ -418,6 +426,7 @@ const CODES: ReadonlySet<string> = new Set([
   "MODEL_PURPOSE_AMBIGUOUS",
   "MODEL_NOT_FOUND",
   "MODEL_REFUSED",
+  "MODEL_OUTPUT_TRUNCATED",
   "MODEL_STRUCTURED_INVALID",
   "MODEL_STRUCTURED_UNSUPPORTED",
   "MODEL_HTTP_ERROR",
@@ -651,6 +660,7 @@ const CODES: ReadonlySet<string> = new Set([
 export function createPrologueError(
   code: PrologueErrorCode,
   safeMessage: string,
+  detail?: { readonly status?: number },
 ): PrologueError {
   const error = new Error(safeMessage) as PrologueError;
   Object.defineProperties(error, {
@@ -658,6 +668,7 @@ export function createPrologueError(
     code: { value: code, enumerable: true },
     safeMessage: { value: safeMessage, enumerable: true },
   });
+  if (detail?.status !== undefined && Number.isInteger(detail.status) && detail.status >= 400 && detail.status <= 599) Object.defineProperty(error, "status", { value: detail.status, enumerable: true });
   return error;
 }
 
diff --git a/packages/sdk/src/model/README.md b/packages/sdk/src/model/README.md
index 1f50d853..c2fd8099 100644
--- a/packages/sdk/src/model/README.md
+++ b/packages/sdk/src/model/README.md
@@ -12,6 +12,8 @@
 
 不持有密钥（Host 在最后边界注入），不拥有 Run 状态，不做协议探测或失败换格式。
 
+目录登记和 Run 启动使用相同的端点形状合同：HTTPS 或本机回环 HTTP。Host 的回环主机判断经模块窄口复用，登记通过不授予网络权限；真实调用仍由 Host 逐跳检查。见 [回环端点合同](../../../../docs/slices/model-loopback.md)。
+
 公开协议缝使用的 `ToolSpec`、`WireMessage` 与 `ProtocolAdapter` 一同经模块 API 和 SDK 根入口提供类型；消费者无需深导入才能声明规范工具和消息。它们是已有协议合同的命名类型，不是原始 Provider 帧读取入口。
 
 ## 可配与可换
diff --git a/packages/sdk/src/model/core/adapter.ts b/packages/sdk/src/model/core/adapter.ts
index 4142b949..1f574e29 100644
--- a/packages/sdk/src/model/core/adapter.ts
+++ b/packages/sdk/src/model/core/adapter.ts
@@ -1,4 +1,5 @@
 import { createPrologueError } from "../../kernel/core/index.js";
+import { isLoopbackHost } from "../../host/core/index.js";
 import { assertStructuredSupported, type StructuredRequest } from "./structured.js";
 import type {
   ModelAuthScheme,
@@ -12,6 +13,17 @@ import type {
   WireMessage,
 } from "./types.js";
 
+/** Input shape only. Host separately enforces channel, loopback and target permissions. */
+export function assertModelEndpoint(endpoint: unknown): asserts endpoint is string {
+  if (typeof endpoint === "string") {
+    try {
+      const url = new URL(endpoint);
+      if (url.protocol === "https:" || url.protocol === "http:" && isLoopbackHost(url.hostname)) return;
+    } catch { /* Report a stable error without echoing the target. */ }
+  }
+  throw createPrologueError("RUNTIME_OPTIONS_INVALID", "A model endpoint must be an https URL or a loopback http URL; network permission is checked separately.");
+}
+
 /**
  * 一次调用可以拧的旋钮。
  *
diff --git a/packages/sdk/src/model/core/catalog.ts b/packages/sdk/src/model/core/catalog.ts
index a8437436..3fd339af 100644
--- a/packages/sdk/src/model/core/catalog.ts
+++ b/packages/sdk/src/model/core/catalog.ts
@@ -1,5 +1,6 @@
 import { createPrologueError, mintRef, type ExactRef } from "../../kernel/core/index.js";
 import type { ModelProtocol } from "./types.js";
+import { assertModelEndpoint } from "./adapter.js";
 
 /**
  * 一条模型登记。**能干什么要明说**——「支持结构化输出」这类东西
@@ -14,7 +15,7 @@ export interface ModelCapabilities {
 export interface ModelEntryInput {
   readonly name: string;
   readonly protocol: ModelProtocol;
-  /** 完整的 HTTPS URL。本机回环要在出网放行里显式打开。 */
+  /** HTTPS 或本机回环 HTTP URL。回环仍要在 Host 出网放行里显式打开。 */
   readonly endpoint: string;
   /** Provider 侧的 model 标识。 */
   readonly model: string;
@@ -209,12 +210,7 @@ export function createModelCatalog(
       if (input.name.length < 1 || input.model.length < 1) {
         throw createPrologueError("RUNTIME_OPTIONS_INVALID", "A model entry needs a name and a model id.");
       }
-      if (!/^https:\/\//.test(input.endpoint) && !/^http:\/\/(localhost|127\.)/.test(input.endpoint)) {
-        throw createPrologueError(
-          "RUNTIME_OPTIONS_INVALID",
-          "A model endpoint must be an https URL (or explicit loopback).",
-        );
-      }
+      assertModelEndpoint(input.endpoint);
       const entry: ModelEntry = { ...input, ref: mintRef("model", newId()) };
       entries.set(entry.ref.id, entry);
       return entry;
diff --git a/packages/sdk/src/model/core/image.ts b/packages/sdk/src/model/core/image.ts
index c866befb..bc6b0e5f 100644
--- a/packages/sdk/src/model/core/image.ts
+++ b/packages/sdk/src/model/core/image.ts
@@ -68,7 +68,7 @@ export interface ImageSpend {
 
 /** adapter 从响应正文里解出来的东西。 */
 export interface ImageOutcome {
-  readonly images: readonly ImagePayload[];
+  readonly images: readonly (ImagePayload | { readonly url: string; readonly mediaType: string | undefined })[];
   /**
    * 对方改写过的提示词。**有些服务会重写**，而人得看得见它照着什么画的。
    */
@@ -116,7 +116,7 @@ export interface ImageAdapter {
     readonly mask: ImagePayload | undefined;
   }): ImageBody;
   /** 从响应正文里解出图片。**解不出来要抛**，不回一个空数组。 */
-  readOutcome(body: string): ImageOutcome;
+  readOutcome(body: string, limits?: ImageLimits): ImageOutcome;
 }
 
 export type SelectImageAdapter = (protocol: string) => ImageAdapter;
@@ -202,3 +202,10 @@ export function assertImageRequest(request: ImageRequest): void {
     );
   }
 }
+
+export interface ImageLimits {
+  readonly maxResponseBytes: number;
+  readonly maxImageBytes: number;
+  readonly maxImages: number;
+}
+export const DEFAULT_IMAGE_LIMITS: ImageLimits = Object.freeze({ maxResponseBytes: 40 * 1024 * 1024, maxImageBytes: 20 * 1024 * 1024, maxImages: 10 });
diff --git a/packages/sdk/src/model/core/index.ts b/packages/sdk/src/model/core/index.ts
index c72cbcc8..efb1f2c7 100644
--- a/packages/sdk/src/model/core/index.ts
+++ b/packages/sdk/src/model/core/index.ts
@@ -15,6 +15,7 @@ export type {
 export {
   ALL_KNOBS,
   assertParamsSupported,
+  assertModelEndpoint,
   createAdapterRegistry,
   MAX_REQUEST_BYTES,
   prepareWire,
@@ -109,3 +110,7 @@ export {
   type TranscriptRequest,
   type TranscriptSegment,
 } from "./speech.js";
+
+export { DEFAULT_IMAGE_LIMITS, type ImageLimits } from "./image.js";
+
+export type { TypeSafeRequest, TypeSafeOutcome, TypeSafeAdapter } from "./typesafe.js";
diff --git a/packages/sdk/src/model/core/types.ts b/packages/sdk/src/model/core/types.ts
index 93244e6b..72232007 100644
--- a/packages/sdk/src/model/core/types.ts
+++ b/packages/sdk/src/model/core/types.ts
@@ -13,7 +13,7 @@ import type { ExactRef } from "../../kernel/core/index.js";
  */
 export type ModelProtocol = "openai-compatible" | "anthropic-compatible" | (string & {});
 
-export type ModelAuthScheme = "bearer" | "x-api-key";
+export type ModelAuthScheme = "bearer" | "x-api-key" | "x-goog-api-key";
 
 export type StreamParser = {
   push(chunk: string): void;
@@ -101,6 +101,7 @@ export type StopReason =
   | "unknown";
 
 export type ModelEvent =
+  | Readonly<{ type: "model-reported"; model: string }>
   /**
    * 人这一轮说了什么。
    *
@@ -352,7 +353,7 @@ export type ModelEvent =
    */
   | Readonly<{ type: "agent-control"; state: "running" | "pausing" | "paused" }>
   /** 整批工具未执行，已请求模型纠正；不代表纠正成功，不含原始工具名或参数。 */
-  | Readonly<{ type: "model-response-repair"; reason: "tool-not-declared" }>
+  | Readonly<{ type: "model-response-repair"; reason: "tool-not-declared" | "output-truncated" }>
   /** Explicit recovery of an interrupted Run, never a reconstructed tool transcript. */
   | Readonly<{ type: "run-recovered"; atMs: number; transcript: "opening-only" | "durable-prefix";
       questions?: readonly { ref: ExactRef<"pending">; kind: string; prompt: string }[];
diff --git a/packages/sdk/src/model/core/typesafe.ts b/packages/sdk/src/model/core/typesafe.ts
new file mode 100644
index 00000000..afb5925a
--- /dev/null
+++ b/packages/sdk/src/model/core/typesafe.ts
@@ -0,0 +1,6 @@
+export interface TypeSafeRequest { readonly model: string; readonly state: string; readonly questions: Readonly<Record<string, unknown>> }
+export interface TypeSafeOutcome { readonly model?: string; readonly answers: Readonly<Record<string, unknown>>; readonly usage?: { readonly input_tokens?: number; readonly output_tokens?: number }; readonly [key: string]: unknown }
+export interface TypeSafeAdapter {
+  buildBody(input: TypeSafeRequest): Uint8Array;
+  readOutcome(body: string): TypeSafeOutcome;
+}
diff --git a/packages/sdk/src/model/plugin/anthropic-compatible.ts b/packages/sdk/src/model/plugin/anthropic-compatible.ts
index bdfb9331..5b9ecd81 100644
--- a/packages/sdk/src/model/plugin/anthropic-compatible.ts
+++ b/packages/sdk/src/model/plugin/anthropic-compatible.ts
@@ -131,6 +131,8 @@ export function createAnthropicCompatibleParser(emit: (event: ModelEvent) => voi
       collector.noteInvalid();
       return;
     }
+    const reportedModel = (parsed as { message?: { model?: unknown } } | null)?.message?.model;
+    if (typeof reportedModel === "string" && /^[A-Za-z0-9][A-Za-z0-9._:/@+-]{0,199}$/.test(reportedModel)) emit({ type: "model-reported", model: reportedModel });
     const observed = collector.observe(parsed);
     // **停止原因先记，而且无条件记**——一帧可以同时带内容和停止原因。
     const reason = stopReasonOf(parsed);
diff --git a/packages/sdk/src/model/plugin/gemini-images.ts b/packages/sdk/src/model/plugin/gemini-images.ts
new file mode 100644
index 00000000..aa01dbf6
--- /dev/null
+++ b/packages/sdk/src/model/plugin/gemini-images.ts
@@ -0,0 +1,27 @@
+import { createPrologueError } from "../../kernel/core/index.js";
+import { DEFAULT_IMAGE_LIMITS, type ImageAdapter, type ImagePayload } from "../core/image.js";
+import { imageBytes } from "./image-bytes.js";
+export const GEMINI_IMAGES: ImageAdapter = {
+  protocol: "gemini", authScheme: "x-goog-api-key", maxPerCall: 4, extraHeaders: () => ({}),
+  buildBody({ request, from, mask }) {
+    if (from.length || mask) throw createPrologueError("MODEL_REQUEST_INVALID", "This Gemini adapter currently supports text-to-image requests only.");
+    return { bytes: new TextEncoder().encode(JSON.stringify({
+      contents: [{ role: "user", parts: [{ text: request.prompt }] }],
+      generationConfig: { responseModalities: ["TEXT", "IMAGE"], ...(request.aspectRatio ? { imageConfig: { aspectRatio: request.aspectRatio } } : {}) },
+    })) };
+  },
+  readOutcome(raw, limits = DEFAULT_IMAGE_LIMITS) {
+    let body: { candidates?: { content?: { parts?: { thought?: boolean; inlineData?: { data?: unknown; mimeType?: unknown }; inline_data?: { data?: unknown; mime_type?: unknown } }[] } }[] };
+    try { body = JSON.parse(raw); } catch { throw createPrologueError("MODEL_RESPONSE_INVALID", "The image response was not JSON."); }
+    const images: ImagePayload[] = [];
+    for (const candidate of body?.candidates ?? []) for (const part of candidate.content?.parts ?? []) {
+      if (part.thought || images.length >= limits.maxImages) continue;
+      const inline = part.inlineData ?? part.inline_data;
+      if (!inline || typeof inline.data !== "string") continue;
+      const mime = "mimeType" in inline ? inline.mimeType : "mime_type" in inline ? inline.mime_type : undefined;
+      images.push({ bytes: imageBytes(inline.data, limits.maxImageBytes), mediaType: typeof mime === "string" ? mime : undefined });
+    }
+    if (!images.length) throw createPrologueError("MODEL_RESPONSE_INVALID", "The image response carried no images.");
+    return { images, revisedPrompt: undefined, spend: { amount: undefined, currency: undefined, tokens: undefined } };
+  },
+};
diff --git a/packages/sdk/src/model/plugin/image-bytes.ts b/packages/sdk/src/model/plugin/image-bytes.ts
new file mode 100644
index 00000000..d94055dc
--- /dev/null
+++ b/packages/sdk/src/model/plugin/image-bytes.ts
@@ -0,0 +1,9 @@
+import { createPrologueError, decodeBase64 } from "../../kernel/core/index.js";
+export function imageBytes(value: string, maxBytes: number): Uint8Array {
+  const padding = value.indexOf("=");
+  const validPadding = padding === -1 || padding >= value.length - 2 && value.slice(padding) === "=".repeat(value.length - padding);
+  if (!value || value.length > Math.ceil(maxBytes / 3) * 4 || value.length % 4 !== 0 || /[^A-Za-z0-9+/=]/u.test(value) || !validPadding) throw createPrologueError("MODEL_RESPONSE_INVALID", "Image data is invalid or exceeds its byte limit.");
+  const bytes = decodeBase64(value);
+  if (!bytes.length || bytes.length > maxBytes) throw createPrologueError("MODEL_RESPONSE_INVALID", "Image data exceeds its byte limit.");
+  return bytes;
+}
diff --git a/packages/sdk/src/model/plugin/openai-compatible.ts b/packages/sdk/src/model/plugin/openai-compatible.ts
index a613588c..3f48b6ec 100644
--- a/packages/sdk/src/model/plugin/openai-compatible.ts
+++ b/packages/sdk/src/model/plugin/openai-compatible.ts
@@ -234,6 +234,8 @@ export function createOpenAICompatibleParser(emit: (event: ModelEvent) => void):
      * ——被吞掉的是一次工具请求，而调用方看到的是「模型什么也没做就
      * 完成了」。停止原因当初就是这么漏的，这里是同一个坑的另一半。
      */
+    const reportedModel = (parsed as { model?: unknown } | null)?.model;
+    if (typeof reportedModel === "string" && /^[A-Za-z0-9][A-Za-z0-9._:/@+-]{0,199}$/.test(reportedModel)) emit({ type: "model-reported", model: reportedModel });
     const reason = finishReasonOf(parsed);
     if (reason !== undefined) {
       lastStop = reason;
diff --git a/packages/sdk/src/model/plugin/openai-images.ts b/packages/sdk/src/model/plugin/openai-images.ts
index 64198c09..3a818d53 100644
--- a/packages/sdk/src/model/plugin/openai-images.ts
+++ b/packages/sdk/src/model/plugin/openai-images.ts
@@ -1,3 +1,5 @@
+import { DEFAULT_IMAGE_LIMITS } from "../core/image.js";
+import { imageBytes } from "./image-bytes.js";
 import { createPrologueError, decodeBase64 as decode } from "../../kernel/core/index.js";
 import { buildMultipart, type MultipartFile } from "./multipart.js";
 import type {
@@ -23,6 +25,7 @@ import type {
 interface ImagesResponse {
   readonly data?: readonly {
     readonly b64_json?: unknown;
+    readonly url?: unknown;
     readonly revised_prompt?: unknown;
   }[];
   readonly usage?: {
@@ -100,9 +103,7 @@ export const OPENAI_IMAGES: ImageAdapter = {
     }
     const body: Record<string, unknown> = {
       model: request.model,
-      n: request.n ?? 1,
-      // **只要字节。** URL 会过期，而且取它还得再出一次网。
-      response_format: "b64_json",
+      ...(request.n === undefined ? {} : { n: request.n }),
     };
     if (request.prompt !== undefined) {
       body.prompt = request.prompt;
@@ -116,14 +117,14 @@ export const OPENAI_IMAGES: ImageAdapter = {
     return { bytes: new TextEncoder().encode(JSON.stringify(body)) };
   },
 
-  readOutcome(raw): ImageOutcome {
+  readOutcome(raw, limits = DEFAULT_IMAGE_LIMITS): ImageOutcome {
     let parsed: ImagesResponse;
     try {
       parsed = JSON.parse(raw) as ImagesResponse;
     } catch {
       throw createPrologueError("MODEL_RESPONSE_INVALID", "The image response was not JSON.");
     }
-    const rows = parsed.data ?? [];
+    const rows = Array.isArray(parsed?.data) ? parsed.data.slice(0, limits.maxImages) : [];
     if (rows.length === 0) {
       /*
        * **空名单是失败，不是「零张图」。**
@@ -133,18 +134,10 @@ export const OPENAI_IMAGES: ImageAdapter = {
        */
       throw createPrologueError("MODEL_RESPONSE_INVALID", "The image response carried no images.");
     }
-    const images: ImagePayload[] = rows.map((row) => {
-      if (typeof row.b64_json !== "string") {
-        throw createPrologueError(
-          "MODEL_RESPONSE_INVALID",
-          "An image came back without base64 bytes; this adapter asks for b64_json.",
-        );
-      }
-      return {
-        bytes: decodeBase64(row.b64_json),
-        // 认不出就别猜——留空，由资源那侧按魔数嗅。
-        mediaType: undefined,
-      };
+    const images: ImageOutcome["images"] = rows.map(row => {
+      if (typeof row.b64_json === "string") return { bytes: imageBytes(row.b64_json, limits.maxImageBytes), mediaType: undefined };
+      if (typeof row.url === "string") return { url: row.url, mediaType: undefined };
+      throw createPrologueError("MODEL_RESPONSE_INVALID", "An image response needs bytes or a URL.");
     });
     const revised = rows.find((row) => typeof row.revised_prompt === "string")?.revised_prompt;
     return {
diff --git a/packages/sdk/src/model/plugin/registry.ts b/packages/sdk/src/model/plugin/registry.ts
index acc3e710..cfde5a52 100644
--- a/packages/sdk/src/model/plugin/registry.ts
+++ b/packages/sdk/src/model/plugin/registry.ts
@@ -1,6 +1,7 @@
 import type { ProtocolAdapter } from "../core/adapter.js";
 import type { ImageAdapter } from "../core/image.js";
 import type { SpeechAdapter, TranscriptAdapter } from "../core/speech.js";
+import { GEMINI_IMAGES } from "./gemini-images.js";
 import { OPENAI_IMAGES } from "./openai-images.js";
 import { OPENAI_SPEECH, OPENAI_TRANSCRIPTIONS } from "./openai-speech.js";
 import {
@@ -67,7 +68,7 @@ export const BUILT_IN_ADAPTERS: readonly ProtocolAdapter[] = Object.freeze([
  * 随 SDK 一起发的**出图**协议。和上面那张表各自独立：两者只在「按名字
  * 选」这件事上一样，消息形状、解析和计费单位都不同。
  */
-export const BUILT_IN_IMAGE_ADAPTERS: readonly ImageAdapter[] = Object.freeze([OPENAI_IMAGES]);
+export const BUILT_IN_IMAGE_ADAPTERS: readonly ImageAdapter[] = Object.freeze([OPENAI_IMAGES, GEMINI_IMAGES]);
 
 /**
  * 随 SDK 一起发的**说话**协议。
diff --git a/packages/sdk/src/model/plugin/typesafe-systemone.ts b/packages/sdk/src/model/plugin/typesafe-systemone.ts
new file mode 100644
index 00000000..058175f5
--- /dev/null
+++ b/packages/sdk/src/model/plugin/typesafe-systemone.ts
@@ -0,0 +1,14 @@
+import { createPrologueError } from "../../kernel/core/index.js";
+import type { TypeSafeAdapter, TypeSafeOutcome } from "../core/typesafe.js";
+export const TYPESAFE_SYSTEMONE: TypeSafeAdapter = {
+  buildBody(input) {
+    if (!input.model.trim() || typeof input.state !== "string" || !input.questions || Array.isArray(input.questions) || !Object.keys(input.questions).length) throw createPrologueError("MODEL_REQUEST_INVALID", "A TypeSafe request needs a model, state and native questions.");
+    return new TextEncoder().encode(JSON.stringify({ model: input.model, state: input.state, questions: input.questions }));
+  },
+  readOutcome(raw) {
+    let result: unknown;
+    try { result = JSON.parse(raw); } catch { throw createPrologueError("MODEL_RESPONSE_INVALID", "The judgment response was not JSON."); }
+    if (!result || typeof result !== "object" || !("answers" in result) || !result.answers || typeof result.answers !== "object" || Array.isArray(result.answers)) throw createPrologueError("MODEL_RESPONSE_INVALID", "The judgment response carried no native answers.");
+    return result as TypeSafeOutcome;
+  },
+};
diff --git a/packages/sdk/src/orchestration/api/index.ts b/packages/sdk/src/orchestration/api/index.ts
index c36af404..3a5d56d5 100644
--- a/packages/sdk/src/orchestration/api/index.ts
+++ b/packages/sdk/src/orchestration/api/index.ts
@@ -26,6 +26,9 @@ export {
 export {
   createTaskBoardStore,
   DEFAULT_BOARD_LIMITS,
+  describeAssignee,
+  type BoardAssignee,
+  type HumanPrincipal,
   type BoardLimits,
   type BoardProposal,
   type FailurePolicy,
diff --git a/packages/sdk/src/orchestration/core/index.ts b/packages/sdk/src/orchestration/core/index.ts
index a7dba297..8736f3e8 100644
--- a/packages/sdk/src/orchestration/core/index.ts
+++ b/packages/sdk/src/orchestration/core/index.ts
@@ -43,6 +43,11 @@ export {
 export {
   createTaskBoardStore,
   DEFAULT_BOARD_LIMITS,
+  reportsFor,
+  describeAssignee,
+  type BoardAssignee,
+  type BoardReporter,
+  type HumanPrincipal,
   type BoardLimits,
   type BoardProposal,
   type FailurePolicy,
diff --git a/packages/sdk/src/orchestration/core/subagent.ts b/packages/sdk/src/orchestration/core/subagent.ts
index 350c0b40..6ce212ab 100644
--- a/packages/sdk/src/orchestration/core/subagent.ts
+++ b/packages/sdk/src/orchestration/core/subagent.ts
@@ -49,6 +49,12 @@ export interface SubagentRequest {
    * 在 fork / 子 Provider / 工具之前失败。
    */
   readonly workspace?: string;
+  /**
+   * Steps on a task board the subagent takes on. They pass to the subagent's session before it starts, so only it
+   * reports on them while it runs; whatever it leaves unfinished passes back to the dispatching session when it
+   * ends. Only steps that are unfinished and belong to the dispatching session (or to no one) can be handed over.
+   */
+  readonly claims?: { readonly board: string; readonly nodes: readonly string[] };
 }
 
 /** 受信 App 回收来源用的那一份：展示 id 加真实根引用。 */
diff --git a/packages/sdk/src/orchestration/core/taskboard.ts b/packages/sdk/src/orchestration/core/taskboard.ts
index b4d79a1e..1d946610 100644
--- a/packages/sdk/src/orchestration/core/taskboard.ts
+++ b/packages/sdk/src/orchestration/core/taskboard.ts
@@ -24,6 +24,41 @@ export type NodeState =
   /** 按 `block-for-human` 卡住。**这不是失败，也不是成功。** */
   | "blocked";
 
+/**
+ * A person, as a task's assignee. People are not Runtime objects, so this is a plain App-given identity.
+ */
+export interface HumanPrincipal {
+  readonly kind: "human";
+  readonly id: string;
+}
+
+/**
+ * Who a task belongs to: a role (character), a whole session (including a subagent's own session), or a person.
+ * Only that assignee reports on the task; an unassigned task takes reports from anyone.
+ */
+export type BoardAssignee = ExactRef<"character"> | ExactRef<"session"> | HumanPrincipal;
+
+/** The identity a report is made under. Filled by the layer that knows it, never by the model. */
+export interface BoardReporter {
+  readonly by: ExactRef<"character"> | undefined;
+  readonly session?: ExactRef<"session">;
+  readonly human?: HumanPrincipal;
+}
+
+/** An assignee in a few words, for messages and for reading the board. */
+export function describeAssignee(assignee: BoardAssignee | undefined): string {
+  if (assignee === undefined) return "no one";
+  return `${assignee.kind === "human" ? "person" : assignee.kind} ${assignee.id}`;
+}
+
+/** Whether this reporter is the node's assignee (or the node has none). */
+export function reportsFor(assignee: BoardAssignee | undefined, reporter: BoardReporter): boolean {
+  if (assignee === undefined) return true;
+  if (assignee.kind === "character") return reporter.by?.id === assignee.id;
+  if (assignee.kind === "session") return reporter.session?.id === assignee.id;
+  return reporter.human?.id === assignee.id;
+}
+
 export interface NodeProposal {
   readonly id: string;
   readonly title: string;
@@ -38,7 +73,7 @@ export interface NodeProposal {
    * **不给是合法的**——那是「还没定」，不是「谁都不管」。拆图的人
    * 可以先把步骤和依赖写下来，负责人稍后再定。
    */
-  readonly assignee?: ExactRef<"character">;
+  readonly assignee?: BoardAssignee;
 }
 
 export interface BoardProposal {
@@ -62,6 +97,12 @@ export interface BoardProposal {
 export interface NodeReport {
   /** 谁报的。**没绑档案就是没有人**，不编一个。 */
   readonly by: ExactRef<"character"> | undefined;
+  /** The session the report came from, when the reporter runs in one. */
+  readonly session?: ExactRef<"session">;
+  /** The person who reported, when a person did. */
+  readonly human?: HumanPrincipal;
+  /** Set when this entry records the task passing from one assignee to another. */
+  readonly handover?: { readonly from: BoardAssignee | undefined; readonly to: BoardAssignee | undefined };
   /** 一句有界的说明。**正文不进图**，大正文走资源引用。 */
   readonly note: string;
   readonly atMs: number;
@@ -75,7 +116,7 @@ export interface TaskNode {
   readonly onFailure: FailurePolicy;
   readonly state: NodeState;
   /** 这活归谁。**没定就是 `undefined`**，那时谁都报得了。 */
-  readonly assignee: ExactRef<"character"> | undefined;
+  readonly assignee: BoardAssignee | undefined;
   /** 报上来的那几条，早的在前。条数满了丢最早的。 */
   readonly reports: readonly NodeReport[];
 }
@@ -199,7 +240,26 @@ export interface TaskBoardStore {
     readonly ref: ExactRef<"task-board">;
     readonly expectedVersion: number;
     readonly nodeId: string;
-    readonly assignee: ExactRef<"character"> | undefined;
+    readonly assignee: BoardAssignee | undefined;
+  }): Promise<TaskBoard>;
+  /**
+   * Pass an unfinished task from its assignee to another: to a subagent that takes it on, or back from one that
+   * ended. Unlike `reassign` this works on a task already under way: its state and reports stay as they are, and a
+   * report says who handed it to whom and why, so who did what remains on the record.
+   */
+  handOver(input: {
+    readonly ref: ExactRef<"task-board">;
+    readonly expectedVersion: number;
+    readonly nodeId: string;
+    /** It must still belong to this assignee (`undefined`: to no one); otherwise someone else changed it and nothing happens. */
+    readonly from: BoardAssignee | undefined;
+    readonly to: BoardAssignee | undefined;
+    readonly note: string;
+    readonly atMs: number;
+    /** The person who handed it over, when a person did (through the App); recorded on the handover entry. */
+    readonly human?: HumanPrincipal;
+    /** 不给就用默认上界：说明同样受字数上限、进展条数同样只留最近那些。 */
+    readonly limits?: BoardLimits;
   }): Promise<TaskBoard>;
   /**
    * 在一个节点上报一条进展，可以顺带把状态改掉。
@@ -216,6 +276,15 @@ export interface TaskBoardStore {
     readonly nodeId: string;
     /** 谁报的。**没绑档案就是 `undefined`**，那时报不了别人名下的活。 */
     readonly by: ExactRef<"character"> | undefined;
+    /** The reporter's session, when it runs in one: it can report on tasks assigned to that session. */
+    readonly session?: ExactRef<"session">;
+    /** The reporting person, when a person reports: it can report on tasks assigned to them. */
+    readonly human?: HumanPrincipal;
+    /**
+     * A person's decision on a task someone else holds, recorded by the App: it passes the assignee check and is
+     * recorded under `human`. Only the App's own calls set it; the model's board tools never do.
+     */
+    readonly override?: boolean;
     readonly note: string;
     readonly atMs: number;
     /** 顺带改状态。不给就只留一条进展。 */
@@ -247,12 +316,15 @@ export interface TaskBoardStore {
   subscribe(listener: (event: BoardEvent) => void): () => void;
 }
 
-/** 图上发生了什么。**订阅拿到的就是这个**，有界。 */
-export type BoardEvent =
+/** 图上发生了什么。**订阅拿到的就是这个**，有界。`board` is the id of the board it happened on. */
+export type BoardEvent = BoardChange & { readonly board: string };
+
+type BoardChange =
   | { readonly type: "node-state"; readonly nodeId: string; readonly to: NodeState }
   | { readonly type: "node-added"; readonly nodeId: string }
   | { readonly type: "rewired"; readonly nodeId: string }
   | { readonly type: "reassigned"; readonly nodeId: string }
+  | { readonly type: "handed-over"; readonly nodeId: string }
   /** 有人报了一条。**不带那句说明**——事件里只有身份和状态。 */
   | { readonly type: "node-reported"; readonly nodeId: string }
   | { readonly type: "board-cancelled" };
@@ -354,7 +426,8 @@ export function createTaskBoardStore(
   };
 
   const listeners = new Set<(event: BoardEvent) => void>();
-  const announce = (event: BoardEvent): void => {
+  const announce = (held: Held, change: BoardChange): void => {
+    const event: BoardEvent = { ...change, board: held.ref.id };
     for (const listener of listeners) {
       try {
         listener(event);
@@ -740,7 +813,7 @@ export function createTaskBoardStore(
       normalize(held);
       held.version += 1;
       await save(held);
-      announce({ type: "node-added", nodeId: node.id });
+      announce(held, { type: "node-added", nodeId: node.id });
       return view(held);
     },
 
@@ -795,7 +868,7 @@ export function createTaskBoardStore(
       normalize(held);
       held.version += 1;
       await save(held);
-      announce({ type: "rewired", nodeId });
+      announce(held, { type: "rewired", nodeId });
       return view(held);
     },
 
@@ -820,7 +893,36 @@ export function createTaskBoardStore(
       held.nodes.set(nodeId, { ...node, assignee });
       held.version += 1;
       await save(held);
-      announce({ type: "reassigned", nodeId });
+      announce(held, { type: "reassigned", nodeId });
+      return view(held);
+    },
+
+    async handOver(input): Promise<TaskBoard> {
+      const held = guard(input.ref, input.expectedVersion);
+      const node = held.nodes.get(input.nodeId);
+      if (node === undefined) {
+        throw createPrologueError("TASKBOARD_NOT_FOUND", `This board has no node "${input.nodeId}".`);
+      }
+      if (isTerminal(node.state)) {
+        // A finished task keeps who finished it.
+        throw createPrologueError("TASKBOARD_CONFLICT", `"${node.id}" is already ${node.state}; there is nothing to hand over.`);
+      }
+      const same = (a: BoardAssignee | undefined, b: BoardAssignee | undefined): boolean =>
+        a === undefined || b === undefined ? a === b : a.kind === b.kind && a.id === b.id;
+      if (!same(node.assignee, input.from)) {
+        throw createPrologueError("TASKBOARD_NOT_ASSIGNED", `"${node.id}" now belongs to ${describeAssignee(node.assignee)}, not the one handing it over.`);
+      }
+      const limits = input.limits ?? DEFAULT_BOARD_LIMITS;
+      if (input.note.length < 1 || input.note.length > limits.maxReportChars) {
+        throw createPrologueError("TASKBOARD_PROPOSAL_INVALID",
+          `Handing a task over needs a note saying why, at most ${String(limits.maxReportChars)} characters.`);
+      }
+      const report: NodeReport = { by: undefined, ...(input.human === undefined ? {} : { human: input.human }),
+        handover: { from: input.from, to: input.to }, note: input.note, atMs: input.atMs };
+      held.nodes.set(node.id, { ...node, assignee: input.to, reports: [...node.reports, report].slice(-limits.maxReportsPerNode) });
+      held.version += 1;
+      await save(held);
+      announce(held, { type: "handed-over", nodeId: node.id });
       return view(held);
     },
 
@@ -844,10 +946,13 @@ export function createTaskBoardStore(
        * 没绑档案的那一跑（`by` 是 `undefined`）报不了任何写着名字的
        * 节点：那时根本没有「我是谁」可比，而这一栏正是「这活归谁」。
        */
-      if (node.assignee !== undefined && input.by?.id !== node.assignee.id) {
+      if (input.override === true && input.human === undefined) {
+        throw createPrologueError("TASKBOARD_PROPOSAL_INVALID", "A report over the assignee must say which person decided.");
+      }
+      if (input.override !== true && !reportsFor(node.assignee, input)) {
         throw createPrologueError(
           "TASKBOARD_NOT_ASSIGNED",
-          `"${node.id}" is assigned to someone else; only its assignee reports on it.`,
+          `"${node.id}" is assigned to ${describeAssignee(node.assignee)}; only its assignee reports on it.`,
         );
       }
       if (input.note.length < 1) {
@@ -861,7 +966,8 @@ export function createTaskBoardStore(
             "shorten it, or put the long form behind a resource reference.",
         );
       }
-      const report: NodeReport = { by: input.by, note: input.note, atMs: input.atMs };
+      const report: NodeReport = { by: input.by, ...(input.session === undefined ? {} : { session: input.session }),
+        ...(input.human === undefined ? {} : { human: input.human }), note: input.note, atMs: input.atMs };
       // 满了丢最早的那条。**不是拒绝**——正在干活的那个不该因为
       // 「说过太多次」就再也报不上来。
       const kept = [...node.reports, report].slice(-limits.maxReportsPerNode);
@@ -874,15 +980,18 @@ export function createTaskBoardStore(
        * ready 直接报成功」会被拒绝，却已经把那句说明留在了图上——版本
        * 没加、盘上没有，只有这个进程里多了一条谁也解释不了的进展。
        */
-      if (input.to !== undefined) {
-        moveTo(held, node.id, input.to);
+      // Reporting the state a node is already in ("still running", "still blocked") is progress, not a move:
+      // the note is kept and the state is left alone. Refusing it lost the note and read like a version conflict.
+      const moves = input.to !== undefined && input.to !== node.state;
+      if (moves) {
+        moveTo(held, node.id, input.to as NodeState);
       }
       held.nodes.set(node.id, { ...(held.nodes.get(node.id) as TaskNode), reports: kept });
       held.version += 1;
       await save(held);
-      announce({ type: "node-reported", nodeId: node.id });
-      if (input.to !== undefined) {
-        announce({ type: "node-state", nodeId: node.id, to: input.to });
+      announce(held, { type: "node-reported", nodeId: node.id });
+      if (moves) {
+        announce(held, { type: "node-state", nodeId: node.id, to: input.to as NodeState });
       }
       return view(held);
     },
@@ -904,7 +1013,7 @@ export function createTaskBoardStore(
       }
       held.version += 1;
       await save(held);
-      announce({ type: "board-cancelled" });
+      announce(held, { type: "board-cancelled" });
       return view(held);
     },
 
@@ -958,7 +1067,7 @@ export function createTaskBoardStore(
       held.version += 1;
       // **落下去再返回**：写不下去是稳定错误，不是「改成功了但没存」。
       await save(held);
-      announce({ type: "node-state", nodeId: input.nodeId, to: input.to });
+      announce(held, { type: "node-state", nodeId: input.nodeId, to: input.to });
       return view(held);
     },
   };
diff --git a/packages/sdk/src/runtime/core/config.ts b/packages/sdk/src/runtime/core/config.ts
index 8053d9f1..a796c810 100644
--- a/packages/sdk/src/runtime/core/config.ts
+++ b/packages/sdk/src/runtime/core/config.ts
@@ -1,3 +1,5 @@
+import { DEFAULT_TOOL_LIMITS } from "../../tool/core/index.js";
+import { DEFAULT_IMAGE_LIMITS, type ImageLimits } from "../../model/core/index.js";
 import {
   DEFAULT_BREAKER_LIMITS,
   DEFAULT_DIAGNOSTIC_COMMANDS,
@@ -63,6 +65,8 @@ export interface ModuleConfig {
     compactAboveTokens?: number;
   }>;
   readonly tool?: Readonly<{
+    /** Trusted App ceiling for registered tool deadlines; does not change individual timeouts. */
+    maxTimeoutMs?: number;
     /** 一次工具观察回灌多少。看构建日志要 500 行，问一句 50 行就够。 */
     observation?: Readonly<{ maxLines?: number; maxBytes?: number }>;
     /** 超过这个数就把闭集外的工具**只报名字**，形状等模型来取。 */
@@ -111,6 +115,8 @@ export interface ModuleConfig {
     extraction?: boolean;
   }>;
   readonly model?: Readonly<{
+    images?: Partial<ImageLimits>;
+    maxStructuredResponseBytes?: number;
     /** 一次运行最多花多少 token。不给就是不限。 */
     tokenBudget?: number;
   }>;
@@ -142,6 +148,7 @@ export interface ResolvedConfig {
     compactAboveTokens: number;
   }>;
   readonly tool: Readonly<{
+    maxTimeoutMs: number;
     observation: Readonly<{ maxLines: number; maxBytes: number }>;
     /** 普通提问的存活时间。开跑冻结。 */
     inputWaitTtlMs: number;
@@ -182,7 +189,7 @@ export interface ResolvedConfig {
   readonly source: SourceLimits;
   readonly character: Readonly<{ includeBuiltIns: boolean }>;
   readonly memory: Readonly<{ extraction: boolean }>;
-  readonly model: Readonly<{ tokenBudget: number | undefined }>;
+  readonly model: Readonly<{ tokenBudget: number | undefined; images: ImageLimits; maxStructuredResponseBytes: number }>;
 }
 
 /**
@@ -218,6 +225,7 @@ export const DEFAULT_MODULE_CONFIG: ResolvedConfig = Object.freeze({
     compactAboveTokens: 100_000,
   }),
   tool: Object.freeze({
+    maxTimeoutMs: DEFAULT_TOOL_LIMITS.maxTimeoutMs,
     observation: Object.freeze({ maxLines: 200, maxBytes: 32 * 1024 }),
     // 默认不延后。见上面那段。
     deferToolSchemasBeyond: undefined,
@@ -250,7 +258,7 @@ export const DEFAULT_MODULE_CONFIG: ResolvedConfig = Object.freeze({
   // 默认不自动提炼记忆。
   memory: Object.freeze({ extraction: false }),
   // 不给就是不限。未知用量**不当成零**。
-  model: Object.freeze({ tokenBudget: undefined }),
+  model: Object.freeze({ tokenBudget: undefined, images: DEFAULT_IMAGE_LIMITS, maxStructuredResponseBytes: 4 * 1024 * 1024 }),
 });
 
 /**
@@ -277,6 +285,7 @@ export function resolveModuleConfig(...layers: readonly (ModuleConfig | undefine
       context: Object.freeze({ ...resolved.context, ...strip(layer.context) }),
       tool: Object.freeze({
         ...resolved.tool,
+        maxTimeoutMs: toolTimeout(layer.tool?.maxTimeoutMs) ?? resolved.tool.maxTimeoutMs,
         ...strip({ deferToolSchemasBeyond: layer.tool?.deferToolSchemasBeyond }),
         inputWaitTtlMs: finiteTtl(layer.tool?.inputWaitTtlMs) ?? resolved.tool.inputWaitTtlMs,
         observation: Object.freeze({
@@ -307,7 +316,7 @@ export function resolveModuleConfig(...layers: readonly (ModuleConfig | undefine
       source: Object.freeze({ ...resolved.source, ...strip(layer.source) }),
       character: Object.freeze({ ...resolved.character, ...strip(layer.character) }),
       memory: Object.freeze({ ...resolved.memory, ...strip(layer.memory) }),
-      model: Object.freeze({ ...resolved.model, ...strip(layer.model) }),
+      model: Object.freeze({ ...resolved.model, ...strip(layer.model), images: Object.freeze({ ...resolved.model.images, ...strip(layer.model?.images) }) }),
     });
   }
   return resolved;
@@ -334,3 +343,8 @@ function strip<T extends object>(layer: T | undefined): Partial<T> {
     Object.entries(layer).filter(([, value]) => value !== undefined),
   ) as Partial<T>;
 }
+
+function toolTimeout(value: number | undefined): number | undefined {
+  if (value !== undefined && (!Number.isSafeInteger(value) || value <= 0)) throw new RangeError("tool.maxTimeoutMs must be a positive integer");
+  return value;
+}
diff --git a/packages/sdk/src/session/README.md b/packages/sdk/src/session/README.md
index 8e742ca2..30b5f085 100644
--- a/packages/sdk/src/session/README.md
+++ b/packages/sdk/src/session/README.md
@@ -12,6 +12,8 @@
 
 不发模型请求、不执行工具。Agent 和 Function 请求开始/继续/取消，但不能自己另记一份终态。
 
+Run 启动将端点形状交给 Model 的同一预检窄口，避免与目录登记不一致；预检不替代 Host 网络授权。见 [回环端点合同](../../../../docs/slices/model-loopback.md)。
+
 原问题等待、持久答案、独立Run输入事件与取消/重启的现役消费合同见 [AskUser](../../../../docs/slices/ask-user.md)。该合同不把UI或第二套等待状态搬入模块。
 
 ## 可配与可换
diff --git a/packages/sdk/src/session/core/run.ts b/packages/sdk/src/session/core/run.ts
index f5b7a470..eaf0e597 100644
--- a/packages/sdk/src/session/core/run.ts
+++ b/packages/sdk/src/session/core/run.ts
@@ -10,6 +10,7 @@ import { visibleUnderPrivacy } from "./metadata.js";
 import type { BoundAttachments } from "../../resource/core/index.js";
 import {
   assertParamsSupported,
+  assertModelEndpoint,
   decidePromptCache,
   estimateTokens,
   mergeUsageSnapshot,
@@ -83,9 +84,7 @@ export function assertRunStartable(input: {
   for (const binding of input.start.attachments ?? []) {
     input.assertBindable(binding);
   }
-  if (typeof input.start.endpoint !== "string" || !/^https:\/\//.test(input.start.endpoint)) {
-    throw createPrologueError("RUNTIME_OPTIONS_INVALID", "endpoint must be an https URL.");
-  }
+  assertModelEndpoint(input.start.endpoint);
   if (typeof input.start.model !== "string" || input.start.model.length < 1) {
     throw createPrologueError("RUNTIME_OPTIONS_INVALID", "model is required.");
   }
diff --git a/packages/sdk/src/testing/api/memory-host.ts b/packages/sdk/src/testing/api/memory-host.ts
index 603ec22b..d30c63f1 100644
--- a/packages/sdk/src/testing/api/memory-host.ts
+++ b/packages/sdk/src/testing/api/memory-host.ts
@@ -17,6 +17,7 @@ import {
   HOST_PROTOCOL_MINOR,
   CHANNELS_NEEDING_CREDENTIAL,
   isChannelAllowed,
+  isLoopbackHost,
   type NetworkAllowance,
   patchBaseUnchanged,
 } from "../../host/core/index.js";
@@ -396,6 +397,8 @@ export function createMemoryHost(
     },
     async prepareModelNetwork(input): Promise<ModelNetworkHandle> {
       commandCount += 1;
+      if (input.imageDownloadOf || input.method === "GET") throw createPrologueError("HOST_CAPABILITY_MISSING", "Memory Host image download requires a real Node Host test.");
+      const localNoAuth = input.authScheme === "none" && input.credentialRef === undefined && allowance.loopback === true && isLoopbackHost(new URL(input.endpoint).hostname);
       if (!isChannelAllowed(allowance, input.channel)) {
         throw createPrologueError(
           "NETWORK_CHANNEL_DENIED",
@@ -428,7 +431,7 @@ export function createMemoryHost(
        * 上**永远走不通**的路在替身上是绿的。
        */
       if (input.credentialRef === undefined) {
-        if (CHANNELS_NEEDING_CREDENTIAL.includes(input.channel)) {
+        if (CHANNELS_NEEDING_CREDENTIAL.includes(input.channel) && !localNoAuth) {
           throw createPrologueError(
             "MODEL_NETWORK_FAILED",
             `Egress on the "${input.channel}" channel needs a credential.`,
diff --git a/packages/sdk/src/tool/core/invoke.ts b/packages/sdk/src/tool/core/invoke.ts
index afa0b2b9..2e9a0ea6 100644
--- a/packages/sdk/src/tool/core/invoke.ts
+++ b/packages/sdk/src/tool/core/invoke.ts
@@ -193,6 +193,9 @@ const NO_OPINION: PolicyDecision = Object.freeze({
   why: "The owner of this side effect decides; no rule here names this tool.",
 });
 
+/** One storage segment, short enough to sit after the parent run's key. */
+const IDEMPOTENCY_KEY = /^[A-Za-z0-9][A-Za-z0-9._-]{0,47}$/;
+
 function requiredNames(parameters: Readonly<Record<string, unknown>>): readonly string[] {
   const required = (parameters as { required?: unknown }).required;
   return Array.isArray(required) ? required.filter((name): name is string => typeof name === "string") : [];
@@ -247,6 +250,11 @@ export function createToolInvoker(input: {
    * 不给就是不记归属（没接线的调用方照常过门）。
    */
   readonly origin?: () => EffectOrigin | undefined;
+  /**
+   * Arguments that name something the runtime can check (such as a subagent's character) are checked here,
+   * before any review: otherwise the person approves a call that can only fail afterwards.
+   */
+  readonly precheck?: (name: string, args: Readonly<Record<string, unknown>>) => void;
 }): ToolInvoker {
   let closed = false;
 
@@ -296,6 +304,18 @@ export function createToolInvoker(input: {
         );
       }
     }
+    /*
+     * **幂等键会成为存储地址的一段。** 不合法的键若放过去，要等到落盘才被拒——那时人已经批准了这次
+     * 调用，得到的却是一次失败。在进审查之前就拒，模型当场改一个键重来。
+     */
+    const key = args.idempotencyKey;
+    const properties = (registration.parameters as { properties?: Record<string, unknown> }).properties;
+    if (typeof key === "string" && properties !== undefined && "idempotencyKey" in properties && !IDEMPOTENCY_KEY.test(key)) {
+      throw createPrologueError(
+        "TOOL_ARGUMENTS_INVALID",
+        `The "${registration.name}" tool needs an idempotencyKey of 1–48 letters, digits, dots, dashes or underscores, starting with a letter or digit.`,
+      );
+    }
     const allowed = allowedNames(registration.parameters);
     if (allowed !== undefined) {
       const extra = Object.keys(args).filter((name) => !allowed.includes(name));
@@ -388,10 +408,18 @@ export function createToolInvoker(input: {
     }
     const { registration } = resolve(request);
     checkArgs(registration, request.args);
+    input.precheck?.(registration.name, request.args);
 
     // Read effects need no mutation approval, but App data-scope hooks still
     // apply. Skipping these leaks another session's board through board-read.
     if (registration.effectKind === "safe-read") {
+      const origin = input.origin?.();
+      const decision = input.effects.evaluate({ kind: "safe-read", subject: { what: "tool", name: registration.name, input: JSON.stringify(request.args) },
+        summary: registration.name, inputFingerprint: registration.name,
+        ...(origin === undefined ? {} : { origin }) });
+      if (decision.outcome === "deny" && decision.reason !== "default-deny") {
+        throw createPrologueError("EFFECT_NOT_AUTHORIZED", `Policy blocked "${registration.name}". Nothing was read.`);
+      }
       let args = request.args;
       if (input.hooks !== undefined) {
         const origin = input.origin?.();
diff --git a/packages/sdk/src/tool/core/system.ts b/packages/sdk/src/tool/core/system.ts
index 8ba1b112..52d166be 100644
--- a/packages/sdk/src/tool/core/system.ts
+++ b/packages/sdk/src/tool/core/system.ts
@@ -167,7 +167,14 @@ const BLUEPRINTS: readonly Blueprint[] = [
   {
     name: "list",
     description: "List files and directories under a relative path in the authorized project.",
-    parameters: relativePath(false, "Relative directory path. Defaults to the project root."),
+    parameters: {
+      type: "object",
+      properties: {
+        path: { type: "string", description: "Relative directory path. Defaults to the project root." },
+        workspace: { type: "string", description: "Optional: the id of a subtask workspace this run was granted to read. Leave out for this run's own workspace." },
+      },
+      additionalProperties: false,
+    },
     effectKind: "safe-read",
     needs: "workspace",
   },
@@ -178,7 +185,8 @@ const BLUEPRINTS: readonly Blueprint[] = [
       type: "object",
       properties: {
         query: { type: "string", description: "Substring to find." },
-        path: { type: "string", description: "Optional relative directory to search in." },
+        path: { type: "string", description: "Optional relative directory or single file to search in." },
+        workspace: { type: "string", description: "Optional: the id of a subtask workspace this run was granted to read. Leave out for this run's own workspace." },
       },
       required: ["query"],
       additionalProperties: false,
@@ -188,8 +196,19 @@ const BLUEPRINTS: readonly Blueprint[] = [
   },
   {
     name: "read",
-    description: "Read a UTF-8 text file at a relative path.",
-    parameters: relativePath(true, "Relative file path."),
+    description:
+      "Read a UTF-8 text file at a relative path. Files over 64 KB cannot be read whole: pass offset and limit to read a window of lines.",
+    parameters: {
+      type: "object",
+      properties: {
+        path: { type: "string", description: "Relative file path." },
+        offset: { type: "integer", minimum: 1, description: "Optional first line to read (1-based)." },
+        limit: { type: "integer", minimum: 1, maximum: 2000, description: "Optional number of lines to read from offset (at most 2000)." },
+        workspace: { type: "string", description: "Optional: the id of a subtask workspace this run was granted to read. Leave out for this run's own workspace." },
+      },
+      required: ["path"],
+      additionalProperties: false,
+    },
     effectKind: "safe-read",
     needs: "workspace",
   },  {
@@ -342,7 +361,10 @@ const BLUEPRINTS: readonly Blueprint[] = [
             "Exactly which tools the subagent may use. An empty list means none; " +
             "omitting it does not inherit this run's tools.",
         },
-        idempotencyKey: { type: "string", description: "Same key and same request dispatches once." },
+        idempotencyKey: {
+          type: "string",
+          description: "Same key and same request dispatches once. 1–48 letters, digits, dots, dashes or underscores.",
+        },
         character: {
           type: "string",
           description:
@@ -367,6 +389,19 @@ const BLUEPRINTS: readonly Blueprint[] = [
             "Optional id of a workspace this run was granted. Choose only from those ids; " +
             "do not pass a path, root reference, or authorization data.",
         },
+        claims: {
+          type: "object",
+          description:
+            "Optional steps on a task board this subagent takes on. They become its own: only it reports on them " +
+            "until it ends, and whatever it leaves unfinished comes back to you. Only unfinished steps that are " +
+            "yours or no one's can be handed over.",
+          properties: {
+            board: { type: "string", description: "The board id." },
+            nodes: { type: "array", items: { type: "string" }, description: "The step ids." },
+          },
+          required: ["board", "nodes"],
+          additionalProperties: false,
+        },
       },
       required: ["instruction", "tools", "idempotencyKey"],
       additionalProperties: false,
@@ -552,7 +587,7 @@ const BLUEPRINTS: readonly Blueprint[] = [
           enum: ["notice", "request", "reply", "handoff", "status", "cancel-request", "inject"],
         },
         body: { type: "string", description: "Bounded text. No secrets, no absolute paths." },
-        idempotencyKey: { type: "string" },
+        idempotencyKey: { type: "string", description: "1–48 letters, digits, dots, dashes or underscores." },
       },
       required: ["to", "kind", "body", "idempotencyKey"],
       additionalProperties: false,
@@ -595,7 +630,7 @@ const BLUEPRINTS: readonly Blueprint[] = [
       properties: {
         session: { type: "string" },
         text: { type: "string" },
-        idempotencyKey: { type: "string" },
+        idempotencyKey: { type: "string", description: "1–48 letters, digits, dots, dashes or underscores." },
       },
       required: ["session", "text", "idempotencyKey"],
       additionalProperties: false,
diff --git a/packages/sdk/src/workspace/core/mutate.ts b/packages/sdk/src/workspace/core/mutate.ts
index 6d5ce28a..a8676b7d 100644
--- a/packages/sdk/src/workspace/core/mutate.ts
+++ b/packages/sdk/src/workspace/core/mutate.ts
@@ -41,7 +41,7 @@ export interface MutationLimits {
   readonly mayMutateFiles: boolean;
   readonly mayRunCommands: boolean;
 }
-import { isReadOnlyMetadata, type AuthorizedRoot } from "./root.js";
+import { isReadOnlyMetadata, MAX_LARGE_READ, type AuthorizedRoot } from "./root.js";
 
 /**
  * 一次可见的补丁提案。**准备阶段磁盘写入为零**——它只读当前内容、
@@ -479,7 +479,8 @@ export function createWorkspaceMutator(input: {
       let baseExists = false;
       let baseDigest: string | undefined;
       try {
-        const held = await input.host.readFile({ rootRef, path });
+        // The base is only diffed, never shown whole, so a file up to 2MB can be changed.
+        const held = await input.host.readFile({ rootRef, path, maxBytes: MAX_LARGE_READ });
         baseText = held.text;
         baseDigest = held.digest;
         baseExists = true;
diff --git a/packages/sdk/src/workspace/core/root.ts b/packages/sdk/src/workspace/core/root.ts
index 2dc31092..e3b4691a 100644
--- a/packages/sdk/src/workspace/core/root.ts
+++ b/packages/sdk/src/workspace/core/root.ts
@@ -69,7 +69,8 @@ export interface WorkspaceReader {
      * 灾难性回溯的模式会被拒——见 `assertPatternIsSafe`。
      */
     readonly regex?: boolean;
-  }): Promise<{ readonly matches: readonly WorkspaceMatch[] }>;
+    /** `truncated`：没有搜全（走到文件数或条数上界）。没找到时据此说明，而不是说不存在。 */
+  }): Promise<{ readonly matches: readonly WorkspaceMatch[]; readonly truncated?: boolean }>;
   /**
    * 在授权根里**按名字**找路径。受忽略规则约束。
    *
@@ -105,6 +106,8 @@ export interface WorkspaceReader {
      * 过忽略规则，只是不再叠上工作目录那一段。
      */
     readonly fromRoot?: boolean;
+    /** 为编辑读整份：正文只用来生成差异、不进模型上下文，所以可超过 64KB（最多 2MB）。 */
+    readonly forEdit?: boolean;
   }): Promise<WorkspaceRead>;
 }
 
@@ -208,7 +211,7 @@ export interface Workspace {
    * `.env` 会大摇大摆地进列表、进检索、进模型上下文。硬敏感名单在
    * 规则那边是配不掉的一档，这里只是把它真的用上。
    */
-  reader(ref: ExactRef<"authorized-root">, ignore: IgnoreRules): WorkspaceReader;
+  reader(ref: ExactRef<"authorized-root">, ignore: IgnoreRules, options?: { readonly observe?: boolean }): WorkspaceReader;
   /**
    * 这一份根上「看过什么」的账。
    *
@@ -413,7 +416,7 @@ export function createWorkspace(input: {
       return { from, to };
     },
 
-    reader(ref, ignore): WorkspaceReader {
+    reader(ref, ignore, options): WorkspaceReader {
       const entry = load(ref);
       const observed = entry.observed;
       const at = (path?: string): string => {
@@ -440,7 +443,7 @@ export function createWorkspace(input: {
           }
           if (regex !== true) {
             const found = await input.host.searchFiles({ rootRef: ref, query, path: at(path) });
-            return { matches: found.matches.filter((one) => ignore.isVisible(rel(one.path))) };
+            return { matches: found.matches.filter((one) => ignore.isVisible(rel(one.path))), ...(found.truncated ? { truncated: true } : {}) };
           }
           /*
            * **正则这一档自己扫。**
@@ -453,8 +456,18 @@ export function createWorkspace(input: {
            */
           const pattern = new RegExp(query, "s");
           const candidates = await this.find({ glob: "**", ...(path === undefined ? {} : { path }) });
+          const files = [...candidates.paths];
+          // 路径指的是单个文件时，find 只会列目录、找不到候选：这份文件本身就是唯一候选。
+          if (path !== undefined && files.length === 0 && ignore.isVisible(rel(path))) {
+            try {
+              await input.host.readFile({ rootRef: ref, path: at(path) });
+              files.push(rel(path));
+            } catch {
+              // 不是可读的文件（目录、不存在、二进制）：照旧没有候选。
+            }
+          }
           const matches: WorkspaceMatch[] = [];
-          for (const where of candidates.paths.slice(0, MAX_SEARCH_FILES)) {
+          for (const where of files.slice(0, MAX_SEARCH_FILES)) {
             if (matches.length >= MAX_SEARCH_MATCHES) {
               break;
             }
@@ -482,7 +495,8 @@ export function createWorkspace(input: {
               }
             }
           }
-          return { matches };
+          const truncated = candidates.truncated || files.length > MAX_SEARCH_FILES || matches.length >= MAX_SEARCH_MATCHES;
+          return { matches, ...(truncated ? { truncated: true } : {}) };
         },
 
         async find({ glob, path }) {
@@ -525,7 +539,7 @@ export function createWorkspace(input: {
           };
         },
 
-        async read({ path, lines, fromRoot }) {
+        async read({ path, lines, fromRoot, forEdit }) {
           const why = ignore.hiddenBecause(rel(path));
           if (why !== undefined) {
             /*
@@ -538,9 +552,12 @@ export function createWorkspace(input: {
           }
           // `fromRoot` 只是不叠工作目录那一段。规范化、拒 `..`、拒绝对
           // 路径、过忽略规则，一样都没少。
+          // A line window or an edit never puts the whole text in front of the model, so it may read past the
+          // 64KB whole-read cap (up to 2MB); a plain whole read keeps the cap.
           const held = await input.host.readFile({
             rootRef: ref,
             path: fromRoot === true ? rel(path) : at(path),
+            ...(lines !== undefined || forEdit === true ? { maxBytes: MAX_LARGE_READ } : {}),
           });
           /*
            * 记下**看到的是哪一版**。
@@ -551,7 +568,9 @@ export function createWorkspace(input: {
            * **只记整份读**：取了一段行窗的那次，调用方看到的不是整个
            * 文件，拿它当「看过了」就是骗自己。
            */
-          if (lines === undefined) {
+          // A look from outside the root (a parent checking a child's workspace) is not this root's own read:
+          // it must not stand in for the read that an edit here requires.
+          if (lines === undefined && options?.observe !== false) {
             observed.note(fromRoot === true ? rel(path) : at(path), held.digest);
           }
           return lines === undefined ? { ...held, truncated: false } : sliceLines(held, lines);
@@ -634,6 +653,8 @@ export function sliceLines(
 
 /** 一次按名字找最多回多少条。**有界**，不是把整棵树倒出来。 */
 export const MAX_FIND = 200;
+/** 行窗与编辑读取的字节上限（整份读仍是 64KB）。 */
+export const MAX_LARGE_READ = 2 * 1024 * 1024;
 /** 正则检索最多读几份文件。**有界**——这一档是逐份读的。 */
 export const MAX_SEARCH_FILES = 200;
 /** 正则检索最多回几条。 */
diff --git a/packages/sdk/test/agent-native-continuation.test.ts b/packages/sdk/test/agent-native-continuation.test.ts
index f7cb38ec..98e67a47 100644
--- a/packages/sdk/test/agent-native-continuation.test.ts
+++ b/packages/sdk/test/agent-native-continuation.test.ts
@@ -676,3 +676,52 @@ describe("同 Run 原生响应续接", () => {
     await started.runtime.shutdown();
   });
 });
+
+describe("回答被输出上限截断时接着写", () => {
+  const cut = (text: string) => nativeToolFrames({ text, tools: [], stopReason: "max_tokens" });
+  const end = (text: string) => nativeToolFrames({ text, tools: [], stopReason: "end_turn" });
+  const thoughtOut = () => nativeToolFrames({ thinkingParts: [THINK_A], signatureParts: [SIG_A], tools: [], stopReason: "max_tokens" });
+  const written = (seen: readonly ModelEvent[]) =>
+    seen.filter((one) => one.type === "text-delta").map((one) => (one as { text: string }).text).join("");
+  const code = (event: ModelEvent) => (event as { error?: { code?: string } }).error?.code;
+
+  it("截下来的那段留在历史里，续写到写完为止，几段连成一条完整回答", async () => {
+    const probe: MemoryHostPrepareProbe = {};
+    const started = await boot({ probe, turns: [{ frames: cut("第一段，") }, { frames: cut("第二段，") }, { frames: end("写完了。") }] });
+    expect((await started.wait()).type).toBe("completed");
+    expect(written(started.seen)).toBe("第一段，第二段，写完了。");
+    const [, second, third] = probe.bodies ?? [];
+    expect(second).toContain("第一段，");
+    expect(second).toContain("cut off at the output limit");
+    expect(third).toContain("第一段，");
+    expect(third).toContain("第二段，");
+    expect(started.seen.filter((one) => one.type === "model-response-repair" && one.reason === "output-truncated")).toHaveLength(2);
+    await started.runtime.shutdown();
+  });
+
+  it("续写到上界还在被截断：如实失败，不把半截当成完成", async () => {
+    const probe: MemoryHostPrepareProbe = {};
+    const started = await boot({ probe, turns: Array.from({ length: 9 }, (_, index) => ({ frames: cut(`第${index + 1}段，`) })) });
+    const terminal = await started.wait();
+    expect(terminal.type).toBe("failed");
+    expect(code(terminal)).toBe("MODEL_OUTPUT_TRUNCATED");
+    expect(probe.bodies).toHaveLength(9);
+    await started.runtime.shutdown();
+  });
+
+  it("思考用完上限、还没写正文：让它少想一点再答；一直如此就失败，不交空回答", async () => {
+    const recovered: MemoryHostPrepareProbe = {};
+    const once = await boot({ probe: recovered, params: { thinking: "adaptive" }, turns: [{ frames: thoughtOut() }, { frames: end("答案。") }] });
+    expect((await once.wait()).type).toBe("completed");
+    expect(written(once.seen)).toBe("答案。");
+    expect(recovered.bodies?.[1]).toContain("Keep your reasoning shorter");
+    await once.runtime.shutdown();
+
+    const probe: MemoryHostPrepareProbe = {};
+    const started = await boot({ probe, params: { thinking: "adaptive" }, turns: [{ frames: thoughtOut() }, { frames: thoughtOut() }, { frames: thoughtOut() }] });
+    const terminal = await started.wait();
+    expect(terminal.type).toBe("failed");
+    expect(code(terminal)).toBe("MODEL_OUTPUT_TRUNCATED");
+    await started.runtime.shutdown();
+  });
+});
diff --git a/packages/sdk/test/agent-scopes.live.test.ts b/packages/sdk/test/agent-scopes.live.test.ts
new file mode 100644
index 00000000..69d5d723
--- /dev/null
+++ b/packages/sdk/test/agent-scopes.live.test.ts
@@ -0,0 +1,162 @@
+import { mkdirSync, mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
+import { createServer, type ServerResponse } from "node:http";
+import { tmpdir } from "node:os";
+import { join } from "node:path";
+import { expect, it } from "vitest";
+import { createRuntime, OFFICIAL_CODING_PACK, type CreateRuntimeInput, type ModelEvent, type Run, type ScenarioPack } from "../src/index.js";
+import { createNodeHost } from "../src/node.js";
+
+function end(run: Run) {
+  return new Promise<ModelEvent[]>((resolve, reject) => {
+    const events: ModelEvent[] = [];
+    const timer = setTimeout(() => reject(new Error(`Run did not finish: ${events.map(e => e.type).join(",")}`)), 8000);
+    run.subscribe(event => { events.push(event); if (["completed", "failed", "cancelled"].includes(event.type)) { clearTimeout(timer); resolve(events); } });
+  });
+}
+function say(response: ServerResponse, text = "answered") {
+  response.writeHead(200, { "content-type": "text/event-stream" });
+  response.end(`data: ${JSON.stringify({ model: "provider-model", choices: [{ delta: { content: text } }], usage: { prompt_tokens: 7, completion_tokens: 3 } })}\n\ndata: [DONE]\n\n`);
+}
+function call(response: ServerResponse, name: string, args: object) {
+  response.writeHead(200, { "content-type": "text/event-stream" });
+  response.end(`data: ${JSON.stringify({ usage: { prompt_tokens: 7, completion_tokens: 3 }, choices: [{ delta: { tool_calls: [{ index: 0, id: "c1", type: "function", function: { name, arguments: JSON.stringify(args) } }] } }] })}\n\ndata: [DONE]\n\n`);
+}
+async function fixture(handler: (body: any, response: ServerResponse, turn: number) => void, over: Partial<CreateRuntimeInput> = {}) {
+  const home = mkdtempSync(join(tmpdir(), "prologue-agent-scopes-"));
+  const workspaceCalls: string[] = [];
+  const requests: any[] = [];
+  const turns = new Map<string, number>();
+  const server = createServer(async (request, response) => {
+    let raw = ""; for await (const chunk of request) raw += chunk;
+    const body = JSON.parse(raw); requests.push(body);
+    const turn = turns.get(body.model) ?? 0; turns.set(body.model, turn + 1);
+    handler(body, response, turn);
+  });
+  await new Promise<void>(resolve => server.listen(0, "127.0.0.1", resolve));
+  const address = server.address(); if (!address || typeof address === "string") throw new Error("missing endpoint");
+  let dispatchGuards = 0;
+  const actual = createNodeHost({ storageRoot: join(home, "store"), beforeModelDispatch: async () => { dispatchGuards += 1; } });
+  const methods = new Set(["authorizeRoot", "listDirectory", "searchFiles", "readFile", "readBinary", "writeFile", "applyPatch", "prepareCommand", "runCommand"]);
+  const host = new Proxy(actual, { get(target, key, receiver) { const value = Reflect.get(target, key, receiver); return typeof value !== "function" ? value : (...args: unknown[]) => { if (methods.has(String(key))) workspaceCalls.push(String(key)); return value.apply(target, args); }; } });
+  const runtime = await createRuntime({ app: { appId: "prologue.agent-scopes", appVersion: "1.0.0" }, host, preset: "local-agent",
+    network: { model: true, loopback: true }, posture: { sandbox: "workspace-write" }, permissionMode: { mode: "ask-always" }, ...over });
+  const credentialRef = (await runtime.credentials.write({ label: "test", secret: { plaintext: new TextEncoder().encode("synthetic") } })).ref;
+  const start = (model: string) => ({ protocol: "openai-compatible", endpoint: `http://127.0.0.1:${address.port}/chat`, credentialRef, model, messages: [{ role: "user" as const, text: `task ${model}` }] });
+  const root = async (name: string, posture: "read-only" | "workspace-write" = "workspace-write") => { const path = join(home, name); mkdirSync(path); writeFileSync(join(path, "draft.txt"), "before"); return { path, grant: await runtime.workspace.authorize({ path, posture }) }; };
+  return { runtime, home, workspaceCalls, requests, start, root, guardCalls: () => dispatchGuards, async close() { await runtime.shutdown(); server.closeAllConnections(); await new Promise<void>(resolve => server.close(() => resolve())); rmSync(home, { recursive: true, force: true }); } };
+}
+
+it("workspace:none preserves fixed Character, materials, history, model usage and durable Session with zero workspace calls", async () => {
+  const f = await fixture((_body, response, turn) => say(response, turn === 0 ? "first answer" : "second answer"));
+  try {
+    const session = await f.runtime.sessions.create();
+    const character = f.runtime.characters.create({ id: "lia", version: 1, name: "Lia", role: "Fixed warm persona", tools: [] });
+    f.runtime.characters.publish(character.ref);
+    const bytes = new TextEncoder().encode("FIXED MATERIAL");
+    const stage = f.runtime.resources.stage({ mediaKind: "text", byteLength: bytes.length, label: "note" }); stage.write(bytes); const material = stage.publish().ref;
+    for (const key of ["one", "two"]) {
+      const started = await f.runtime.startAgentRun({ session, workspace: "none", start: f.start("assistant"), history: "session",
+        agent: { idempotencyKey: key, toolNames: [], characterRef: character.ref, mount: { textResources: [{ ref: material, as: "original" }] } } });
+      const events = await end(started.run); expect(events.at(-1)?.type).toBe("completed");
+      expect(events.some(event => event.type === "usage-recorded")).toBe(true);
+      expect(started.pack.items.some(item => item.text.includes("Fixed warm persona"))).toBe(true);
+    }
+    expect(f.workspaceCalls).toEqual([]);
+    expect(f.requests.every(request => !request.tools?.length)).toBe(true);
+    expect(JSON.stringify(f.requests[1])).toContain("first answer");
+    expect(JSON.stringify(f.requests[1])).toContain("FIXED MATERIAL");
+    expect(f.runtime.usage.spentTokens).toBe(20);
+    expect(f.guardCalls()).toBe(2);
+    expect((await session.conversation()).turns).toHaveLength(2);
+  } finally { await f.close(); }
+});
+
+it("workspace:none rejects tools, roots, workspace context, subagents and contributed Session pack before network", async () => {
+  const f = await fixture((_body, response) => say(response));
+  try {
+    const session = await f.runtime.sessions.create();
+    const base = { session, workspace: "none" as const, start: f.start("reject"), agent: { idempotencyKey: "reject", toolNames: [] } };
+    for (const change of [{ agent: { idempotencyKey: "missing" } }, { agent: { idempotencyKey: "tool", toolNames: ["read"] } },
+      { context: { projectTrusted: true } }, { subagents: {} }, { rootRef: { kind: "authorized-root", id: "fake", revision: 1 } },
+      { runTool: async () => "forbidden" }, { grants: { toolNames: [], paths: ["."] } },
+      { agent: { idempotencyKey: "directory", toolNames: [], workingDirectory: "." } }]) {
+      await expect(f.runtime.startAgentRun({ ...base, ...change } as never)).rejects.toMatchObject({ code: "AGENT_START_INVALID" });
+    }
+    const packed = await f.runtime.sessions.create({ pack: OFFICIAL_CODING_PACK, executors: Object.fromEntries(OFFICIAL_CODING_PACK.needs.executors.map(name => [name, async () => "unused"])) });
+    await expect(f.runtime.startAgentRun({ ...base, session: packed })).rejects.toMatchObject({ code: "AGENT_START_INVALID" });
+    await expect(f.runtime.startAgentRun({ session, start: f.start("default"), agent: { idempotencyKey: "default" } } as never)).rejects.toMatchObject({ code: "ROOT_NOT_FOUND" });
+    expect(f.workspaceCalls).toEqual([]); expect(f.requests).toEqual([]);
+  } finally { await f.close(); }
+});
+
+it("workspace:none keeps token budgets and cancellation on the same loop", async () => {
+  let arrived!: () => void; const entered = new Promise<void>(resolve => { arrived = resolve; });
+  const f = await fixture((body, response) => { if (body.model === "hang") { response.writeHead(200, { "content-type": "text/event-stream" }); response.write(": waiting\n\n"); arrived(); } else call(response, "read", { path: "forbidden.txt" }); });
+  try {
+    const budget = await f.runtime.startAgentRun({ session: await f.runtime.sessions.create(), workspace: "none", start: f.start("budget"), agent: { idempotencyKey: "budget", toolNames: [], budget: { maxTokens: 1 } } });
+    expect((await end(budget.run)).at(-1)).toMatchObject({ type: "failed", error: { code: "AGENT_BUDGET_EXCEEDED" } });
+    const hanging = await f.runtime.startAgentRun({ session: await f.runtime.sessions.create(), workspace: "none", start: f.start("hang"), agent: { idempotencyKey: "cancel", toolNames: [] } });
+    const ended = end(hanging.run); await entered; await hanging.run.cancel(); expect((await ended).at(-1)?.type).toBe("cancelled");
+    expect(f.workspaceCalls).toEqual([]);
+  } finally { await f.close(); }
+});
+
+it("a Builder grants only its own concurrent Session/root; Coding still awaits approval", async () => {
+  const f = await fixture((_body, response, turn) => turn === 0 ? call(response, "read", { path: "draft.txt" }) : turn === 1 ? call(response, "write", { path: "draft.txt", text: "after" }) : say(response));
+  try {
+    const builder = await f.runtime.sessions.create(), coding = await f.runtime.sessions.create();
+    const a = await f.root("builder"), b = await f.root("coding");
+    for (const path of ["nested", "../escape", "/absolute"]) {
+      await expect(f.runtime.startAgentRun({ session: builder, rootRef: a.grant.ref, grants: { toolNames: ["write"], paths: [path] }, start: f.start("invalid-path"), agent: { idempotencyKey: "invalid-path", mode: "build", toolNames: ["write"] } })).rejects.toMatchObject({ code: "AGENT_START_INVALID" });
+    }
+    const grants = { toolNames: ["read", "write"], paths: ["."] };
+    const launch = f.runtime.startAgentRun({ session: builder, rootRef: a.grant.ref, grants, start: f.start("builder"), agent: { idempotencyKey: "builder", mode: "build", toolNames: ["read", "write"] } });
+    grants.toolNames.length = 0; grants.paths.length = 0;
+    const startedA = await launch;
+    const startedB = await f.runtime.startAgentRun({ session: coding, rootRef: b.grant.ref, start: f.start("coding"), agent: { idempotencyKey: "coding", mode: "build", toolNames: ["read", "write"] } });
+    const bEnd = end(startedB.run);
+    const eventsA = await end(startedA.run); expect(eventsA.at(-1)?.type).toBe("completed");
+    expect(readFileSync(join(a.path, "draft.txt"), "utf8")).toBe("after");
+    expect(readFileSync(join(b.path, "draft.txt"), "utf8")).toBe("before");
+    expect(startedB.run.state).not.toBe("completed");
+    const recovery = await f.runtime.effects.readRecovery();
+    const scoped = recovery.effects.find(effect => effect.proposal.origin?.session === builder.ref.id)!;
+    expect(scoped).toBeDefined();
+    await expect(f.runtime.effects.prepare(scoped.proposal, await f.runtime.readClock())).rejects.toMatchObject({ code: "EFFECT_NOT_AUTHORIZED" });
+    await expect(f.runtime.effects.dispatch(scoped.ref, { recheck: () => true, run: async () => { throw new Error("must not dispatch"); } })).rejects.toMatchObject({ code: "EFFECT_NOT_AUTHORIZED" });
+    expect(f.runtime.effects.evaluate({ ...scoped.proposal, inputFingerprint: "origin-spoof" }).outcome).not.toBe("allow");
+    await startedB.run.cancel(); await bEnd;
+  } finally { await f.close(); }
+});
+
+for (const blocker of ["policy", "hook", "sandbox", "escape"] as const) it(`Builder grants cannot bypass ${blocker}`, async () => {
+  const f = await fixture((_body, response, turn) => turn === 0 ? call(response, "read", { path: "draft.txt" }) : turn === 1 ? call(response, "write", { path: blocker === "escape" ? "../outside.txt" : "draft.txt", text: "after" }) : say(response),
+    blocker === "policy" ? { rules: [{ source: "org", effect: "deny", match: { what: "path", under: ["draft.txt"] } }] } : {});
+  try {
+    const session = await f.runtime.sessions.create(); const root = await f.root("builder", blocker === "sandbox" ? "read-only" : "workspace-write");
+    if (blocker === "hook") f.runtime.hooks.register({ id: "deny-write", event: "tool-before", forSession: session.ref.id, handler: async event => event.toolName === "write" ? { kind: "deny", why: "outside allowed builder files" } : { kind: "allow" } });
+    const started = await f.runtime.startAgentRun({ session, rootRef: root.grant.ref, grants: { toolNames: ["read", "write"], paths: ["."] }, start: f.start(blocker), agent: { idempotencyKey: blocker, mode: "build", toolNames: ["read", "write"] } });
+    await end(started.run);
+    expect(readFileSync(join(root.path, "draft.txt"), "utf8")).toBe("before");
+  } finally { await f.close(); }
+});
+
+it("two simultaneous granted Sessions retain separate pack executors and effect receipts", async () => {
+  const f = await fixture((_body, response, turn) => turn === 0 ? call(response, "builder_checks", {}) : say(response));
+  try {
+    const pack: ScenarioPack = { id: "builder", version: "1.0.0", source: { kind: "app-embedded" }, permissions: { tools: ["builder.checks"], network: [], paths: [] },
+      memory: { scope: "session", write: "deny" }, needs: { hostCapabilities: [], executors: ["checks"] }, roster: [{ role: "builder", skills: [], writes: true }],
+      planning: { plannedBy: "builder", planFirst: false }, config: {},
+      tools: [{ executor: "checks", registration: { name: "builder.checks", version: "1", description: "Run builder checks", parameters: { type: "object", properties: {}, additionalProperties: false }, effectKind: "mutate-local", timeoutMs: 1000, idempotency: "pure" } }] };
+    const calls: string[] = [];
+    const sessions = await Promise.all(["a", "b"].map(name => f.runtime.sessions.create({ pack, executors: { checks: async () => { calls.push(name); return `checked ${name}`; } } })));
+    const roots = await Promise.all([f.root("a"), f.root("b")]);
+    const started = await Promise.all(sessions.map((session, index) => f.runtime.startAgentRun({ session, rootRef: roots[index]!.grant.ref, grants: { toolNames: ["builder.checks"], paths: ["."] }, start: f.start(`plugin${index}`), agent: { idempotencyKey: `plugin${index}`, mode: "build", toolNames: ["builder.checks"] } })));
+    const results = await Promise.all(started.map(item => end(item.run)));
+    expect(results.every(events => events.at(-1)?.type === "completed")).toBe(true);
+    expect(calls.sort()).toEqual(["a", "b"]);
+    const effects = (await f.runtime.effects.readRecovery()).effects.filter(effect => effect.proposal.subject.what === "tool");
+    expect(effects).toHaveLength(2); expect(new Set(effects.map(effect => effect.proposal.origin?.session)).size).toBe(2);
+    expect(effects.every(effect => effect.state === "completed")).toBe(true);
+  } finally { await f.close(); }
+});
diff --git a/packages/sdk/test/board-claims.test.ts b/packages/sdk/test/board-claims.test.ts
new file mode 100644
index 00000000..31e75198
--- /dev/null
+++ b/packages/sdk/test/board-claims.test.ts
@@ -0,0 +1,187 @@
+import { describe, expect, it } from "vitest";
+import { createRuntime, type ModelEvent } from "../src/index.js";
+import { createTaskBoardStore, reportsFor } from "../src/orchestration/core/index.js";
+import { mintRef } from "../src/kernel/core/index.js";
+import { createMemoryHost, type MemoryHostPrepareProbe } from "../src/testing/api/index.js";
+
+const APP = { appId: "prologue.harness", appVersion: "0.1.0" };
+
+function call(id: string, name: string, args: unknown): { readonly frames: readonly string[] } {
+  return {
+    frames: [
+      `data: {"type":"content_block_start","content_block":{"type":"tool_use","id":"${id}","name":"${name}","input":${JSON.stringify(args)}}}\n`,
+      'data: {"type":"message_stop"}\n',
+    ],
+  };
+}
+
+function say(text: string): { readonly frames: readonly string[] } {
+  return {
+    frames: [
+      `data: {"type":"content_block_delta","delta":{"type":"text_delta","text":${JSON.stringify(text)}}}\n`,
+      'data: {"type":"message_stop"}\n',
+    ],
+  };
+}
+
+function toolNamesIn(body: string): readonly string[] {
+  return ((JSON.parse(body) as { readonly tools?: readonly { readonly name: string }[] }).tools ?? []).map((tool) => tool.name);
+}
+
+describe("任务的负责人可以是角色、会话或人", () => {
+  it("只有负责人报得了；交接不改状态和进展，只换负责人并留一条记录", async () => {
+    let n = 0;
+    const boards = createTaskBoardStore(() => `b${String(++n)}`);
+    const events: Array<{ type: string; board: string }> = [];
+    boards.subscribe((event) => events.push({ type: event.type, board: event.board }));
+    const session = mintRef("session", "s-1");
+    const other = mintRef("session", "s-2");
+    const person = { kind: "human", id: "u-1" } as const;
+    const held = await boards.admit({ nodes: [
+      { id: "mine", title: "session step", dependsOn: [], onFailure: "block-for-human", assignee: session },
+      { id: "theirs", title: "person step", dependsOn: [], onFailure: "block-for-human", assignee: person },
+      { id: "open", title: "no one's", dependsOn: [], onFailure: "block-for-human" },
+    ] });
+    const report = (node: string, who: Record<string, unknown>) =>
+      boards.report({ ref: held.ref, expectedVersion: boards.get(held.ref).version, nodeId: node, by: undefined, ...who, note: "n", atMs: 1, to: "running" });
+    await expect(report("mine", { session: other })).rejects.toThrow(/assigned to session s-1/);
+    await expect(report("mine", { human: person })).rejects.toThrow(/TASKBOARD_NOT_ASSIGNED|assigned to session/);
+    await report("mine", { session });
+    await expect(report("theirs", { session })).rejects.toThrow(/assigned to person u-1/);
+    await report("theirs", { human: person });
+    await report("open", { session: other });
+    const after = boards.get(held.ref);
+    expect(after.nodes.find((node) => node.id === "mine")?.reports[0]?.session?.id).toBe("s-1");
+    expect(after.nodes.find((node) => node.id === "theirs")?.reports[0]?.human?.id).toBe("u-1");
+    expect(reportsFor(undefined, { by: undefined })).toBe(true);
+
+    // A handover must name who holds it now; a stale one changes nothing.
+    await expect(boards.handOver({ ref: held.ref, expectedVersion: after.version, nodeId: "mine", from: other, to: other, note: "x", atMs: 2 }))
+      .rejects.toThrow(/now belongs to session s-1/);
+    const passed = await boards.handOver({ ref: held.ref, expectedVersion: after.version, nodeId: "mine", from: session, to: other, note: "taken on", atMs: 2 });
+    const mine = passed.nodes.find((node) => node.id === "mine")!;
+    expect(mine.state).toBe("running");
+    expect(mine.assignee).toEqual(other);
+    expect(mine.reports.map((one) => one.note)).toEqual(["n", "taken on"]);
+    expect(mine.reports[1]?.handover).toEqual({ from: session, to: other });
+    expect(mine.reports[1]?.human).toBeUndefined();
+    // Each change says which board it happened on, so a listener can follow just one.
+    expect(events.at(-1)).toEqual({ type: "handed-over", board: held.ref.id });
+    // The one it came from can no longer report; the new holder can.
+    await expect(report("mine", { session })).rejects.toThrow(/assigned to session s-2/);
+    await boards.report({ ref: held.ref, expectedVersion: boards.get(held.ref).version, nodeId: "mine", by: undefined, session: other, note: "done", atMs: 3, to: "succeeded" });
+    // A person's decision through the App passes over the assignee, and says who decided.
+    await expect(boards.report({ ref: held.ref, expectedVersion: boards.get(held.ref).version, nodeId: "open", by: undefined, override: true, note: "x", atMs: 3 }))
+      .rejects.toThrow(/which person decided/);
+    await boards.report({ ref: held.ref, expectedVersion: boards.get(held.ref).version, nodeId: "theirs", by: undefined, human: { kind: "human", id: "u-2" }, override: true, note: "decided", atMs: 3, to: "blocked" });
+    expect(boards.get(held.ref).nodes.find((node) => node.id === "theirs")?.reports.at(-1)).toMatchObject({ human: { id: "u-2" }, note: "decided" });
+    // A person's handover says who made it.
+    const byPerson = await boards.handOver({ ref: held.ref, expectedVersion: boards.get(held.ref).version, nodeId: "open", from: undefined, to: person, note: "mine now", atMs: 4, human: person });
+    expect(byPerson.nodes.find((node) => node.id === "open")?.reports.at(-1)).toMatchObject({ human: person, handover: { from: undefined, to: person } });
+    // A finished step keeps who finished it.
+    await expect(boards.handOver({ ref: held.ref, expectedVersion: boards.get(held.ref).version, nodeId: "mine", from: other, to: session, note: "back", atMs: 4 }))
+      .rejects.toThrow(/already succeeded/);
+  });
+});
+
+describe("派子任务时交给它几步（claims）", () => {
+  it("子任务开跑前接过这几步、只有它报得了；它没做完的在它结束时回到父会话；别人的步骤派不出去", async () => {
+    const probe: MemoryHostPrepareProbe = {};
+    const turns: { readonly frames: readonly string[] }[] = [];
+    const host = createMemoryHost({ network: { model: true }, script: { turns }, files: { "src/a.ts": "a\n" } }, probe);
+    const runtime = await createRuntime({
+      app: APP,
+      host,
+      network: { model: true },
+      preset: "local-agent",
+      posture: { sandbox: "workspace-write" },
+      rules: [{ source: "user", effect: "allow", match: { what: "tool" as const } }],
+    });
+    try {
+      // The role reads only; the board tools come with the steps it holds.
+      const reviewer = runtime.characters.create({ id: "reviewer", version: 1, name: "Reviewer", role: "reads", tools: ["read"] });
+      runtime.characters.publish(reviewer.ref);
+      const session = await runtime.sessions.create();
+      const board = await runtime.boards.admit({ nodes: [
+        { id: "a", title: "fix a", dependsOn: [], onFailure: "block-for-human", assignee: session.ref },
+        { id: "b", title: "fix b", dependsOn: [], onFailure: "block-for-human" },
+        { id: "c", title: "someone else's", dependsOn: [], onFailure: "block-for-human", assignee: mintRef("session", "elsewhere") },
+        { id: "d", title: "the parent's own", dependsOn: [], onFailure: "block-for-human", assignee: session.ref },
+      ] });
+      const id = board.ref.id;
+      turns.push(
+        // Someone else's step: refused whole, and no child exists.
+        call("p1", "dispatch-subagent", { instruction: "take c", tools: ["read"], idempotencyKey: "c", claims: { board: id, nodes: ["c"] } }),
+        call("p2", "dispatch-subagent", { instruction: "fix a and b", tools: ["read"], idempotencyKey: "ab", character: "reviewer@1", claims: { board: id, nodes: ["a", "b"] } }),
+        // The child's turns: the parent waits on it, so these land on the child.
+        call("c1", "board-read", { board: id }),
+        call("c2", "board-report", { board: id, version: 3, node: "a", note: "fixing a", state: "running" }),
+        call("c3", "board-report", { board: id, version: 4, node: "a", note: "a fixed", state: "succeeded" }),
+        call("c4", "board-report", { board: id, version: 5, node: "b", note: "b half done", state: "running" }),
+        say("a done, b half done"),
+        // Back with the parent: b is its own again.
+        call("p3", "board-read", { board: id, node: "b" }),
+        call("p4", "board-report", { board: id, version: 7, node: "b", note: "finished b myself", state: "succeeded" }),
+        say("all done"),
+      );
+      const cred = await runtime.credentials.write({ label: "k", secret: { plaintext: new TextEncoder().encode("sk-x") } });
+      const grant = await runtime.workspace.authorize({ path: "/project" });
+      const { run } = await runtime.startAgentRun({
+        session,
+        rootRef: grant.ref,
+        start: { protocol: "anthropic-compatible", endpoint: "https://api.anthropic.com/v1/messages", model: "primary", credentialRef: cred.ref,
+          messages: [{ role: "user", text: "split the work" }] },
+        agent: { idempotencyKey: `claims-${Math.random()}`, mode: "build" },
+      });
+      const results: string[] = [];
+      const seen: ModelEvent[] = [];
+      await new Promise<void>((resolve) => {
+        run.subscribe((event) => {
+          seen.push(event);
+          if (event.type === "tool-result") results.push(event.text);
+          if (event.type === "completed" || event.type === "failed" || event.type === "cancelled") resolve();
+        });
+      });
+      expect(seen.at(-1)?.type, JSON.stringify(results)).toBe("completed");
+      expect(results[0]).toMatch(/TASKBOARD_NOT_ASSIGNED/);
+      expect(results[0]).toContain("belongs to session elsewhere");
+      expect(runtime.subagents.list().map((one) => one.key)).toEqual([expect.stringMatching(/\.ab$/)]);
+
+      const bodies = probe.bodies ?? [];
+      const child = bodies.filter((body) => body.includes("You hold these steps on task board"));
+      expect(child.length).toBeGreaterThan(0);
+      expect(child[0]).toContain("- a: fix a");
+      expect(toolNamesIn(child[0]!)).toEqual(expect.arrayContaining(["read", "board-read", "board-report"]));
+      expect(toolNamesIn(child[0]!)).not.toContain("write");
+
+      const childSession = runtime.subagents.list()[0]!.session;
+      // The child's own board read, as its next request carried it back to the model.
+      const childRead = child.at(-1)!;
+      expect(childRead).toMatch(/a \[ready\] — fix a — owner you \(this session\)/);
+      expect(childRead).toMatch(/c \[ready\] — someone else's — owner session elsewhere/);
+      // A step its dispatcher still holds reads as such, not as a bare session id.
+      expect(childRead).toMatch(/d \[ready\] — the parent's own — owner the session that dispatched you/);
+      expect(results[1]).toContain("subagent reviewer@1 completed");
+
+      const final = runtime.boards.get(board.ref);
+      const [a, b, c] = ["a", "b", "c"].map((one) => final.nodes.find((node) => node.id === one)!);
+      expect(a!.state).toBe("succeeded");
+      expect(a!.assignee).toEqual(childSession);
+      expect(a!.reports.at(-1)).toMatchObject({ note: "a fixed", session: childSession, by: { id: "reviewer@1" } });
+      expect(b!.state).toBe("succeeded");
+      expect(b!.assignee).toEqual(session.ref);
+      expect(b!.reports.map((one) => one.handover === undefined ? one.note : "handover")).toEqual(["handover", "b half done", "handover", "finished b myself"]);
+      expect(b!.reports[2]?.note).toMatch(/ended \(completed\) without finishing/);
+      expect(c!.assignee).toEqual(mintRef("session", "elsewhere"));
+
+      // The parent reads the subagent by the reference it got when dispatching it.
+      const parentRead = results.at(-2)!;
+      const subagent = runtime.subagents.list()[0]!.ref.id;
+      expect(parentRead).toContain("owner you (this session)");
+      expect(parentRead).toContain(`handed from subagent ${subagent} to you (this session)`);
+      expect(results.at(-1)).toContain("b is succeeded");
+    } finally {
+      await runtime.shutdown();
+    }
+  });
+});
diff --git a/packages/sdk/test/bounded-inference.live.test.ts b/packages/sdk/test/bounded-inference.live.test.ts
new file mode 100644
index 00000000..8d99643b
--- /dev/null
+++ b/packages/sdk/test/bounded-inference.live.test.ts
@@ -0,0 +1,148 @@
+import { mkdtempSync, rmSync } from "node:fs";
+import { createServer, type IncomingMessage, type ServerResponse } from "node:http";
+import { tmpdir } from "node:os";
+import { join } from "node:path";
+import { expect, it } from "vitest";
+import { createRuntime, type CreateRuntimeInput } from "../src/index.js";
+import { createNodeHost } from "../src/node.js";
+const png = Buffer.from([137,80,78,71,13,10,26,10]);
+const app = { appId: "prologue.bounded-inference", appVersion: "1.0.0" };
+type Handler = (request: IncomingMessage, response: ServerResponse, body: Record<string, unknown>) => void;
+async function fixture(handler: Handler, config?: CreateRuntimeInput["config"], beforeModelDispatch?: () => Promise<void>) {
+  const home = mkdtempSync(join(tmpdir(), "prologue-bounded-"));
+  const seen: { url: string; headers: IncomingMessage["headers"]; body: Record<string, unknown> }[] = [];
+  const server = createServer(async (request, response) => {
+    let raw = ""; for await (const bytes of request) raw += bytes;
+    const body = raw ? JSON.parse(raw) : {};
+    seen.push({ url: request.url!, headers: request.headers, body }); handler(request, response, body);
+  });
+  await new Promise<void>(resolve => server.listen(0, "127.0.0.1", resolve));
+  const bound = server.address(); if (!bound || typeof bound === "string") throw new Error("No endpoint");
+  const origin = `http://127.0.0.1:${bound.port}`;
+  const host = createNodeHost({ storageRoot: home, ...(beforeModelDispatch ? { beforeModelDispatch } : {}) });
+  const runtime = await createRuntime({ app, host, network: { model: true, loopback: true }, ...(config ? { config } : {}) });
+  const credentialRef = (await runtime.credentials.write({ label: "fixture", secret: { plaintext: new TextEncoder().encode("synthetic-key") } })).ref;
+  return { runtime, host, seen, origin, credentialRef, async close() { await runtime.shutdown(); server.closeAllConnections(); await new Promise<void>(resolve => server.close(() => resolve())); rmSync(home, { recursive: true, force: true }); } };
+}
+const json = (response: ServerResponse, body: unknown) => { response.writeHead(200, { "content-type": "application/json" }); response.end(JSON.stringify(body)); };
+
+it("OpenAI image bytes and Gemini native parts use one Runtime with real Host auth and resource receipts", async () => {
+  const f = await fixture((request, response) => request.url === "/openai"
+    ? json(response, { data: Array.from({ length: 5 }, (_, i) => i === 4 ? { b64_json: "invalid" } : { b64_json: png.toString("base64") }) })
+    : json(response, { candidates: [{ content: { parts: [{ thought: true, inlineData: { data: "invalid" } }, { inline_data: { data: png.toString("base64"), mime_type: "image/png" } }] } }] }), { model: { images: { maxImages: 4 } } });
+  try {
+    const first = await f.runtime.images.generate({ protocol: "openai-images", endpoint: f.origin + "/openai", model: "image", prompt: "draw", credentialRef: f.credentialRef });
+    expect(first.images).toHaveLength(4);
+    expect(f.seen[0]!.body).toEqual({ model: "image", prompt: "draw" });
+    expect(f.seen[0]!.headers.authorization).toBe("Bearer synthetic-key");
+    expect((await f.runtime.resources.readChunk(first.images[0]!, 0)).bytes).toEqual(new Uint8Array(png));
+    const second = await f.runtime.images.generate({ protocol: "gemini", endpoint: f.origin + "/gemini", model: "gemini", prompt: "draw", aspectRatio: "16:9", credentialRef: f.credentialRef });
+    expect(second.images).toHaveLength(1);
+    expect(f.seen[1]!.headers["x-goog-api-key"]).toBe("synthetic-key");
+    expect(f.seen[1]!.headers.authorization).toBeUndefined();
+    expect(f.seen[1]!.body).toMatchObject({ generationConfig: { responseModalities: ["TEXT", "IMAGE"], imageConfig: { aspectRatio: "16:9" } } });
+  } finally { await f.close(); }
+});
+
+it("URL results are downloaded by Host without model credentials; local unauthenticated generation is explicit", async () => {
+  let origin = "";
+  const f = await fixture((request, response) => {
+    if (request.url === "/file") { response.writeHead(200, { "content-type": "image/png" }); response.end(png); }
+    else json(response, { data: [{ url: origin + "/file" }] });
+  }); origin = f.origin;
+  try {
+    const request = { protocol: "openai-images", endpoint: f.origin + "/generate", model: "image", prompt: "draw" };
+    const receipt = await f.runtime.images.generate({ ...request, credentialRef: f.credentialRef });
+    expect(receipt.images).toHaveLength(1);
+    expect(f.seen[1]!.headers.authorization).toBeUndefined();
+    expect(f.seen[1]!.headers["x-api-key"]).toBeUndefined();
+    expect(f.seen[1]!.body).toEqual({});
+    await f.runtime.images.generate({ ...request, auth: "none" });
+    expect(f.seen[2]!.headers.authorization).toBeUndefined();
+    await expect(f.runtime.images.generate({ ...request, endpoint: "https://example.com/images", auth: "none" })).rejects.toMatchObject({ code: "MODEL_NETWORK_FAILED" });
+    expect(f.seen).toHaveLength(4);
+  } finally { await f.close(); }
+});
+
+it("image URL redirects and another loopback origin cannot receive downloads", async () => {
+  let origin = "", target = "redirect";
+  const f = await fixture((request, response) => {
+    if (request.url === "/redirect") { response.writeHead(302, { location: origin + "/secret" }); response.end(); }
+    else json(response, { data: [{ url: target === "redirect" ? origin + "/redirect" : "http://127.0.0.1:1/file" }] });
+  }); origin = f.origin;
+  try {
+    const request = { protocol: "openai-images", endpoint: f.origin + "/generate", model: "image", prompt: "draw", credentialRef: f.credentialRef };
+    await expect(f.runtime.images.generate(request)).rejects.toMatchObject({ code: "NETWORK_TARGET_DENIED" });
+    expect(f.seen.map(row => row.url)).toEqual(["/generate", "/redirect"]);
+    target = "other";
+    await expect(f.runtime.images.generate(request)).rejects.toMatchObject({ code: "NETWORK_TARGET_DENIED" });
+    expect(f.runtime.resources.publishedBytes).toBe(0);
+  } finally { await f.close(); }
+});
+
+it("bounded structured TypeSafe retains native question/results and safe HTTP status", async () => {
+  const body = { model: "native-reported", answers: { color: { choice: "blue", probabilities: { blue: 0.9 }, confidence: 0.9 } }, usage: { input_tokens: 12, output_tokens: 3 } };
+  const f = await fixture((request, response) => { if (request.url === "/fail") { response.writeHead(401); response.end("private credential text"); } else json(response, body); });
+  try {
+    const input = { protocol: "typesafe" as const, endpoint: f.origin + "/systemone", credentialRef: f.credentialRef, model: "requested", state: "a blue object", questions: { color: { type: "choice", criteria: { blue: "blue" } } } };
+    expect(await f.runtime.typesafe.evaluate(input)).toEqual(body);
+    expect(f.seen[0]!.body).toEqual({ model: input.model, state: input.state, questions: input.questions });
+    expect(f.seen[0]!.body).not.toHaveProperty("messages");
+    await expect(f.runtime.typesafe.evaluate({ ...input, endpoint: f.origin + "/fail" })).rejects.toMatchObject({ code: "MODEL_HTTP_ERROR", status: 401 });
+    expect(f.seen).toHaveLength(2);
+  } finally { await f.close(); }
+});
+
+it("declared/streamed response bounds, decoded image bounds and MIME mismatch fail without resources", async () => {
+  let mode = "declared";
+  const f = await fixture((_request, response) => {
+    if (mode === "declared") { response.writeHead(200, { "content-length": "10000" }); response.end("small"); }
+    else if (mode === "streamed") { response.writeHead(200); response.write("x".repeat(600)); response.end(); }
+    else if (mode === "decoded") json(response, { data: [{ b64_json: Buffer.concat([png, Buffer.alloc(70)]).toString("base64") }] });
+    else json(response, { candidates: [{ content: { parts: [{ inlineData: { data: png.toString("base64"), mimeType: "image/jpeg" } }] } }] });
+  }, { model: { images: { maxResponseBytes: 512, maxImageBytes: 64 } } });
+  try {
+    for (const value of ["declared", "streamed", "decoded", "mime"]) {
+      mode = value;
+      await expect(f.runtime.images.generate({ protocol: value === "mime" ? "gemini" : "openai-images", endpoint: f.origin + "/image", model: "image", prompt: "draw", credentialRef: f.credentialRef })).rejects.toMatchObject({ code: "MODEL_RESPONSE_INVALID" });
+      expect(f.runtime.resources.publishedBytes).toBe(0);
+    }
+  } finally { await f.close(); }
+});
+
+it("multi-megabyte image decoding remains bounded without recursive regular-expression overflow", async () => {
+  const large = Buffer.concat([png, Buffer.alloc(2 * 1024 * 1024)]);
+  const f = await fixture((_request, response) => json(response, { data: [{ b64_json: large.toString("base64") }] }));
+  try {
+    const result = await f.runtime.images.generate({ protocol: "openai-images", endpoint: f.origin + "/image", model: "image", prompt: "draw", credentialRef: f.credentialRef });
+    expect(f.runtime.resources.inspect(result.images[0]!)?.byteLength).toBe(large.length);
+  } finally { await f.close(); }
+});
+
+it("abort and Runtime shutdown cancel actual pending native requests without publishing late results", async () => {
+  let entered = Promise.withResolvers<void>();
+  const f = await fixture(() => entered.resolve());
+  try {
+    const controller = new AbortController();
+    const request = { protocol: "openai-images", endpoint: f.origin + "/image", model: "image", prompt: "draw", credentialRef: f.credentialRef };
+    const pending = f.runtime.images.generate({ ...request, signal: controller.signal });
+    const rejected = expect(pending).rejects.toMatchObject({ name: "AbortError" });
+    await entered.promise; controller.abort(); await rejected;
+    expect(f.runtime.resources.publishedBytes).toBe(0);
+    entered = Promise.withResolvers<void>();
+    const judgment = f.runtime.typesafe.evaluate({ protocol: "typesafe", endpoint: f.origin + "/judgment", model: "native", state: "state", questions: { q: {} }, credentialRef: f.credentialRef });
+    const closed = expect(judgment).rejects.toMatchObject({ name: "AbortError" });
+    await entered.promise; await f.runtime.shutdown(); await closed;
+  } finally { await f.close(); }
+});
+
+it("final Host dispatch guard runs after preparation and denies without sending model bytes", async () => {
+  const entered = Promise.withResolvers<void>(), release = Promise.withResolvers<void>(); let revoked = false;
+  const f = await fixture((_request, response) => json(response, { answers: {} }), undefined, async () => { entered.resolve(); await release.promise; if (revoked) throw new Error("revoked"); });
+  try {
+    const pending = f.runtime.typesafe.evaluate({ protocol: "typesafe", endpoint: f.origin + "/judgment", model: "native", state: "private source", questions: { q: {} }, credentialRef: f.credentialRef });
+    const rejected = expect(pending).rejects.toThrow("revoked");
+    await entered.promise; revoked = true; release.resolve(); await rejected;
+    expect(f.seen).toHaveLength(0);
+  } finally { release.resolve(); await f.close(); }
+});
diff --git a/packages/sdk/test/bounded-inference.test.ts b/packages/sdk/test/bounded-inference.test.ts
new file mode 100644
index 00000000..1e3f46e2
--- /dev/null
+++ b/packages/sdk/test/bounded-inference.test.ts
@@ -0,0 +1,37 @@
+import { expect, it } from "vitest";
+import { createRuntime, type Runtime } from "../src/index.js";
+import { createMemoryHost } from "../src/testing/api/index.js";
+const app = { appId: "prologue.bounded-lifecycle", appVersion: "1.0.0" };
+const png = new Uint8Array([137,80,78,71,13,10,26,10]);
+const body = JSON.stringify({ data: [{ b64_json: Buffer.from(png).toString("base64") }] });
+const request = { protocol: "openai-images", endpoint: "https://api.example.com/images", model: "image", prompt: "draw" };
+
+it("cancel during preparation settles the surface and shutdown, and cancels a late Host handle without dispatch", async () => {
+  const host = createMemoryHost({ script: { frames: [body] } });
+  const prepare = host.prepareModelNetwork.bind(host), entered = Promise.withResolvers<void>(), release = Promise.withResolvers<void>();
+  let executeCount = 0, cancelled = 0;
+  host.prepareModelNetwork = async input => { entered.resolve(); await release.promise; return prepare(input); };
+  const execute = host.executeModelNetwork.bind(host), cancel = host.cancelModelNetwork.bind(host);
+  host.executeModelNetwork = async input => { executeCount++; return execute(input); };
+  host.cancelModelNetwork = async input => { cancelled++; return cancel(input); };
+  const runtime = await createRuntime({ app, host, network: { model: true } });
+  const credentialRef = (await runtime.credentials.write({ label: "test", secret: { plaintext: new TextEncoder().encode("synthetic") } })).ref;
+  const controller = new AbortController();
+  const pending = runtime.images.generate({ ...request, credentialRef, signal: controller.signal });
+  const rejected = expect(pending).rejects.toMatchObject({ name: "AbortError" });
+  await entered.promise; controller.abort(); await rejected;
+  await runtime.shutdown();
+  release.resolve(); await new Promise(resolve => setTimeout(resolve, 0));
+  expect(cancelled).toBe(1); expect(executeCount).toBe(0);
+});
+
+it("shutdown after response completion but before publication cannot return successful resources", async () => {
+  const host = createMemoryHost({ script: { frames: [body] } });
+  const execute = host.executeModelNetwork.bind(host);
+  let runtime: Runtime, shutdown: Promise<unknown> | undefined;
+  host.executeModelNetwork = async input => { const outcome = await execute(input); shutdown = runtime.shutdown(); return outcome; };
+  runtime = await createRuntime({ app, host, network: { model: true } });
+  const credentialRef = (await runtime.credentials.write({ label: "test", secret: { plaintext: new TextEncoder().encode("synthetic") } })).ref;
+  await expect(runtime.images.generate({ ...request, credentialRef })).rejects.toMatchObject({ name: "AbortError" });
+  await shutdown;
+});
diff --git a/packages/sdk/test/model-loopback.live.test.ts b/packages/sdk/test/model-loopback.live.test.ts
new file mode 100644
index 00000000..605655f2
--- /dev/null
+++ b/packages/sdk/test/model-loopback.live.test.ts
@@ -0,0 +1,112 @@
+import { mkdtempSync, rmSync } from "node:fs";
+import { createServer } from "node:http";
+import { tmpdir } from "node:os";
+import { join } from "node:path";
+import { expect, it } from "vitest";
+import { createRuntime, type NetworkAllowance, type Run } from "../src/index.js";
+import { createNodeHost } from "../src/node.js";
+import { createMemoryHost } from "../src/testing/api/index.js";
+import { checkNetworkTarget } from "../src/host/core/index.js";
+
+const app = { appId: "prologue.loopback", appVersion: "0.1.0" };
+const definition = { name: "Local", protocol: "openai-compatible", model: "local-model", purposes: [], capabilities: { tools: false, structuredOutput: false, promptCache: false } } as const;
+
+function terminal(run: Run): Promise<void> {
+  if (["completed", "failed", "cancelled"].includes(run.state)) return Promise.resolve();
+  return new Promise(resolve => {
+    const stop = run.subscribe(event => {
+      if (["completed", "failed", "cancelled"].includes(event.type)) { stop(); resolve(); }
+    });
+  });
+}
+async function ready(host: Parameters<typeof createRuntime>[0]["host"], network: NetworkAllowance = {}) {
+  const runtime = await createRuntime({ app, host, network });
+  const credential = await runtime.credentials.write({ label: "fixture", secret: { plaintext: new TextEncoder().encode("loopback-fixture-key") } });
+  const session = await runtime.sessions.create();
+  const start = (endpoint: string) => session.startRun({ protocol: definition.protocol, model: definition.model, endpoint, credentialRef: credential.ref, messages: [{ role: "user", text: "local evidence" }] });
+  return { runtime, session, start };
+}
+
+it("catalog and Run reject malformed or non-loopback plaintext endpoints before execution", async () => {
+  const { runtime, session, start } = await ready(createMemoryHost(), { model: true, loopback: true });
+  try {
+    for (const endpoint of ["", "https://", "not-a-url", "file:///tmp/model", "http://api.example.com/v1", "http://localhost.example.com/v1", "http://127.example.com/v1", "http://127.0.0.1.example.com/v1", "http://localhost@api.example.com/v1", "http://192.168.0.1/v1"]) {
+      expect(() => runtime.models.register({ ...definition, endpoint })).toThrow(expect.objectContaining({ code: "RUNTIME_OPTIONS_INVALID" }));
+      await expect(start(endpoint)).rejects.toMatchObject({ code: "RUNTIME_OPTIONS_INVALID" });
+      expect(() => checkNetworkTarget(endpoint, { model: true, loopback: true })).toThrow(expect.objectContaining({ code: "NETWORK_TARGET_DENIED" }));
+    }
+    expect(runtime.models.list()).toHaveLength(0);
+    expect(await session.terminalRuns()).toHaveLength(0);
+  } finally { await runtime.shutdown(); }
+});
+
+it("catalog and Run share normalized local endpoint shapes; registration cannot enable the model channel", async () => {
+  const { runtime, session, start } = await ready(createMemoryHost());
+  try {
+    for (const endpoint of ["http://localhost:11434/v1", "http://model.localhost:11434/v1", "http://127.0.0.1/v1", "http://127.2.3.4/v1", "http://[::1]/v1", "http://[0:0:0:0:0:0:0:1]/v1", "https://api.example.com/v1"]) {
+      const entry = runtime.models.register({ ...definition, endpoint });
+      expect(runtime.models.resolve({ ref: entry.ref }).endpoint).toBe(endpoint);
+      const run = await start(endpoint);
+      await terminal(run);
+      expect(run.state).toBe("failed");
+      expect(await session.replay(run.ref)).toContainEqual(expect.objectContaining({ type: "failed", error: expect.objectContaining({ code: "MODEL_NETWORK_FAILED", safeMessage: expect.stringContaining('"model" channel was never allowed') }) }));
+    }
+  } finally { await runtime.shutdown(); }
+});
+
+for (const address of ["127.0.0.1", "::1"]) it(`public Session uses a real HTTP model on ${address}, with cancellation and local replay`, async () => {
+  const storageRoot = mkdtempSync(join(tmpdir(), "prologue-loopback-"));
+  let requests = 0, hold = false;
+  const entered = Promise.withResolvers<void>();
+  const bodies: string[] = [], keys: (string | string[] | undefined)[] = [];
+  const server = createServer(async (request, response) => {
+    let body = ""; for await (const chunk of request) body += chunk;
+    requests++; bodies.push(body); keys.push(request.headers.authorization);
+    if (hold) { entered.resolve(); return; }
+    response.writeHead(200, { "content-type": "text/event-stream" });
+    response.end(`data: ${JSON.stringify({ choices: [{ index: 0, delta: { content: "local result" }, finish_reason: null }] })}\n\ndata: ${JSON.stringify({ choices: [{ index: 0, delta: {}, finish_reason: "stop" }] })}\n\ndata: [DONE]\n\n`);
+  });
+  await new Promise<void>((resolve, reject) => { server.once("error", reject); server.listen(0, address, resolve); });
+  const bound = server.address(); if (!bound || typeof bound === "string") throw new Error("No server address");
+  const endpoint = `http://${address === "::1" ? "[::1]" : address}:${bound.port}/v1/chat/completions`;
+  const { runtime, session, start } = await ready(createNodeHost({ storageRoot }), { model: true, loopback: true });
+  try {
+    runtime.models.register({ ...definition, endpoint });
+    const run = await start(endpoint); await terminal(run);
+    expect(run.state).toBe("completed"); expect(requests).toBe(1);
+    expect(keys).toEqual(["Bearer loopback-fixture-key"]);
+    expect(JSON.parse(bodies[0]!)).toMatchObject({ model: "local-model", messages: [{ role: "user", content: "local evidence" }] });
+    expect(await session.replay(run.ref)).toContainEqual(expect.objectContaining({ type: "text-delta", text: "local result" }));
+    expect(requests).toBe(1);
+    hold = true;
+    const cancelled = await start(endpoint); await entered.promise;
+    await cancelled.cancel(); await terminal(cancelled);
+    expect(cancelled.state).toBe("cancelled"); expect(requests).toBe(2);
+    expect(JSON.stringify(await session.replay(cancelled.ref))).not.toContain("loopback-fixture-key");
+  } finally {
+    await runtime.shutdown(); server.closeAllConnections();
+    await new Promise<void>(resolve => server.close(() => resolve()));
+    rmSync(storageRoot, { recursive: true, force: true });
+  }
+}, 15_000);
+
+it("real Node Host requires explicit loopback and model grants even for a registered model", async () => {
+  let requests = 0;
+  const server = createServer((_request, response) => { requests++; response.end("unexpected"); });
+  await new Promise<void>(resolve => server.listen(0, "127.0.0.1", resolve));
+  const bound = server.address(); if (!bound || typeof bound === "string") throw new Error("No server address");
+  const endpoint = `http://127.0.0.1:${bound.port}/v1/chat/completions`;
+  try {
+    for (const [network, reason] of [[{}, /model.*channel was never allowed/], [{ model: true }, /explicitly enabled loopback/], [{ loopback: true }, /model.*channel was never allowed/]] as const) {
+      const storageRoot = mkdtempSync(join(tmpdir(), "prologue-loopback-denied-"));
+      const { runtime, session, start } = await ready(createNodeHost({ storageRoot }), network);
+      try {
+        runtime.models.register({ ...definition, endpoint });
+        const run = await start(endpoint); await terminal(run);
+        expect(run.state).toBe("failed");
+        expect(await session.replay(run.ref)).toContainEqual(expect.objectContaining({ type: "failed", error: expect.objectContaining({ code: "MODEL_NETWORK_FAILED", safeMessage: expect.stringMatching(reason) }) }));
+        expect(requests).toBe(0);
+      } finally { await runtime.shutdown(); rmSync(storageRoot, { recursive: true, force: true }); }
+    }
+  } finally { server.closeAllConnections(); await new Promise<void>(resolve => server.close(() => resolve())); }
+});
diff --git a/packages/sdk/test/module-config.test.ts b/packages/sdk/test/module-config.test.ts
index 48489c30..d86ce4fc 100644
--- a/packages/sdk/test/module-config.test.ts
+++ b/packages/sdk/test/module-config.test.ts
@@ -222,3 +222,17 @@ describe("阈值只有一处入口", () => {
     expect(resolveModuleConfig({ orchestration: { subagent: { maxDepth: 1 } } }).orchestration.subagent.maxDepth).toBe(1);
   });
 });
+
+it("App tool deadline ceiling admits long checks without changing existing tool deadlines", async () => {
+  const runtime = await createRuntime({ app: APP, host: createMemoryHost(), preset: "local-agent", config: { tool: { maxTimeoutMs: 300_000 } } });
+  try {
+    const before = runtime.tools.get("read")?.timeoutMs;
+    runtime.tools.register({ name: "long-check", version: "1", description: "Host-owned build checks", parameters: { type: "object" },
+      effectKind: "safe-read", timeoutMs: 300_000, idempotency: "pure" });
+    expect(runtime.tools.get("long-check")?.timeoutMs).toBe(300_000);
+    expect(runtime.tools.get("read")?.timeoutMs).toBe(before);
+    expect(() => runtime.tools.register({ name: "overlong", version: "1", description: "Over ceiling", parameters: { type: "object" },
+      effectKind: "safe-read", timeoutMs: 300_001, idempotency: "pure" })).toThrow();
+    expect(() => resolveModuleConfig({ tool: { maxTimeoutMs: NaN } })).toThrow();
+  } finally { await runtime.shutdown(); }
+});
diff --git a/packages/sdk/test/subagent-character.test.ts b/packages/sdk/test/subagent-character.test.ts
index 029d7f50..cf19dc7b 100644
--- a/packages/sdk/test/subagent-character.test.ts
+++ b/packages/sdk/test/subagent-character.test.ts
@@ -202,15 +202,12 @@ describe("派出去的子 Agent 能点名一个角色", () => {
     expect(toolNamesIn(bodies[1] ?? "")).toEqual(["read"]);
   });
 
-  it("**档案要的比天花板宽：整次派出失败，那个孩子一次都没上线**", async () => {
+  it("**档案声明、父亲也有的工具直接给上：派出时少写一个不再整次失败**", async () => {
     /*
-     * 父亲**有** `write`，但这一次派出只要了 `read`，而档案声明
-     * `read` 和 `write`。
+     * 父亲有 `write`，这一次派出只写了 `read`，而档案声明 `read` 和 `write`。
      *
-     * 天花板是求交后那份名单，不是父亲手上全部。所以这是一次扩权
-     * 尝试：**不是「尽力给一部分」**——一份档案拿不到它声明的东西，
-     * 它就不是当初被点头的那份档案了。这条语义在 `character/profile`
-     * 的绑定预检那儿，这里只是证明子 Agent 这条路也走它。
+     * 用户定的是"就给工具"：点名的角色带着它声明的工具去，只要父亲自己有。原来这里整次派出失败，
+     * 模型只是在名单里漏写一个，就要重派、再审一次。
      */
     const { bodies, terminal, toolResults } = await dispatch({
       character: "reviewer@1",
@@ -218,6 +215,22 @@ describe("派出去的子 Agent 能点名一个角色", () => {
       tools: ["read"],
       parentTools: ["read", "write", "dispatch-subagent"],
     });
+    expect(terminal?.type).toBe("completed");
+    expect(toolResults).toContain("subagent reviewer@1 completed");
+    expect(toolNamesIn(bodies[1] ?? "")).toEqual(["read", "write"]);
+  });
+
+  it("**档案要的工具父亲没有：整次派出失败，那个孩子一次都没上线**", async () => {
+    /*
+     * 孩子永远拿不到父亲没有的东西。档案要 `write`，而父亲这一次没开 `write`：这是扩权尝试，
+     * 不是「尽力给一部分」。
+     */
+    const { bodies, terminal, toolResults } = await dispatch({
+      character: "reviewer@1",
+      characterTools: ["read", "write"],
+      tools: ["read"],
+      parentTools: ["read", "dispatch-subagent"],
+    });
     // 父亲自己没死：拒绝一次派出不等于把这条 Run 弄坏。
     expect(terminal?.type).toBe("completed");
     expect(toolResults).toContain("CHARACTER_ESCALATION");
@@ -235,6 +248,14 @@ describe("派出去的子 Agent 能点名一个角色", () => {
     expect(toolResults).toContain("latest by name");
   });
 
+  it("**点了登记过的角色却写错版本：审查之前就拒，并说出现有的版本**", async () => {
+    const { toolResults, bodies } = await dispatch({ character: "reviewer@2" });
+    expect(toolResults).toContain("CHARACTER_NOT_FOUND");
+    expect(toolResults).toContain("reviewer@2");
+    expect(toolResults).toContain("use one of: reviewer@1. Nothing was dispatched.");
+    expect(bodies).toHaveLength(2);
+  });
+
   it("**点了一个没登记的角色：派不出去，而且孩子没上线**", async () => {
     const { toolResults, bodies } = await dispatch({ character: "ghost@1" });
     expect(toolResults).toContain("CHARACTER_NOT_FOUND");
diff --git a/packages/sdk/test/subagent-depth.test.ts b/packages/sdk/test/subagent-depth.test.ts
index a8917349..6b8ef8e0 100644
--- a/packages/sdk/test/subagent-depth.test.ts
+++ b/packages/sdk/test/subagent-depth.test.ts
@@ -576,6 +576,28 @@ describe("真实运行中的并发、同键重试和失败后的新键", () => {
     expect(runtime.subagents.list()[0]?.key).toContain("ok-second");
     await runtime.shutdown();
   });
+
+  it("不能做存储地址的派出键当场被拒，不等审查或落盘，换个合法键就能派", async () => {
+    const { host } = routedHost({
+      scripts: {
+        parent: [
+          dispatch({ instruction: "bad-key", idempotencyKey: "写 README/第二份", tools: [] }),
+          dispatch({ instruction: "good-key", idempotencyKey: "readme-2", tools: [] }),
+          says("parent-done"),
+        ],
+        "good-key": [says("child-done")],
+      },
+    });
+    const runtime = await bootRuntime({ host, maxDepth: 1, maxConcurrent: 2 });
+    const { events, toolResults } = await runParent({ runtime, task: "go", parentKey: "key-root", toolNames: ["dispatch-subagent"] });
+    expect(events.at(-1)?.type).toBe("completed");
+    expect(toolResults[0]).toContain("TOOL_ARGUMENTS_INVALID");
+    expect(toolResults[0]).toContain("1–48 letters, digits, dots, dashes or underscores");
+    expect(toolResults.some((one) => one.includes("STORAGE_KEY_INVALID"))).toBe(false);
+    expect(runtime.subagents.list()).toHaveLength(1);
+    expect(runtime.subagents.list()[0]?.key).toContain("readme-2");
+    await runtime.shutdown();
+  });
 });
 
 describe("模型侧父任务归属", () => {
diff --git a/packages/sdk/test/subagent-recovery.test.ts b/packages/sdk/test/subagent-recovery.test.ts
index cb307324..f4f8e24d 100644
--- a/packages/sdk/test/subagent-recovery.test.ts
+++ b/packages/sdk/test/subagent-recovery.test.ts
@@ -194,6 +194,24 @@ describe("真实 Node Host 跨进程的子任务登记恢复", () => {
       await runtime.shutdown();
     }
   }, 30_000);
+
+  it("核对并结束孩子被中断的那一轮后，孩子有了终态，不再卡住父会话（不必等下次重启）", async () => {
+    const { temp } = await fromChild("running");
+    const runtime = await createRuntime({ app: APP, host: createNodeHost({ storageRoot: join(temp, "store") }), preset: "local-agent", network: { model: true } });
+    try {
+      const held = runtime.subagents.list()[0]!;
+      expect(held.state).toBe("reconcile-required");
+      const inspected = await runtime.sessions.inspectRecovery(held.session);
+      const interrupted = inspected.runs.find((run) => run.ref.id === held.run.id)!;
+      expect(interrupted.canClose).toBe(true);
+      await runtime.sessions.recoverRun(held.session, held.run, interrupted.version);
+      const after = runtime.subagents.list()[0]!;
+      expect(after.state).not.toBe("reconcile-required");
+      expect(["completed", "failed", "cancelled"]).toContain(after.state);
+    } finally {
+      await runtime.shutdown();
+    }
+  }, 30_000);
 });
 
 function userHas(body: string, token: string): boolean {
diff --git a/packages/sdk/test/subagent-workspaces.test.ts b/packages/sdk/test/subagent-workspaces.test.ts
index 47acd06c..c9a14b11 100644
--- a/packages/sdk/test/subagent-workspaces.test.ts
+++ b/packages/sdk/test/subagent-workspaces.test.ts
@@ -1777,3 +1777,142 @@ describe("真实 Node Host 跨进程恢复 workspace 来源", () => {
     }
   }, 30_000);
 });
+
+describe("父任务只读子目录（parentReads）", () => {
+  const call = (id: string, name: string, input: Record<string, unknown>) => ({
+    frames: [
+      `data: {"type":"content_block_start","content_block":{"type":"tool_use","id":"${id}","name":"${name}","input":${JSON.stringify(input)}}}\n`,
+      'data: {"type":"message_stop"}\n',
+    ],
+  });
+
+  it("授权后父任务按 id 读、列、搜子目录，不能写；它的查看不替子任务的先读后改；未授权时点名子目录被拒", async () => {
+    const temp = mkdtempSync(join(tmpdir(), "prologue-parent-reads-"));
+    tempRoot = temp;
+    const parentDir = join(temp, "parent");
+    const aDir = join(temp, "writer-a");
+    mkdirSync(parentDir);
+    mkdirSync(aDir);
+    writeFileSync(join(parentDir, "note.txt"), "parent-note\n");
+    writeFileSync(join(aDir, "note.txt"), "from-a\n");
+    writeFileSync(join(aDir, "other.txt"), "other-a\n");
+    const mixed = mixNodeModel({
+      storageRoot: join(temp, "store"),
+      scripts: {
+        "parent-task": [
+          call("p1", "read", { path: "other.txt", workspace: "writer-a" }),
+          dispatch({ instruction: "child-a-work", tools: ["read", "write"], idempotencyKey: "a", workspace: "writer-a" }),
+          call("p2", "read", { path: "note.txt", workspace: "writer-a" }),
+          call("p3", "list", { workspace: "writer-a" }),
+          call("p4", "search", { query: "edited", workspace: "writer-a" }),
+          call("p5", "read", { path: "note.txt" }),
+          call("p6", "read", { path: "note.txt", workspace: "writer-b" }),
+          call("p7", "write", { path: "note.txt", text: "parent-wrote\n", workspace: "writer-a" }),
+          says("parent-done"),
+        ],
+        // The parent looked at other.txt; the child never did, so its change is refused.
+        "child-a-work": [writeCall("other.txt", "changed\n"), readCall("note.txt"), writeCall("note.txt", "edited-a\n"), says("a-done")],
+      },
+    });
+    const runtime = await createRuntime({
+      app: APP,
+      host: mixed.host,
+      preset: "local-agent",
+      network: { model: true },
+      posture: { sandbox: "workspace-write" },
+      require: ["workspace.read", "workspace.write"],
+      rules: [
+        ...ASK_WRITE_ALLOW_ORCHESTRATION,
+        { source: "user" as const, effect: "allow" as const, match: { what: "tool" as const, name: "list" } },
+        { source: "user" as const, effect: "allow" as const, match: { what: "tool" as const, name: "search" } },
+      ],
+    });
+    try {
+      const cred = await runtime.credentials.write({ label: "k", secret: { plaintext: new TextEncoder().encode("sk-x") } });
+      const parent = await runtime.workspace.authorize({ path: parentDir });
+      const writerA = await runtime.workspace.authorize({ path: aDir, posture: "workspace-write" });
+      const childResults: string[] = [];
+      const started = await runtime.startAgentRun({
+        session: await runtime.sessions.create(),
+        rootRef: parent.ref,
+        start: {
+          protocol: "anthropic-compatible",
+          endpoint: "https://api.anthropic.com/v1/messages",
+          model: "primary",
+          credentialRef: cred.ref,
+          messages: [{ role: "user", text: "parent-task" }],
+        },
+        agent: { idempotencyKey: "parent-reads", mode: "build", budget: { maxTurns: 12 } },
+        subagents: {
+          requireWorkspace: true,
+          parentReads: true,
+          workspaces: [{ id: "writer-a", rootRef: writerA.ref }],
+          onStarted: async ({ run }) => {
+            run.subscribe((event) => {
+              if (event.type === "tool-result") childResults.push(event.text);
+              if (event.type === "awaiting-approval") {
+                void runtime.readClock().then((now) =>
+                  runtime.effects.pendings.answer(event.pendingRef, { kind: "effect-approval", answer: "allow" }, now),
+                );
+              }
+            });
+          },
+        },
+      });
+      const { events, toolResults } = await terminalOf(started.run);
+      expect(events.at(-1)?.type, JSON.stringify({ last: events.at(-1), toolResults, childResults })).toBe("completed");
+      // The parent's look at other.txt did not count as the child's read.
+      expect(childResults[0]).toContain("WORKSPACE_NOT_OBSERVED");
+      expect(toolResults[0]).toContain("other-a");
+      expect(toolResults.some((one) => one.includes("edited-a")), JSON.stringify({ toolResults, childResults })).toBe(true);
+      expect(toolResults.some((one) => one.includes("other.txt") && one.includes("note.txt") && !one.includes("edited"))).toBe(true);
+      expect(toolResults.some((one) => one.includes("note.txt:1:edited-a"))).toBe(true);
+      expect(toolResults.some((one) => one.includes("parent-note"))).toBe(true);
+      expect(toolResults.some((one) => one.includes('"workspace" must be one of "writer-a"'))).toBe(true);
+      expect(toolResults.at(-1)).toMatch(/TOOL_ARGUMENTS_INVALID|workspace/);
+      expect(readFileSync(join(aDir, "note.txt"), "utf8")).toBe("edited-a\n");
+      expect(readFileSync(join(aDir, "other.txt"), "utf8")).toBe("other-a\n");
+      expect(readFileSync(join(parentDir, "note.txt"), "utf8")).toBe("parent-note\n");
+    } finally {
+      await runtime.shutdown();
+    }
+
+    const plainStore = join(temp, "store-plain");
+    mkdirSync(plainStore);
+    const plainMix = mixNodeModel({
+      storageRoot: plainStore,
+      scripts: { "parent-task": [call("q1", "read", { path: "note.txt", workspace: "writer-a" }), says("parent-done")] },
+    });
+    const plain = await createRuntime({
+      app: APP,
+      host: plainMix.host,
+      preset: "local-agent",
+      network: { model: true },
+      posture: { sandbox: "workspace-write" },
+      rules: [{ source: "user", effect: "allow", match: { what: "tool" as const } }],
+    });
+    try {
+      const cred = await plain.credentials.write({ label: "k", secret: { plaintext: new TextEncoder().encode("sk-x") } });
+      const parent = await plain.workspace.authorize({ path: parentDir });
+      const writerA = await plain.workspace.authorize({ path: aDir, posture: "workspace-write" });
+      const started = await plain.startAgentRun({
+        session: await plain.sessions.create(),
+        rootRef: parent.ref,
+        start: {
+          protocol: "anthropic-compatible",
+          endpoint: "https://api.anthropic.com/v1/messages",
+          model: "primary",
+          credentialRef: cred.ref,
+          messages: [{ role: "user", text: "parent-task" }],
+        },
+        agent: { idempotencyKey: "no-parent-reads", mode: "build" },
+        subagents: { requireWorkspace: true, workspaces: [{ id: "writer-a", rootRef: writerA.ref }] },
+      });
+      const { toolResults } = await terminalOf(started.run);
+      expect(toolResults[0]).toContain("reads only its own workspace");
+      expect(toolResults[0]).not.toContain("edited-a");
+    } finally {
+      await plain.shutdown();
+    }
+  }, 30_000);
+});
diff --git a/packages/sdk/test/workspace-find-search.live.test.ts b/packages/sdk/test/workspace-find-search.live.test.ts
index cbb7ff0b..9bccfe53 100644
--- a/packages/sdk/test/workspace-find-search.live.test.ts
+++ b/packages/sdk/test/workspace-find-search.live.test.ts
@@ -135,6 +135,37 @@ describe("内容正则检索", () => {
     expect(found.matches.every((one) => one.line > 0)).toBe(true);
   });
 
+  it("路径可以是单个文件：只在这份文件里找；路径不存在时报错，不答成「没找到」", async () => {
+    const read = await reader();
+    const literal = await read.search({ query: "const port", path: "src/index.ts" });
+    expect(literal.matches).toEqual([{ path: "src/index.ts", line: 2, text: "const port = 3000;" }]);
+    const pattern = await read.search({ query: "port\\s*=\\s*\\d+", regex: true, path: "src/index.ts" });
+    expect(pattern.matches.map((one) => `${one.path}:${one.line}`)).toEqual(["src/index.ts:2"]);
+    await expect(read.search({ query: "const port", path: "src/gone.ts" })).rejects.toMatchObject({ code: "ROOT_NOT_FOUND" });
+  });
+
+  it("大仓库里走过一千多个文件也照样找到；到了条数上界就说没搜全，不假装找全了", async () => {
+    const at = mkdtempSync(join(tmpdir(), "search-large-"));
+    for (let dir = 0; dir < 30; dir += 1) {
+      mkdirSync(join(at, `a${String(dir).padStart(2, "0")}`));
+      for (let file = 0; file < 50; file += 1) writeFileSync(join(at, `a${String(dir).padStart(2, "0")}`, `f${file}.txt`), `filler ${dir}-${file}\n`);
+    }
+    mkdirSync(join(at, "zz"));
+    writeFileSync(join(at, "zz", "target.ts"), "export const needle = 1;\n");
+    const runtime = await createRuntime({ app: { appId: "prologue.harness", appVersion: "0.1.0" }, host: createNodeHost(), preset: "local-agent", network: { model: true }, posture: { sandbox: "workspace-write" } });
+    const grant = await runtime.workspace.authorize({ path: at });
+    const read = runtime.workspace.reader(grant.ref, createIgnoreRules({ ignored: DEFAULT_MODULE_CONFIG.context.ignored }));
+    const deep = await read.search({ query: "needle" });
+    expect(deep.matches.map((one) => one.path)).toEqual(["zz/target.ts"]);
+    expect(deep.truncated ?? false).toBe(false);
+    const many = await read.search({ query: "filler" });
+    expect(many.truncated).toBe(true);
+    // A file over the whole-read limit is still searched: 76 KB of styles is where the rule lives.
+    writeFileSync(join(at, "zz", "styles.ts"), `${"/* padding */\n".repeat(6000)}.coding-tool-state[data-tone=failed] { color: red; }\n`);
+    const big = await read.search({ query: "coding-tool-state", path: "zz" });
+    expect(big.matches.map((one) => one.path)).toEqual(["zz/styles.ts"]);
+  });
+
   it("不给 `regex` 就是字面量——元字符不当正则", async () => {
     const read = await reader();
     // 字面量档下这串谁也匹配不上。
diff --git a/packages/sdk/test/workspace-mutate.live.test.ts b/packages/sdk/test/workspace-mutate.live.test.ts
index ef1bfb0a..229a7fce 100644
--- a/packages/sdk/test/workspace-mutate.live.test.ts
+++ b/packages/sdk/test/workspace-mutate.live.test.ts
@@ -1,6 +1,6 @@
 import { createNodeHost } from "../src/node.js";
 import { createHash } from "node:crypto";
-import { mkdtempSync, readFileSync, rmSync, writeFileSync } from "node:fs";
+import { chmodSync, mkdtempSync, readFileSync, rmSync, statSync, writeFileSync } from "node:fs";
 import { tmpdir } from "node:os";
 import { join } from "node:path";
 import { afterEach, beforeEach, describe, expect, it } from "vitest";
@@ -108,6 +108,28 @@ describe.each(["write", "edit"] as const)("%s 的模型观察与真实磁盘一
   });
 });
 
+describe("超过 64KB 的文件", () => {
+  it("整读仍被拒并指向行窗；按行窗读得到任意一段；edit 照样能改", async () => {
+    const lines = Array.from({ length: 6000 }, (_, index) => `/* padding ${index + 1} */`);
+    writeFileSync(join(project, "styles.ts"), `${lines.join("\n")}\n.coding-tool-state[data-tone=unknown] { color: amber; }\n`);
+    const { runtime, root } = await ready();
+    try {
+      const runner = runtime.createSystemToolRunner(root.ref);
+      const tool = (name: string) => runtime.systemTools.find(one => one.name === name)!.registration;
+      await expect(runner({ registration: tool("read"), args: { path: "styles.ts" } })).rejects.toThrow(/offset and limit/);
+      const window = await runner({ registration: tool("read"), args: { path: "styles.ts", offset: 5999, limit: 5 } });
+      expect(window).toBe("/* padding 5999 */\n/* padding 6000 */\n.coding-tool-state[data-tone=unknown] { color: amber; }\n[lines 5999–6001 of 6001]");
+      const observation = await runner({ registration: tool("edit"), args: {
+        path: "styles.ts",
+        find: ".coding-tool-state[data-tone=unknown] { color: amber; }",
+        replace: ".coding-tool-state[data-tone=retry] { color: gray; }\n.coding-tool-state[data-tone=unknown] { color: amber; }",
+      } });
+      expect(observation).toContain("Status: applied to disk");
+      expect(onDisk("styles.ts").endsWith("/* padding 6000 */\n.coding-tool-state[data-tone=retry] { color: gray; }\n.coding-tool-state[data-tone=unknown] { color: amber; }\n")).toBe(true);
+    } finally { await runtime.shutdown(); }
+  });
+});
+
 // 真实 Host 证据：真实磁盘、真实写入、真实进程。
 describe("真实 Host 上的补丁", () => {
   it("准备阶段磁盘写入为零，Diff 可见", async () => {
@@ -216,6 +238,24 @@ describe("真实 Host 上的补丁", () => {
     await runtime.shutdown();
   });
 
+  it("改文件不改它的权限：可执行的脚本改完、回退后仍可执行；新文件是普通权限", async () => {
+    const { runtime, root, now } = await ready();
+    const mutator = runtime.createMutator({ rootRef: root.ref, mode: "build" });
+    const script = join(project, "run.sh");
+    writeFileSync(script, "#!/bin/sh\necho 1\n");
+    chmodSync(script, 0o755);
+    const edited = await mutator.applyPatch(await mutator.preparePatch({ path: "run.sh", nextText: "#!/bin/sh\necho 2\n" }), now);
+    expect(readFileSync(script, "utf8")).toBe("#!/bin/sh\necho 2\n");
+    expect(statSync(script).mode & 0o777).toBe(0o755);
+    await mutator.rewind(edited.checkpointId, now);
+    expect(readFileSync(script, "utf8")).toBe("#!/bin/sh\necho 1\n");
+    expect(statSync(script).mode & 0o777).toBe(0o755);
+    await mutator.applyPatch(await mutator.preparePatch({ path: "fresh.ts", nextText: "export {};\n" }), now);
+    const umask = process.umask();
+    expect(statSync(join(project, "fresh.ts")).mode & 0o777).toBe(0o666 & ~umask);
+    await runtime.shutdown();
+  });
+
   it("按检查点回退能把内容还原", async () => {
     const { runtime, root, now } = await ready();
     const mutator = runtime.createMutator({ rootRef: root.ref, mode: "build" });
diff --git a/packages/sdk/test/workstation.test.ts b/packages/sdk/test/workstation.test.ts
index 06ef798c..26d1abe1 100644
--- a/packages/sdk/test/workstation.test.ts
+++ b/packages/sdk/test/workstation.test.ts
@@ -227,6 +227,18 @@ describe("任务图", () => {
     expect(error.message).toContain("nothing changed");
   });
 
+  it("报一个节点已经在的状态只记进展，不算冲突；显式转换仍然严格", async () => {
+    const boards = createTaskBoardStore(newId);
+    let board = await boards.admit(good);
+    board = await boards.report({ ref: board.ref, expectedVersion: board.version, nodeId: "read", by: undefined, note: "started", atMs: 1, to: "running" });
+    board = await boards.report({ ref: board.ref, expectedVersion: board.version, nodeId: "read", by: undefined, note: "two callers found", atMs: 2, to: "running" });
+    const read = board.nodes.find((node) => node.id === "read");
+    expect(read?.state).toBe("running");
+    expect(read?.reports.map((report) => report.note)).toEqual(["started", "two callers found"]);
+    const error = await asyncFail(() => boards.transition({ ref: board.ref, expectedVersion: board.version, nodeId: "read", to: "running" }));
+    expect(error.message).toContain("cannot become running");
+  });
+
   it("依赖没满足不得调度", async () => {
     const boards = createTaskBoardStore(newId);
     const board = await boards.admit(good);
