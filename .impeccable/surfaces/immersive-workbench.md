Scope: immersive project workbench, Operate mode. Updated 2026-09-13 to record the accepted project home and its narrow-footer / quotation follow-up inside the existing graphite two-level navigation.

Current authority: specs/immersive-workbench-implementation/spec.md. Project home is the entry surface. Goal work defaults to the existing real terminal; Conversation is disabled by explicit user instruction. The sidebar uses a fixed project row, same-column root/plugin directories and text-only plugin strip. Both titlebars are 32px. Plugin activation is stored per project.

Directory follow-up: specs/immersive-directory-refinement/spec.md owns the 2026-09-12 fixes based on actual user screenshots. Directory width supports pointer/keyboard resize, per-project restore and double-click reset (236–520px, leaving 360px for the stage; phones stay a drawer). Goals use consistent 32px single-line rows, normal title weight and text status labels. Feed and Sessions use compact same-row search/filter tools and quieter empty states. Plugin directories do not show a bottom item-count footer; the account footer remains pinned. Legacy Goal ledger styles and legacy mobile chrome hiding explicitly exclude the immersive workbench. Current directory visual evidence is `.impeccable/review/directory/`.

Goals open in a fixed frame inset 24px (10px phone), with the real terminal and a 300px information/timeline rail. Below 840px frame width details overlay; below 600px viewport width the directory is a drawer. Expanded frames do not drag; collapse restores camera. Editors/readers manage covered controls and return focus. Work retains Session/PTY ownership and no opening action implicitly sends input.

Home, Goals, Sessions, Feed/Inbox/Sources, Market and in-workbench Artifacts share the new app stage. Settings retains its existing implementation. Old 70/30, top Goal tabs, root planning placeholders and vertically stacked phone workspace are superseded.

Initial implementation verification (2026-09-12): 1440/1024/390px, light/dark, real Goal event writeback, history and retry, market persistence and cross-project isolation, exact Artifact versions, real local PTY output and Registry readback. Functional level 3; packaged macOS app and external AI execution were not run in that pass. Preview is isolated from user project data.

Source: apps/workbench/src/{goals-page-renderer,immersive-shell}.ts, scripts/client/{immersive-navigation,plugin-workbench}.ts, styles/{immersive-navigation,goal-canvas}.ts; native Goals/Work/Artifacts retain content behavior. Visual evidence: .impeccable/review/production-*.png. Full verification and remaining boundaries: specs/immersive-workbench-implementation/progress.md.

2026-09-12 Graph follow-up authority: specs/graph-selection-and-project-home/spec.md. Graph selection is local to the canvas and highlights lineage; open via corner button/double-click/Enter. First view centers a startable leaf at 100%, retaining intentional camera changes. Its earlier home composition is superseded by the home contract below.

## Project home — 2026-09-13

Authority: `specs/poetic-project-home/spec.md`; footer and quotation follow-up: `specs/home-narrow-footer-quotes/spec.md`. The user accepted the interactive preview's quiet Google/Claude-like single-line composer near the bottom, with shortcuts immediately above it, local date/weekday/automatic quotations on the upper left and a small month calendar on the upper right. This remains an Operate surface within GoalBoard's incumbent neutral Light/Dark and violet theme. There is no greeting headline, recent Goal activity list, editable draft, quotation control bar, raster illustration or third-party favicon. Goals and the plugin directory retain their existing behavior.

Composition: the centered content is at most 800px wide; the upper context is at most 670px and the lower launch area at most 740px. Flexible space holds the launch area near the bottom without fixing it over content. The calendar is 200px wide, tightens to 180px at a 640px stage width, then becomes a centered 224px calendar in the single-column stack at 450px. The narrow order is date/quotation → month calendar → wrapping shortcuts → composer/status. The composer is 76px high with a 23px corner and faint shadow, reducing to 70px on the narrow stage. Shortcuts use 43px circular icon grounds and 88px text columns. Chinese and English use the same structure. All quotation pages share one grid cell, reserving the natural height of the longest content at the current width with a 112px minimum. Long text wraps completely; rotation does not shift the date, calendar or launch area, and directory resizing or returning home naturally recalculates the reserve.

Daily context: date and weekday follow local civil time and interface locale, synchronizing across midnight and when the page returns. Month browsing starts on Monday, supports previous/next month and marks today. A browsed month survives ordinary refresh synchronization. The calendar represents dates only. The quotation collection preserves the three existing sourced passages, followed in original order by all ten Adeptify English originals and their original attributions from `adeptify-next/client-app-adeptify/pc/shell/src/pages/login/BrandPanel.tsx`, for 13 total. Migrated text stays English in the Chinese interface. Attributions use quiet 11px text; the original three source links remain, while new attributions are plain text with no invented citations or claim of independent verification.

Quotation behavior: the five-second interval and 0.5-second fade-out/fade-in are unchanged. Inactive pages use `visibility: hidden`, `aria-hidden="true"` and `inert`, retaining layout instead of using `hidden` or `display: none`; only the active page is visible and available to focus or assistive technology. Only the outer group fades, and its descendants have no transitions so global reduced-motion rules cannot animate visibility. Hovering or focusing the quotation defers rotation; a hidden page or inactive home does not advance; reduced-motion preference switches text without fading. There are no quote play, pause or navigation controls.

Account footer: immersive navigation now owns the complete base styles at every width, independent of the legacy 761px desktop breakpoint. Name and local-space label always occupy separate rows with safely shrinking, ellipsized text. The account link uses theme ink without an underline; the 25px avatar and 26px Settings region stay centered and do not shrink. This applies to desktop, the 761/760px boundary, 600px and the 390px drawer. The existing global-settings destination is unchanged.

Shortcuts: first use contains only Add shortcut. Users add, edit or remove a name and http(s) URL in the modal; name is limited to 32 characters and URL to 4096, with a real host and no embedded username/password. Names render as text and all links share a local Lucide icon. Preferences restore from localStorage in the current browser or desktop profile, isolated by project_id (board_id in legacy single-board mode); they do not write Goal truth or synchronize across devices. Cancel saves nothing. Validation, save and remove failures keep the modal and entered content available for correction or retry; read failures and external-open failures show honest feedback. Closing returns focus to the trigger or Add shortcut. Web links open a new page; the macOS adapter opens them in the system browser through the main-window, local-origin-restricted native channel, with protocol/host/credential validation repeated natively.

Agent boundary: the one-line input and send button both use native disabled state and show “Agent 尚未开放”. There is no home draft capture, message submission, simulated reply or Harness integration. Earlier browser draft data is left intact but no longer consumed. This home-specific composer does not change the Goal workspace: real Work-owned terminals still open only through an explicit action, opening a Goal never starts/sends/rebinds execution, and Conversation remains disabled.

Source: `apps/workbench/src/project-home.ts`, `apps/workbench/src/styles/project-home.ts`, `apps/workbench/src/scripts/client/project-home.ts`, `apps/workbench/src/scripts/client/project-home-shortcuts.ts`. Colors inherit `apps/workbench/src/styles/immersive-navigation.ts`. Native external opening lives in `apps/desktop/src/shell.ts` and `apps/desktop/adapters/tauri/src/external_links.rs` with the main-window capability/permission.

Initial home evidence: the 10 home/i18n checks, 7 Graph/navigation/native-bootstrap checks and 20 Rust checks passed; independent review returned ship with no material findings. A manual run of the same macOS debug binary added a temporary local-health shortcut, opened its exact destination in Chrome while GoalBoard remained on home, then removed the shortcut and cleaned up the test window. Functional level 3 with Agent Harness intentionally unavailable; this is not a distribution-release claim. The home spec owns that verification record.

Footer and quotation follow-up evidence: four relevant browser tests passed across the verification runs. `.impeccable/review/home-footer-quotes/` holds eight Light/Dark footer captures at 761/760/600/390px. `.impeccable/review/home-start/{desktop,desktop-dark,user-1024,user-1024-dark,mobile,mobile-dark,english-1024,english-760,english-mobile,dialog-mobile}.png` holds the ten current home captures, covering long quotations, Chinese/English and the shortcut modal; `english-760.png` adds the intermediate-width case. The final independent review marked F1, F2 and F3 resolved, with disposition ship. Functional level 3 and the disabled Agent boundary remain unchanged; this follow-up does not re-claim the earlier native verification.
